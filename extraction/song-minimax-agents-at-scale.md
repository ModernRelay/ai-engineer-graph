# SPIKE extraction — "Agents at Scale: Inside MiniMax's Model and the Infrastructure Behind It" (Olive Song, MiniMax + Dan Fu, Together AI) — FOR REVIEW

Source transcript: `transcripts/song-minimax-agents-at-scale.txt` (auto-captions — quotes are paraphrases, not verbatim; several garbles, see review notes 2–3).
Video: https://youtu.be/AVMr9PMINyo — AI Engineer World's Fair, published 2026-07-31.
`stagingTimestamp` for the artifact and all dated nodes (signals, knowhows): 2026-07-31 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.
Speaker attribution: this is a **two-guest fireside**, not a solo talk — Olive Song (RL research lead, MiniMax: post-training and model shipping) and "Dan" (VP of Kernels, Together AI: inference/GPU optimization), plus an unnamed Together AI moderator (see review note 1). Claims are attributed per speaker below.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-song-minimax-at-scale` | Agents at Scale: Inside MiniMax's Model and the Infrastructure Behind It (Olive Song, MiniMax; Dan Fu, Together AI — AI Engineer World's Fair) | youtube | https://youtu.be/AVMr9PMINyo |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-olive-song`, `ContributedByExpert → exp-dan-fu`.

## Experts (2 new)

| slug | name | edges |
|---|---|---|
| `exp-olive-song` | Olive Song (research lead of RL at MiniMax — owns final training and shipping of the model, "everything before the inference") | `AffiliatedWithCompany → co-minimax` |
| `exp-dan-fu` | Dan Fu (VP of Kernels, Together AI — leads inference and GPU optimization; long-running sparse-attention/kernel work "going all the way back to my PhD", recent Stanford lecture) | `AffiliatedWithCompany → co-together-ai` (⚠ surname inferred — the captions only ever say "Dan"; see review note 1) |

## Companies (2 new)

| slug | name | type | note |
|---|---|---|---|
| `co-minimax` | MiniMax | developer | Chinese frontier lab shipping open-weight models (M2 series, M2.5, M2.7, M3); mission framed in-talk as "intelligence with everyone" — open weights so developers contribute feedback/PRs and serving providers can optimize them. Joins `co-zhipu-ai`, `co-moonshot-ai`, `co-deepseek` as the corpus's Chinese-lab set |
| `co-together-ai` | Together AI | developer | Inference/serving provider and kernel shop ("how do you make intelligence abundant") — serves open models at scale, claims the lion's share of MiniMax M3 token usage, writes the kernels and publishes kernel benchmarks |

Reused: `co-zhipu-ai`, `co-moonshot-ai`, `co-deepseek`, `co-anthropic`, `co-openai` **[registry]** — all name-checked as the closed/open frontier comparison set. Kept in prose (no node/edge): SpaceX (⚠ garble, review note 3), Stanford, ICLR.

## Elements (6 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-minimax-m3` | MiniMax M3 | product | training | MiniMax's strongest and first natively multimodal open-weight model: trained multimodal **from step zero** (text + image together) rather than bolted on — the lab solved the mid-training collapse "normal for many other labs" — with 1M-token context and MiniMax sparse attention. Attention maps show text tokens attending to visual tokens, so the modalities "naturally understand each other"; the payoff shown is web-app development where the model can look at the rendered site and optimize against it during RL. Highlighted benchmarks: SVGBench, KernelBench, OSWorld. Applications seen in the wild: agentic multimodal agents, computer use, and game development (the "hidden gem") |
| `el-minimax-sparse-attention` | MiniMax sparse attention | technology | inference | M3's sparse-attention scheme, deliberately different from DeepSeek's and GLM's variants; the mechanism behind the 1M context. Serving-side view: every open model now differs in attention, MoE choices and quantization enough to need new or modified kernels, but the optimization and kernel-writing lessons transfer between sparse-attention families |
| `el-self-evolution` | Self-evolution (model builds its own harness) | concept | training | MiniMax's M2/M3-series post-training loop: the model builds its own harness, trains inside it, and produces the next checkpoint. Its second face is organizational — the lab actively uses its own models to speed up internal development, and the resulting work becomes a source of internal evals "closely related to our own work" that are unavailable to outsiders |
| `el-parallel-kernel-bench` | Parallel Kernel Bench | framework | infra | Together AI benchmark (⚠ name per captions) built by surveying every way people serve model inference and collecting the speedups that have **no good kernel yet** — i.e. a benchmark of *unsolved* problems. Designed so bench-maxing is the point: any kernel produced by overfitting to it gets harvested straight into the production inference stack |
| `el-day-zero-inference-ramp` | Day-zero inference ramp | ops | inference | The serving-side launch playbook for a new open model: get architecture details early under partnership, write/modify/benchmark kernels before launch, optimize for output **quality** on day zero, then work a standing list (KV cache, attention kernels, quantization) so the same weights measurably speed up between day 0, day 7 and day 14 — "improving performance since last month? did you mean since last night?" |
| `el-kv-cache-as-distributed-store` | KV cache as a distributed store | concept | inference | At agentic context lengths (concurrent requests of 500K–1M tokens) KV-cache management stops being a serving detail and becomes a distributed-filesystem/large-database problem — where to store the cache, how to know you have seen this prefix before, how to fetch it, how to move it between machines. Framed as rediscovered undergraduate distributed systems: "not that complicated, but you do have to do a good job" |

Element edges: all six `IdentifiedInArtifact → ia-aie-song-minimax-at-scale`; `el-minimax-m3` `DevelopedByCompany → co-minimax`, `UsesElement → el-minimax-sparse-attention`, `ExemplifiesPattern → pat-sovereign-ai` **[registry]**; `el-minimax-sparse-attention` `DevelopedByCompany → co-minimax`; `el-self-evolution` `DevelopedByCompany → co-minimax`, `UsesElement → el-agentic-environment` **[registry]**, `EnablesPattern → pat-accelerated-research` **[registry]**; `el-parallel-kernel-bench` `DevelopedByCompany → co-together-ai`, `EnablesPattern → pat-accelerated-research` **[registry]**; `el-day-zero-inference-ramp` `UsesElement → el-kv-cache-as-distributed-store`; `el-kv-cache-as-distributed-store` `UsesElement → el-prefix-caching` **[registry]**; `el-token-factory` **[registry]** `DevelopedByCompany → co-together-ai` (⚠ proposed — Together is the archetype the registry brief describes; drop if you want that element vendor-free).

Reused elements (no new nodes): `el-agentic-environment`, `el-prefix-caching`, `el-token-factory`, `el-glm-52`, `el-kimi-k25`, `el-dynamic-quantization` **[registry]**.

## Signals (6 new)

All: `SpottedInArtifact → ia-aie-song-minimax-at-scale`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | other edges |
|---|---|---|---|---|
| `sig-minimax-m3-open-multimodal` | training | MiniMax ships its strongest model (M3) open-weight on principle: multimodal-from-scratch (image data from step zero, collapse problem solved), 1M context, MiniMax sparse attention — released open because "we want to have intelligence with everyone", developers contribute feedback and PRs back, and serving providers can make it faster for everyone. First multimodal model in the line (M2/M2.5/M2.7 were text-only), and the multimodality is load-bearing: the model can look at a site it built and optimize against the rendering during RL | `FormsPattern → pat-sovereign-ai` **[registry]** | `OnElement → el-minimax-m3`, `el-minimax-sparse-attention`; `RelevantCompany → co-minimax` |
| `sig-open-frontier-not-far-behind` | training | Together AI's kernels lead, from the serving vantage point on essentially every open model: the recurring "OpenAI and Anthropic are so far ahead" question should be "put to bed" — with M3, GLM and Kimi "the open-source frontier really can catch up, and it's not even that far behind". Song's version from the lab side: models were already accelerating their own development a year ago, that acceleration compounds, and it is how open-weight labs close the gap with frontier labs | `FormsPattern → pat-sovereign-ai` **[registry]** | `OnElement → el-glm-52`, `el-kimi-k25`, `el-minimax-m3` **[registry ×2]**; `RelevantCompany → co-together-ai`, `co-minimax`, `co-zhipu-ai` **[registry]**, `co-moonshot-ai` **[registry]** |
| `sig-agentic-workloads-reshape-inference` | inference | The serving workload has shifted from chat (a few-thousand-token system prompt plus chat logs) to agentic (whole codebases uploaded, hundreds of multi-turn tool calls inside a harness, plus multimodal turns) — and that shift, not model quality, now decides which part of the inference stack gets optimized: KV cache, prompt/prefix handling, routing and kernels are all different problems under agentic traffic. Together tracks workload evolution explicitly to adapt its engines | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-kv-cache-as-distributed-store`, `el-prefix-caching` **[registry]**, `el-token-factory` **[registry]**; `RelevantCompany → co-together-ai` |
| `sig-kv-cache-is-a-storage-problem` | infra | Concurrent 500K–1M-token agentic requests turn KV-cache handling into "recreating a distributed file system … or a very big database": storage placement, prefix-recognition, fetch, and machine-to-machine transfer. The frontier serving problem is being solved with 1970s systems engineering that "most of us skipped in undergrad and are rediscovering live in industry" | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-kv-cache-as-distributed-store`, `el-day-zero-inference-ramp`; `RelevantCompany → co-together-ai` |
| `sig-environments-not-scale-drive-long-horizon-rl` | training | MiniMax's RL lead on training for kernel optimization and 12-hour paper-replication runs: the deciding variables are **the environment, the data, and how the problem and rewards are formulated** — not model scale. Hard tasks (GPU-constrained kernel work, reproducing an ICLR paper end to end) need environments deliberately designed so RL can iterate, plus algorithm tweaks for training efficiency. Verification is built in: models submit several times per task, each attempt is scored, and separate validation/test checks catch runs where "the models would hack" | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-agentic-environment` **[registry]**, `el-minimax-m3`; `RelevantCompany → co-minimax` |
| `sig-benchmark-of-unsolved-problems` | infra | Together AI released Parallel Kernel Bench, deliberately built from kernel speedups that **do not yet exist**, with an inverted stance on bench-maxing: "if you overfit to it, that's great, because we'll take those kernels and use them to accelerate inference." The moderator's gloss — if researchers publish the genuinely useful benchmarks and everything gets bench-maxed into distribution, that is the good outcome, not the failure mode | `FormsPattern → pat-accelerated-research` **[registry]** | `OnElement → el-parallel-kernel-bench`; `RelevantCompany → co-together-ai` |

## Insights (2 new)

| slug | name / brief | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-open-weights-are-a-two-sided-loop` | Open weights are not a give-away but a loop with two suppliers: the lab ships the model and the architecture details early; the serving company writes the kernels, closes the quality gap at day zero and keeps compounding speedups afterwards; usage and PRs come back to the lab. Neither side can deliver "abundant intelligence" alone — the lab cannot serve at scale, the serving provider cannot train, and each new open model's distinct attention/MoE/quantization choices force the partnership to start early rather than at launch | `HighlightsPattern → pat-sovereign-ai` **[registry]** | `ReliesOnElement → el-day-zero-inference-ramp`, `el-token-factory` **[registry]** |
| `ins-environment-design-is-the-post-training-lever` | For long-horizon agentic capability the lever has moved from model and data scale to **environment design** — formulating the problem, the reward, and the sandbox so that RL can iterate against a task that takes twelve hours and real GPUs. The same insight makes hacking a first-class engineering concern (multi-submission scoring, held-out validation) and makes a lab's internal, work-derived evals a private asset outsiders cannot replicate | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-agentic-environment` **[registry]**, `el-self-evolution` |

## KnowHow (2 new)

All: `SourcedFromArtifact → ia-aie-song-minimax-at-scale`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-day-zero-model-serving` | Ramp a new open model from day zero | Partner before launch and take the architecture details early (attention scheme, MoE layout, quantization); triage against your existing kernels — reuse, modify, or write from scratch; make **quality**, not throughput, the day-zero bar so users get the model the lab intended; keep a standing optimization backlog (KV cache, attention kernels, quantization) and burn it down weekly so the same weights get faster at day 7 and day 14; carry lessons across sparse-attention families rather than restarting per model; leave no stone unturned — "if someone tells you you can't do the thousand-and-first thing, try harder" | `ReferencesElement → el-day-zero-inference-ramp`, `el-kv-cache-as-distributed-store`, `el-minimax-sparse-attention`, `el-prefix-caching` **[registry]** |
| `how-train-and-eval-long-horizon-tasks` | Train and evaluate 12-hour agentic tasks | Spend the effort on environment and data design first — the reward formulation and sandbox matter more than scale for kernel-optimization or paper-replication tasks; let the model submit multiple times and grade every intermediate submission instead of only the final artifact; run separate validation/test checks to distinguish real improvement from reward hacking; adjust the RL algorithm for training efficiency so emergent behaviour shows up within a feasible number of iterations; build internal evals out of your own team's real work — they stay relevant and cannot be gamed from outside; and if you publish a benchmark, consider publishing *unsolved* problems so overfitting produces something you actually want | `ReferencesElement → el-agentic-environment` **[registry]**, `el-self-evolution`, `el-parallel-kernel-bench` |

## Dropped

- **The unnamed moderator** — a third Together AI voice (asks the questions, cites "we have the lion's share of MiniMax M3 token usage", ran a long-horizon-tasks workshop on the Monday). Never named in the captions; no Expert node. His bench-maxing gloss is folded into `sig-benchmark-of-unsolved-problems`.
- **"Ingrid"** (Together colleague, one mention) — no node.
- **SVGBench / KernelBench / OSWorld** — named as M3's highlighted benchmarks with no substantive content; kept as prose inside `el-minimax-m3` rather than coined (contrast `el-swe-bench`, coined only once the corpus leaned on it repeatedly).
- **The M2.5 / M2.7 serving history and the Las Vegas event where the partnership started** — anecdote, prose only.
- **Quantization**: mentioned twice as an optimization surface. No edge to `el-dynamic-quantization` **[registry]** (that element is Unsloth-specific); listed as reused only if you want a generic quantization anchor at seeding.
- **SpaceX / 10% FLOP utilization** — see review note 3; no `co-spacexai` **[registry]** edge.

## Review notes

1. **Speaker identification.** The captions name the guests only as "Olive" and "Dan". Olive Song is given by the talk title. "Dan" self-identifies as *VP of Kernels at Together AI*, leading inference and GPU optimization, with a PhD-era history on sparse attention and a recent Stanford lecture — that is Dan Fu on the public record, and I have coined `exp-dan-fu` on that basis with the inference flagged. If you do not want byline-inference in the graph, the fallback is `exp-dan-together` / name "Dan (VP of Kernels, Together AI)" — the affiliation itself is transcript-stated and safe. Precedent both ways in the registry: `exp-aamir-shakir` (surname from public record) vs `exp-shashi` (surname left unresolved).
2. **Company coining note.** MiniMax previously appeared in the corpus only as prose (`lee-chan-snapchat-idea-velocity.md`, batch 6, where it was the budget fallback model and explicitly *not* coined). This talk is a MiniMax primary source, so `co-minimax` is coined here — that reverses the earlier decline deliberately, same as the `co-palantir` reversal in batch 14. `co-together-ai` is a genuine first appearance despite being one of the larger open-model serving providers.
3. **Caption garbles.** (a) *"I think at Anthropic we can do a lot better with the hardware that we're using"* — spoken by Together's VP of Kernels about his own shop; almost certainly **"at Together"**. I have not built any Anthropic edge on it. (b) *"SpaceX said they would have like 10% flop utilization"* — attribution unreliable (the registry already carries a `co-spacexai` garble flag from batch 5); dropped, the underlying claim "we underutilize our GPUs a lot right now" is kept in prose. (c) *"JLM"* → GLM, *"Kimmy"* → Kimi (both resolved to `el-glm-52` / `el-kimi-k25`), *"deep seek"* → DeepSeek, *"KB cache"* → KV cache throughout, *"a gen tech multi-modal agents"* → agentic multimodal agents, *"bench maxing"* is verbatim. (d) **`el-parallel-kernel-bench` name is caption-only** — verify the exact product name against Together's release before public-facing use.
4. **`el-self-evolution` and the uncoined `pat-adaptive-harness` candidate.** "The model builds its own harness, then trains inside it, and you get the next checkpoint" is the cleanest frontier-lab statement of the adaptive-harness thesis the corpus has (RELAI, DSPy Flex, AutoAgent, Uber's optimizer are all vendor-side). Per instruction I did **not** coin; `sig-self-evolution` was not created as a separate signal — the material sits inside `sig-open-frontier-not-far-behind` and `ins-environment-design-is-the-post-training-lever`. If `pat-adaptive-harness` is coined at review, the natural move is to split a `sig-minimax-self-evolution-loop` out of this file and home it there.
5. **Counter-evidence to the uncoined `pat-benchmark-trust-crisis`.** `sig-benchmark-of-unsolved-problems` is the corpus's first *pro-bench-maxing* argument: overfitting is welcomed because the benchmark's contents are the deliverable. It doesn't refute the contamination/economic-motive legs (Shi, Cai) but it does mark a class of benchmark that is immune to them — the benchmark is a work order, not a scoreboard. Weigh it as a scoping constraint on the candidate's brief rather than a counter-instance.
6. **`pat-environments-economy` (uncoined) gains a frontier-lab data point.** Song's "the key there is still the environments and the data" is the first *model-producer-side* confirmation that environment design is the scarce input; every prior data point (Harbor, Prime Intellect, Abundant AI, Snorkel, Andon, Cai) came from the supply side. No edge created.
   **Same-wave siblings (not yet reconciled into the registry):** `garg-theta-rethinking-environments.md` and `heiner-surge-benchmaxxing.md` sit in this same publish window and overlap this file directly — Garg on environment design for long-horizon work, Heiner on benchmark construction and gaming (he coins `el-benchmaxxing`). At reconciliation, consider linking `sig-benchmark-of-unsolved-problems` and `sig-environments-not-scale-drive-long-horizon-rl` to those elements rather than leaving the vocabulary duplicated; I left no cross-file edges because neither sibling file has been reviewed.
7. **Pattern-fit honesty.** `sig-agentic-workloads-reshape-inference` and `sig-kv-cache-is-a-storage-problem` are homed on `pat-model-not-bottleneck` because the claim is that serving value migrated to the layers around the weights. If you read that pattern strictly as an *application*-layer claim, both drop to pattern-less; they are not `pat-harness-over-model` (the harness here is the customer, not the fix).
