# SPIKE extraction — "The State of Model Routing" (panel: NVIDIA / Cognition / OpenRouter) — FOR REVIEW

Source transcript: `transcripts/panel-state-of-model-routing.txt` (auto-captions — quotes are paraphrases, not verbatim).
Video: https://youtu.be/QHBjufYK8TA — AI Engineer (Local AI summit track), published 2026-08-06.
`stagingTimestamp` for the artifact and all signals: 2026-08-06 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.

**Format note:** panel moderated by Carter Abdallah (NVIDIA — "I'm Carter… you guys heard from me a little bit earlier"; node defined in `abdallah-nvidia-local-models.md`, this batch). Panelists: "Walden," co-founder of Cognition (⚠ first-name-only — certainly Walden Yan per public record, NOT coined by rule); "Donne"/"Tuhin," NVIDIA, model evals + router builder (first-name-only via garble, NOT coined); "Alex from OpenRouter," arriving late from the airport (⚠ first-name-only — certainly Alex Atallah, CEO, per public record; NOT coined by rule). The panel repeatedly references the same-day local-AI panels.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-panel-state-of-model-routing` | The State of Model Routing (panel: NVIDIA / Cognition / OpenRouter — AI Engineer) | youtube | https://youtu.be/QHBjufYK8TA |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-carter-abdallah` (defined in `abdallah-nvidia-local-models.md`, this batch).

## Experts (0 new)

**⚠ NOT coined (first-name-only, per the panel rule) — all three add-options documented:**
- **Cognition co-founder "Walden"** — public record: **Walden Yan**, Cognition co-founder. Owns the Devin Fusion material (~40% of the file). Add `exp-walden-yan` + `AffiliatedWithCompany → co-cognition` + ContributedByExpert on title-card confirmation.
- **OpenRouter "Alex"** — public record: **Alex Atallah**, OpenRouter co-founder/CEO. Owns the auto-router/OpenClaw/marketplace material. Add `exp-alex-atallah` + `AffiliatedWithCompany → co-openrouter` on confirmation.
- **NVIDIA "Donne"/"Tuhin"** — captioned "Donne" at intro but addressed as "Tuhin" by the Cognition speaker mid-panel; works on model evaluations (accuracy/efficiency/cost) and builds a router at NVIDIA. Surname never appears; unresolved. Owns jagged-capabilities, Flex Run, probes, and the KV-cache-economics material.

## Companies (1 new)

| slug | name | type | note |
|---|---|---|---|
| `co-openrouter` | OpenRouter | developer | Marketplace for language models ("we exist at" the aggregation layer — can't see into providers' KV caches, passes cache-hit savings through to customers). Ran an auto-router for ~2 years with near-zero adoption before the January-2026 OpenClaw explosion; now ships multiple routers (Pareto code, Fusion) and publishes public usage rankings (top model by dollar spent per task category). Self-describes as ecosystem-driven, building orchestration primitives (sub-agent, advisor tool) for partners |

Reused **[registry]**, edge-only: `co-cognition` **[batch 11]** (Devin; released the Fusion model router; trains its models to be collaborators; bought direct compute capacity from providers in 2024 — before cache pricing existed — to make agents affordable), `co-nvidia` **[batch 2]** (router research; Flex Run; Nemotron post-trained for routing harnesses; Dynamo prefix-cache optimizations; DGX Spark local-utilization work), `co-anthropic` **[seed]** (Opus/Haiku economics; "sorry, Anthropic, if you're watching" on long-context intelligence falloff).

## Elements (7 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-devin-fusion` | Devin Fusion (Cognition model router) | product | inference | Cognition's just-released router: **Fable-level intelligence at ~40% lower cost** by letting the frontier model do planning and hard decisions while delegating implementation to a cheaper (often open) model — which, being cheaper, can attack the task with *more* depth (e.g. three sub-explorations of a codebase can beat one frontier pass). Design philosophy: never route the user to a dumber model and strand them — a frontier agent always "keeps tabs" even when not doing the work, detecting when the delegate is out of its depth. Explicitly framed as v1 of a young field: "I hope a year from now people look back on these as really legacy ideas" |
| `el-sidekick-architecture` | Sidekick delegation (persistent single delegate) | concept | harness | Cognition's alternative to main-agent + sub-agent fan-out: **one** sub-agent ("sidekick") with a continuously running context, so the main agent never re-provides history — it all stays in KV cache at ~10× cheaper token pricing; smart/cheap roles can swap between the two ends without cache loss. Context flows are asymmetric by design: most context goes to one model only; the delegate is tuned to *present* condensed context back (files referenced by path, not dumped). ⚠ naming: distinct from `el-monday-sidekick` [registry] — unrelated products; captions also garble it as "psychic"/"sidecar" |
| `el-jagged-capabilities` | Jagged model capabilities | concept | inference | The routing-side premise: coding (or any domain) is not one capability — within data-science work, scikit-learn vs matplotlib vs model-building strengths differ per model, driven by each lab's training corpora and post-training teacher/sub-task mixes. A model that wins a coding benchmark is not "plain better at every task"; overlapping strengths create an **arbitrage** an orchestrator can harvest — "up to 10% higher accuracy" on router benchmarks (⚠ "LM router bench" garble), depending on model pool and task. Routing = "intimately understanding the behavior, strengths and weaknesses of different models, and applying them thusly" |
| `el-flex-run` | Flex Run (NVIDIA elastic model footprints) | technology | inference | NVIDIA technology (⚠ name from captions): a main model is distilled into smaller footprints **inside one model artifact**, and the system switches which footprint does the decoding per task — activating "a class of model or a section of weights depending on the complexity at hand." Paired claims: with access to the training recipe you can literally determine whether a question is in-distribution, and distillation-gap studies across teachers tell you where the small footprints are weak — both "work very well for open models" |
| `el-hallucination-probes` | Hallucination/lostness probes | technology | inference | Routing trigger built from model internals: linear/magnitude probes (and other classifier heads) over internal state — the prefill "is just a vector at the end of the day" — rating how strongly a model is tending toward hallucination, as a proxy for "how lost a model is in its thinking." Proposed as the principled alternative to token-count heuristics for deciding when a small model needs rescue |
| `el-kv-cache-economics` | KV-cache economics | concept | inference | The cost physics under multi-model systems: cached tokens are ~10× cheaper, so architecture follows the cache — switching models or compacting **takes a cache miss**; cache lifetimes (~5 min) are operational choices (GPU memory eviction), not physics, and self-hosters can extend them; a scheduled cache refresh can carry a "free" frontier-model check; providers amortize everyone's workload shape into one price, so self-hosting a known shape (e.g. 32K cache / 1K in / 1K out) "you'll likely pay much less"; futures floated: S3-backed cache offload, dynamic cache durations, switching-tolerance knobs. ⚠ merge-check vs `el-prompt-caching` / `el-prefix-caching` **[registry, batch 8]** — those nodes name the mechanism; this one is the economics-as-design-constraint claim. Fold at seeding if preferred |
| `el-openrouter` | OpenRouter platform (auto-router, Pareto code, Fusion, rankings) | product | inference | Product node for the marketplace + its routers: **auto-router** (2 years old, adoption ≈ 0 until OpenClaw made it explode — previously "a discovery play"); **Pareto code** (Pareto-optimal coding model under a tunable threshold); **Fusion** (orchestrates multiple models into a fused result; published deep-research results a couple weeks prior — smart model as the outer/wrapper model won); public **usage rankings** by dollar spent per category; cache-savings passthrough. Goal stated: "good primitives developers can use to get really advanced with model orchestration… and an easy slug that works with all harnesses" |

Registry element reuse (no new nodes, edges only):

| slug | reuse note |
|---|---|
| `el-model-routing` **[batch 8]** | The panel's subject; the batch-8 brief (route to cheapest adequate model, cheap router model) is exactly the "naive initial routing" this panel declares fragile — see Review note 3 for the recommended brief widening |
| `el-devin` **[batch 14]** | The product Fusion lives in; doom-loop recovery knowledge as the prompt moat |
| `el-terminal-bench` **[batch 11 late]** | The cost inversion evidence: "if you run terminal bench on Opus and Haiku, Opus will do about three times better at **1/10 the cost** of Haiku" |
| `el-openclaw` **[seed]** | The adoption detonator: OpenClaw's ~10-minute heartbeats to your default model made Opus-as-default absurd and created the first mass two-tier routing need |
| `el-gepa` **[batch 11]** | "Have you looked into prompt tuning, say Japa?" — fifth-plus corpus corroboration of the garble; Cognition is "less bullish" on gradient-style prompt tuning vs smart-model prompt repair |
| `el-context-compaction` **[batch 6]** | Reused twice: compaction as the solved problem routing borrows for cross-model context handoff; and compact-for-intelligence-not-cost ("never recommend using these models past ~200K tokens, under 100K if you can — intelligence falls off a cliff") |
| `el-prompt-caching` **[batch 8]** | The mechanism node under `el-kv-cache-economics` (merge-check flagged there) |
| `el-dgx-spark` **[batch 6]** | The idle-local-compute routing case: "I bought this DGX Spark and I know I'm not at 100% utilization… I'm only paying for the electrons" |

Element edges: all seven new `IdentifiedInArtifact → ia-aie-panel-state-of-model-routing`; all eight reused elements above also `IdentifiedInArtifact → ia-aie-panel-state-of-model-routing` (additional artifact each).
`el-devin-fusion` `DevelopedByCompany → co-cognition` **[registry]**, `UsesElement → el-sidekick-architecture`, `UsesElement → el-context-compaction` **[registry]**; `el-openrouter` `DevelopedByCompany → co-openrouter`; `el-flex-run` `DevelopedByCompany → co-nvidia` **[registry]**; `el-sidekick-architecture` `UsesElement → el-kv-cache-economics`; `el-jagged-capabilities` `EnablesElement → el-model-routing` **[registry]**; `el-hallucination-probes` `EnablesElement → el-model-routing` **[registry]**;
`el-devin-fusion` `ExemplifiesPattern → pat-model-not-bottleneck` **[registry]** (productized periphery around interchangeable models); `el-kv-cache-economics` `EnablesPattern → pat-model-not-bottleneck` **[registry]**.

## Signals (6 new)

All: `SpottedInArtifact → ia-aie-panel-state-of-model-routing`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | extra edges |
|---|---|---|---|---|
| `sig-routing-becomes-a-product-layer` | inference | Model routing is crystallizing from research into a product category in real time: Cognition ships Devin Fusion (**Fable-level intelligence at ~40% lower cost** — frontier model plans, cheaper implementation model executes with *more* depth: three sub-explorations of a codebase can out-comprehend one frontier pass); OpenRouter ships Pareto code and Fusion on top of its marketplace; NVIDIA runs router research and evals; and the moderator's framing — "there isn't a very clear solution here… a lot of space for startups" — is stated as the opportunity. The mechanism-level premise is jagged capability: per-model subtask strengths from divergent training corpora create an arbitrage worth "up to 10% higher accuracy," and cheaper per-token delegates can spend many more tokens within the same budget | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-devin-fusion`, `el-openrouter`, `el-jagged-capabilities`, `el-model-routing` **[registry]**; `RelevantCompany → co-cognition` **[registry]**, `co-openrouter`, `co-nvidia` **[registry]** |
| `sig-small-models-thrash-out-of-distribution` | inference | The panel's counterweight, from both router vendors: cheap models are only cheap **in distribution**. "If you run Terminal-Bench on Opus and Haiku, Opus will do about three times better at 1/10 the cost of Haiku" — out-of-distribution small models inflate cost through crazed tool-calling loops; on OpenRouter's public rankings **the top model by dollar spent on classification tasks is Opus**; naive task-type routing is "extremely fragile" for agentic work because task complexity mutates mid-session (explore → implement → live-debug) and "you don't want to be left with some subpar model for the task you're now on"; larger models are also more token-efficient readers (subset reads, single-command verification). Cognition's mitigation: the guarantee of frontier intelligence always present — a watcher agent detecting when the delegate is out of its depth | `ContradictsPattern → pat-model-not-bottleneck` **[registry]** — scoped counter: for out-of-distribution agentic work the model's intelligence, not the periphery, still decides both quality *and* cost | `OnElement → el-terminal-bench` **[registry]**, `el-model-routing` **[registry]**, `el-devin-fusion`; `RelevantCompany → co-openrouter`, `co-anthropic` **[seed]** |
| `sig-openclaw-heartbeat-segmented-the-market` | inference | A dated adoption discontinuity: OpenRouter's auto-router ran ~2 years with "no real usage" (people wanted specific models; it was a discovery play) — then **exploded around January 2026 with OpenClaw**, whose ~10-minute heartbeats to your default model made Opus-as-default economically absurd. One very popular app with **two completely different intelligence needs**, plus open models good enough for the low tier, segmented the market overnight; "a lot more segmentation blossomed afterwards" into today's router products. The fusion rationale generalizes it: "you want to benefit from all the data being trained on across all the labs, and not just the data from one lab" | — (**held pattern-less** — this is the provider-commoditization space where `pat-provider-blind-ai` was retired; per instructions no resurrection. Ledger note: if a provider/model-market-structure candidate ever forms, this dated discontinuity plus the all-labs-data rationale is prime evidence. See Review note 2) | `OnElement → el-openrouter`, `el-openclaw` **[seed]**, `el-model-routing` **[registry]**; `RelevantCompany → co-openrouter` |
| `sig-collaboration-moves-into-the-weights` | training | Routing's endgame per the panel is not harness logic but trained behavior: Cognition RLs its models **in both orchestration roles** (model-as-orchestrator deciding delegation; model-as-executor following another model's instructions) and expects co-designing models with the orchestration system to be "a big lift in the next step"; new frontier models (Fable family, GPT-5.5/5.6) are "themselves naturally collaborative and better at delegation" — "smarter models get better and better at delegating"; NVIDIA post-trains Nemotron for harnesses including routing, and Flex Run moves the routing switch *inside* the model artifact (per-task footprint activation). Closing consensus: "models are going to get good at routing to other models" | `ContradictsPattern → pat-harness-over-model` **[registry]** — the Steinfurt/batch-6 counter shape: a function built as scaffolding (routing/orchestration) being absorbed into model weights | `OnElement → el-flex-run`, `el-devin-fusion`; `RelevantCompany → co-cognition` **[registry]**, `co-nvidia` **[registry]** |
| `sig-routing-needs-open-interiors` | inference | The routing instruments the NVIDIA seat describes all require seeing inside the model: determining whether a question is in-distribution "works very well for open models — or any model you have access to its data for," because you can check the training recipe directly; distillation-gap studies across teachers locate where small footprints are weak; hallucination probes read internal state (linear/magnitude probes over prefill vectors) to score how lost a model is. The marketplace seat states the complementary limit: OpenRouter "can't see into the KV caches of models unless we're running them ourselves" — aggregators without interior access are structurally confined to price/cache optimization. Routing quality is thus a function of model transparency | `FormsPattern → pat-sovereign-ai` **[registry]** | `OnElement → el-hallucination-probes`, `el-flex-run`, `el-jagged-capabilities`; `RelevantCompany → co-nvidia` **[registry]**, `co-openrouter` |
| `sig-local-cloud-hybrid-routing` | inference | The routing axis "most relevant to this crowd": when to run locally vs reach for frontier. Named cases — **privacy routing** (detect sensitive information in a prompt, keep that inference on-device, anonymize before sending derived work to the cloud) and **utilization routing** (an under-used DGX Spark costs "only the electrons coming in from my power bill, but I'm paying full price for the tokens in the cloud" — so schedule heartbeats and background work onto idle local compute, and spawn collaborative agents to raise edge utilization when memory, not compute, is the bound). NVIDIA states in-flight work here; "the hybrid of local and cloud starts to really emerge as its own sector" | `FormsPattern → pat-sovereign-ai` **[registry]** | `OnElement → el-model-routing` **[registry]**, `el-dgx-spark` **[registry]**; `RelevantCompany → co-nvidia` **[registry]** |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-routing-centralizes-into-a-controller` | Routing is evolving from per-prompt model picking into a persistent session-level controller — and the panel argues the controller is structural: applications built on non-deterministic components "operate in a very low-trust environment," no participant has a perfectly visible world, so "there will have to be some form of arbitration… at the orchestration level," with the web's history as precedent (traffic-based routing controls centralized over time). The open question the panel splits on is *where* it lives — product, plumbing, or the models themselves — with the closing synthesis that "there's not going to be a really great harness in absence of a really great model and vice versa" | `HighlightsPattern → pat-harness-over-model` **[registry]** (the reliability-scoped claim — see Review note 4) | `ReliesOnElement → el-model-routing` **[registry]**, `el-sidekick-architecture`, `el-devin-fusion` |
| `ins-cache-is-the-physics-of-multi-model-systems` | KV-cache economics quietly dictate multi-model architecture: the sidekick pattern exists to avoid re-paying for context; compaction and model switching are priced by the cache miss, not the tokens; cache lifetimes are operational fictions self-hosters can escape; and the move that made agents economically possible at all — Cognition buying direct compute in 2024, "before cache tokens was a thing API providers paid for" — was a cache-economics arbitrage. Any router design that ignores the cache optimizes the wrong bill | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-kv-cache-economics`, `el-sidekick-architecture`, `el-prompt-caching` **[registry]** |

## KnowHow (2 new)

All `SourcedFromArtifact → ia-aie-panel-state-of-model-routing`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-route-models-in-agentic-sessions` | Route sessions, not prompts — and keep a frontier watcher | Don't route on task type: agentic complexity mutates mid-session, so think in subtasks/phases and re-decide as the session evolves. Keep frontier intelligence present even when it isn't working — a watcher that detects the delegate going down a rabbit hole; piggyback those checks on cache refreshes you were paying for anyway ("a free frontier call"). Prefer one persistent sidekick over sub-agent fan-out (running context stays in cache; roles can swap). Send most context to one model only; tune the delegate to report back by reference (file paths, high-level thinking), not by dump. For detection beyond heuristics, use internal-state probes (hallucination/lostness scoring); token-burst heuristics are unproven ("we have to be very empirical to actually know"). Mine production signals — user model upgrades/downgrades, detected mis-routes — as a label stream and iterate the router against real usage. For prompts, prefer heavyweight smart-model repair (ask the model why it chose wrong, patch the prompt, rerun as regression) over gradient-style prompt tuning (GEPA-class) | `ReferencesElement → el-sidekick-architecture`, `el-model-routing` **[registry]**, `el-hallucination-probes`, `el-gepa` **[registry]**, `el-devin-fusion` |
| `how-exploit-cache-economics` | Design against the cache bill, not the token bill | Compact for intelligence, not cost — past ~200K tokens (under 100K if you can) intelligence falls off a cliff regardless of advertised windows; and remember compaction itself takes a cache miss, so schedule it when you were switching models anyway. Treat the ~5-minute cache lifetime as an ops parameter, not physics: self-hosting lets you pin caches as long as business logic wants. If your workload shape is known (e.g. 32K cache / 1K in / 1K out), self-hosting or direct compute beats providers who amortize everyone's shape into one price — the move that made 2024-era agents affordable. Pass cache savings through your own stack the way marketplaces do, and expose switching-tolerance (how much remaining cache you'll sacrifice for a better model) as a tunable | `ReferencesElement → el-kv-cache-economics`, `el-context-compaction` **[registry]**, `el-prompt-caching` **[registry]** |

## Dropped

- **"LM router bench"** — garbled benchmark reference ("this is on LM router bench. There are tons of benchmarks out there") — plausibly RouterBench; kept as ⚠ prose inside `el-jagged-capabilities`; no element node on an unresolvable name.
- **NVIDIA Dynamo** ("wanted to try out Dynamo — a lot of prefix-cache optimizations") — one-line product plug; prose.
- **Advisor tool / sub-agent primitives** (OpenRouter) — named primitives without enough substance to node; folded into `el-openrouter`'s brief.
- **AWS S3-backed cache offload** wish, dynamic cache durations, unified-memory/Rubin cache tricks — futures talk inside `el-kv-cache-economics` prose.
- **"one big model that's most efficient at everything"** — Walden's own steel-man against routing's persistence; kept in Review note 4 (it's an argument, not a claim about the world).
- Scientific-discovery one-shot example, human-memory-vs-context-window exercise, filesystem-as-non-lossy-memory — the last is a restatement of corpus-standard context engineering (batch-6 `el-filesystem-agent-state` territory); no new node, no edge (list-adjacent mention).
- GPT-5.5/5.6 and Fable version elements — carried in `sig-collaboration-moves-into-the-weights` prose; `el-claude-fable`/`el-gpt-56` edges not asserted since the mention is a family-level capability observation (weaker than the abdallah file's embargo usage — reconciler may add both `IdentifiedInArtifact` edges if version-level linkage is preferred).

## Review notes

1. **Zero coinable panelists.** All three substantive panelists are first-name-only in captions; all three are near-certainly identifiable (Walden Yan, Alex Atallah, "Tuhin" unresolved-surname). The file survives their absence — everything is artifact-attributed — but this is the batch's weakest expert coverage; title-card checks would recover two nodes cheaply.
2. **Provider-commoditization guardrail respected.** `sig-openclaw-heartbeat-segmented-the-market` is the one signal whose natural home would have been the retired `pat-provider-blind-ai`; it is held pattern-less per instructions rather than force-fit into `pat-model-not-bottleneck`. Ledger context: the corpus's prior OpenRouter datapoint (Han, batch 3 — per-provider accuracy spread on identical weights) is the *supply-quality* face of the same marketplace; this is the *demand-segmentation* face. Two faces, still no coined thesis — flagging both together for whenever the ledger revisits this space.
3. **`el-model-routing` brief needs widening at seeding.** The batch-8 brief describes exactly the "naive" difficulty-routing this panel calls "extremely fragile" and supersedes with session-level orchestration, watchers, and trained collaboration. Recommend evolving the brief rather than coining a second routing node (I coined none): the batch-8 concept and this panel's controller claim are one node's evolution story.
4. **The harness dialectic lands on both edges here — deliberately.** `sig-collaboration-moves-into-the-weights` adds counter-edge ~№12 to `pat-harness-over-model` (capability absorbed into weights: trained collaborators, Flex Run), while `ins-routing-centralizes-into-a-controller` highlights the *reliability* claim (arbitration layer is structural). Under the batch-15 FINDING-1 rescoping (limit the pattern to reliability engineering), both survive without tension. Walden's closing steel-man — a future single model efficient at everything, yet still needing cache-aware delegation and alignment across models — is quoted in the insight's territory and cuts *for* the controller claim even while cutting *against* multi-model permanence.
5. **Numbers to preserve exactly** (stage-stated, none independently verified): Fusion = Fable-level at ~40% cost reduction; Opus ≈ 3× better at 1/10 total cost vs Haiku on Terminal-Bench; up to 10% accuracy from routing arbitrage; cached tokens ~10× cheaper; ~5-minute default cache lifetime; ~200K/100K context-quality thresholds; auto-router ~2 years old, explosion ~January 2026; OpenClaw heartbeats ~every 10 minutes; top-dollar classification model on OpenRouter = Opus.
6. **Caption garbles normalized** (verify before public-facing use): "Donne" → **Tuhin** (surname unresolved); "psychic agent" → **sidekick** agent (same garble family as batch-10's Monday "psychic/Psyche" → Sidekick — different product); "sidecar" (Alex) → read as his gloss on sidekick-style primitives; "Japa" → **GEPA** (fifth-plus corpus corroboration); "NeMo Triton models" → **Nemotron**; "cash"/"KB cache" → cache / KV cache throughout; "LM router bench" → ⚠ unresolved (RouterBench?); "SCMs to memory" → ⚠ unresolved (SMs-to-memory ratio, best reading); "I Rubin" → Rubin (NVIDIA); "terminal bench" → Terminal-Bench (`el-terminal-bench`); "the Fable models and GPT-5.5 and 5.6 models" — as stated; "headroom is there, RTK is there" → ⚠ unresolved acronyms in the AST/context-representation question (left out of all nodes); "box of root the root cause" → (garbled aside, dropped).
