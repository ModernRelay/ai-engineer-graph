# SPIKE extraction — "Realtime multiplayer, automation, and you!" (Idan Gazit, GitHub) — FOR REVIEW

Source transcript: `transcripts/gazit-github-realtime-multiplayer.txt` (auto-captions — quotes are paraphrases, not verbatim; "Eitan", "Agendic/genetic workflows", "Aces" garbles resolved, see review note 2).
Video: https://youtu.be/iQ5xldZ9StU — AI Engineer World's Fair, published 2026-08-08.
`stagingTimestamp` for the artifact and all dated nodes (signals, knowhows): 2026-08-08 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node. Entities marked **[this batch]** are defined in another file of this four-talk batch.
Shape of the talk: the head of GitHub Next (the Labs team that created Copilot) demos two prototypes under one durable theme — "the greatest value doesn't come from multiplying me into more me; it comes from enabling groups of people to do more" — via two lenses: **automation** (Agentic Workflows: markdown-as-source background agents with deterministic guardrails, public preview) and **collaboration** (Ace: realtime multiplayer development in cloud microVMs, technical preview imminent).

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-gazit-realtime-multiplayer` | Realtime multiplayer, automation, and you! (Idan Gazit, GitHub — AI Engineer World's Fair) | youtube | https://youtu.be/iQ5xldZ9StU |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-idan-gazit`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-idan-gazit` | Idan Gazit (leads GitHub Next, GitHub's Labs team — "the Department of Fool Around and Find Out" — which created Copilot and pioneered spec-based programming and natural-language-to-app; works mostly in the open at githubnext.com) | `AffiliatedWithCompany → co-github` **[registry]** |

⚠ Captions render the name "Eitan" — **Idan Gazit** per the video byline and the GitHub Next public roster. Kept in prose (no nodes): teammates Russ, Kristoff, Maggie, David (first names only, demo participants). "We're a Microsoft company" noted; affiliation edge kept to `co-github` **[registry]** only (distinct node from `co-microsoft` per registry precedent).

## Companies (0 new)

Reused: `co-github` **[registry, b1]**; `co-microsoft` **[registry]** in prose (booth/ownership mention only). Kept in prose: the Home Assistant open-source project (adopter anecdote; same no-node treatment as in the Varda file), Astro (web framework), Cloudflare (deploy-target mention).

## Elements (3 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-github-agentic-workflows` | GitHub Agentic Workflows | product | harness | Background-agent automation where **markdown is the source code**: "if GitHub Actions and Copilot had a baby and it ran on markdown." A workflow is a three-line-ish natural-language playbook ("the message I'd send to a junior developer") plus YAML front matter of deterministic guardrails; Copilot expands it into a full playbook, and the compiled Actions YAML is "a compiled artifact — you never look at it"; to change behavior "just edit the English." Demoed as a super-Dependabot: nightly dependency watcher that read release notes across two major Astro versions, applied the code changes, verified the build, flagged manual steps, and opened one PR with a preview deploy. Ships with a library — issue triager (basis of GitHub's internal spike), repo-assist (a swarm of workflows maintaining a project), CI doctor, N+1-query hunts in GitHub's monolith, daily status homework "for product managers too." In public preview |
| `el-declarative-agent-guardrails` | Declarative agent guardrails (safe outputs) | concept | security | The control model for *unsupervised* agents: guardrails live in deterministic front matter, never in the prompt — "if you're prompting the guardrails at the agent, you're effectively letting the fox loose in the henhouse," because anyone can prompt-inject past instructions. Declares read/write permissions, allowed tools, a network allowlist (default sites + the npm ecosystem + explicitly named docs — "it's not allowed to just go to bitcoin.com"), and **safe outputs**: the complete list of writes the agent may perform (e.g. `create-pull-request`, single — 500 injected PRs is a denial of service) including the explicitly-permitted *do nothing*, "because the last thing I want is the agents denial-of-servicing me" with noise. Secrets stay outside the agent's jail: to use one, the agent asks the warden ("mother, may I talk to that service?"). Four burn-into-your-brain principles: defense in depth; never trust agents with secrets (an agent-visible secret is already compromised); stage and vet all writes; log everything |
| `el-github-ace` | Ace (GitHub Next realtime multiplayer dev) | product | harness | Realtime multiplayer software development in one Slack-shaped surface: every session is a branch of the repo checked out in a **cloud microVM** ("none of this is running on my machine"), holding team chat, a terminal, and a browser preview together. The agent reads the entire team backscroll, so after a human discussion you say "yo, Ace, do it" and it acts on the conversation — including fishing the *final* state out of meandering engineering threads ("let's try it this way… no wait, edge case… back to the first idea") so "I don't need to work for the robots." Plans arrive as markdown docs the *team* edits together, then "we've updated the plan — do it"; plus ambient awareness panes of what teammates' sessions are doing. Chat is where the facts-not-in-code live (political considerations, "that VP will vibe with this direction," the sweetheart Azure deal); everything in code, agents read from code. Technical preview "hopefully later this month" |

Element edges: all three `IdentifiedInArtifact → ia-aie-gazit-realtime-multiplayer`; `el-github-agentic-workflows` `DevelopedByCompany → co-github` **[registry]**, `UsesElement → el-declarative-agent-guardrails`, `UsesElement → el-ci-as-loop-runtime` **[registry, b13]** (compiles to stock Actions — see review note 4), `ExemplifiesPattern → pat-harness-over-model` **[registry]**; `el-declarative-agent-guardrails` `EnablesPattern → pat-verification-gap` **[registry]** (trust placed in deterministic scaffolding, not model obedience); `el-github-ace` `DevelopedByCompany → co-github` **[registry]**, `UsesElement → el-microvm` **[registry, b5]**, `UsesElement → el-doc-as-shared-state` **[this batch — defined in `dailey-ref-velocity-sickness.md`]** (see review note 3).

Reused elements (no new nodes): `el-ci-as-loop-runtime` **[registry, b13]**, `el-microvm` **[registry, b5]**, `el-doc-as-shared-state` **[this batch]**, `el-openclaw` **[seed]** (convergence remark, edged from a signal). Prose only: Copilot (creator credit and workflow-authoring; `el-copilot-coding-agent` **[registry, b6]** is the distinct delegation surface, not what's shown here — no edge), Dependabot (the incumbent being superseded), GitHub Actions (substrate carried by `el-ci-as-loop-runtime`).

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-gazit-realtime-multiplayer`, `SourcedFromSource → source-aie-yt` **[registry]**; all `RelevantCompany → co-github` **[registry]**.

| slug | domain | name / brief | FormsPattern | other edges |
|---|---|---|---|---|
| `sig-background-automation-is-the-ballgame` | harness | GitHub's category bet, stated flatly: "we actually believe this is going to be a **bigger category than interactive AI**, because automations that run in the background while you sleep — that's the ballgame." The framing is industrial: the giant software industry has been "pre-industrial" because its only automations were heuristics ("make sure there's a semicolon at the end of every line"); AI automates work requiring basic judgment — Home Assistant's first workflow walks Python stack traces to close third-party-code issues, "not possible before AI, not possible with heuristics." And scale comes from widening authorship: workflows are plain markdown, so product managers write them too — "we can start to get everybody involved in automation; that's how you actually get industrial scale." Public preview now. **HELD PATTERN-LESS** — dual ledger: the b16 "AI-native software category" candidate (background automation as a category claim) and `pat-ai-native-org` (non-engineers authoring org automation) | `FormsPattern → pat-ai-native-org` (coined 2026-08-16) | `OnElement → el-github-agentic-workflows`, `el-ci-as-loop-runtime` **[registry]** |
| `sig-guardrails-must-be-deterministic` | security | Unsupervised agents get their limits from *config, not prompts*: "it's not enough to prompt the agent 'listen, bro, I don't want you to buy Bitcoin for me ever' — somebody else can prompt-inject it… prompting the guardrails is letting the fox loose in the henhouse." The deterministic front matter declares permissions, tools, a network allowlist, and safe outputs (max one PR — 500 would be a DoS; do-nothing explicitly allowed to stop agents DoSing humans with noise). Secrets never enter the jail — "if an agent can know a secret, treat it as already compromised" — the warden brokers calls; all writes staged and vetted; everything logged; defense in depth, "one layer is never enough" | `FormsPattern → pat-new-cyber-threats` **[registry]**; `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-declarative-agent-guardrails`, `el-github-agentic-workflows` |
| `sig-typing-was-five-percent` | harness | The quantified reframe: a longitudinal study (~100 developers, thousands of hours) found hands-on-keyboard typing is **~5% of the job** — and "for the past few years, AI has helped me to type." The next target is the other 95%: where is the system I want to touch, how does it work today, what do others think about how we should mutate it. The team corollary: "my personal understanding was never sufficient for shipping code inside a team" — us-level understanding can't wait for the end of a process that now runs at agent speed, where "a small misalignment can snowball into a ton of wasted work, and that work costs tokens, and tokens cost real money now" (explicitly building on Litt's understanding-is-the-bottleneck talk — `ia-aie-litt-understanding-bottleneck`, b6) | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-github-ace` |
| `sig-multiplayer-surface-for-realtime-dev` | harness | What replaces the solo-illuminated-by-monitor era: "planning isn't before, and review isn't after — we iterate on the direction together, AI takes a step, we iterate more." Ace's answer: sessions as branches in cloud microVMs, chat + terminal + preview in one place; the agent acts on the whole team backscroll; plans are markdown docs edited *together*, then made real — "to change something about my application, I'm going to edit a document and tell AI: **make the document true**." Slack is dismissed as the wrong substrate ("designed to be better than email for the average office worker — never for making software"), but chat matters as the home of facts-not-in-code. The conviction line: "I don't email Word documents around anymore; I create and collaborate in the same surface. This is coming for code, a trillion percent." **HELD PATTERN-LESS** — `pat-ai-native-org` ledger (uncoined): realtime-multiplayer + ambient awareness as the team's operating surface, from the platform incumbent, same-batch-independent of Ref's decision-doc version | `FormsPattern → pat-ai-native-org` (coined 2026-08-16) | `OnElement → el-github-ace`, `el-doc-as-shared-state` **[this batch]**, `el-microvm` **[registry]** |
| `sig-agents-invoke-humans-now` | harness | The "weird inversion of our relationship with the agents": the better we articulate goals, the less they need us; improving models "spot underspecified behaviors and ask us to clarify," and "whenever they need a pair of hands, they can ask us to be the pair of hands" — interfaces now let agents listen to everything and invoke the human on demand. He names the convergence himself: "we're coming at it from this side and OpenClaw's coming at it from that side, but we're landing in a similar spot" | `FormsPattern → pat-value-of-judgement` **[registry]** | `OnElement → el-github-ace`, `el-openclaw` **[registry]** |

## Insights (2 new)

| slug | name / brief | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-guardrails-outside-the-prompt` | For agents that run while you sleep, safety properties must be properties of the *system*, not requests to the model: any rule expressed as a prompt is one injection away from gone, so the real guardrails are declarative and deterministic — enumerated writes (including "nothing"), allowlisted egress, secrets held by a warden outside the jail, staged writes, total logging. This is what makes unsupervised background automation shippable at platform scale: trust re-architected into scaffolding the model cannot talk its way past. The industrial-revolution corollary: automation only compounds into bought-back time for craft when nobody has to babysit it | `HighlightsPattern → pat-harness-over-model` **[registry]** | `ReliesOnElement → el-declarative-agent-guardrails`, `el-github-agentic-workflows` |
| `ins-collaboration-is-the-95-percent` | AI industrialized the 5% of engineering that was typing; the remaining 95% is shared understanding — and at agent speed, misalignment stops being a soft cost and acquires a token-denominated price. So the next tool generation targets alignment rather than codegen: one surface where the team and its agents share the same backscroll, the same editable plan, and the same preview, with documents as the interface ("make the document true"). The bottleneck moved from generating code to agreeing on what should be true | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-github-ace`, `el-doc-as-shared-state` **[this batch]** |

## KnowHow (1 new)

All: `SourcedFromArtifact → ia-aie-gazit-realtime-multiplayer`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-guardrail-unattended-automations` | Guardrail agent automations you won't supervise | Never encode a guardrail as a prompt — assume prompt injection and make limits deterministic config; practice defense in depth (one layer is never enough); treat any secret an agent can see as already compromised — keep secrets outside the agent's jail and broker their use through a mediating warden; enumerate allowed writes explicitly (safe outputs), cap them (one PR, not 500), and *allow doing nothing* so automations don't DoS humans with noise; allowlist network egress to named destinations; stage and vet all writes before they land; log everything for auditability; author the automation as a short natural-language playbook plus a skill document and let the agent expand it — then iterate by editing the English, treating compiled YAML as an artifact you never touch | `ReferencesElement → el-declarative-agent-guardrails`, `el-github-agentic-workflows`, `el-ci-as-loop-runtime` **[registry]** |

## Dropped

- **The one-minute-early opening bit and Wi-Fi apologies** — logistics; the undemo'd "respond to the color discussion" beat is described in `el-github-ace`'s brief as designed.
- **The personal-website / Astro / Chekhov's-gun demo scaffolding** — the substantive super-Dependabot content is inside `el-github-agentic-workflows`.
- **"My crystal ball barely works into next week" / opportunity-cost-of-token-billionaires musing** — framing for the "we're all labs teams now" line, which is carried in `sig-background-automation-is-the-ballgame`'s ledger note.
- **"Social coding" nostalgia** (the old GitHub tagline) — color around the ambient-awareness feature, folded into `el-github-ace`.
- **The purple/green favorite-color and terminal-command flub** — demo texture.

## Review notes

1. **Prompt-question answered — where multiplayer landed.** Per the extraction brief, both multiplayer signals were **held pattern-less** for the `pat-ai-native-org` ledger rather than forced onto `pat-harness-over-model`. The harness pattern gets its support where the talk actually earns it: deterministic guardrails as the thing that makes unsupervised automation viable (`sig-guardrails-must-be-deterministic`, claim-1 flavor per the b15 FINDING-1 rescoping) — not from the collaboration material.
2. **Garbles.** "Eitan" → **Idan Gazit** (byline); "Agendic"/"genetic workflows" → Agentic Workflows; "Aces a prototype" → "Ace is a prototype" (product is consistently "Ace" thereafter); "bond dev" → `bun dev`; "get at next.com" → githubnext.com; "Vian tooling" → unresolved (some tooling Kristoff works on; no entity impact); "conductor-like product" is genuine speech (the Conductor-style parallel-session product category), not garble. Slug chosen as `el-github-ace` to avoid collision with `el-ace-voice-tutor` (b2 Microsoft).
3. **Same-batch convergence — `el-doc-as-shared-state` reused, not re-coined.** Ace's plan-docs-edited-together and "make the document true" are the same mechanism as Ref's doc-as-state/stateless-agents (defined in `dailey-ref-velocity-sickness.md`, same batch): durable shared doc as the team-and-agent interface, sessions as executors. Reuse follows the b12 `el-pairwise-preference-eval` precedent for same-batch convergence. Two independent vendors — a startup and the incumbent platform — shipping the same shape in the same week is strong texture for any future doc-centric-development candidate; deliberately NOT proposed as a pattern (mechanism, not thesis).
4. **Ledger note — `pat-durable-execution` (uncoined), convention-delivery column.** Agentic Workflows is the *platform owner* shipping background agents as markdown compiled onto stock GitHub Actions — institutionalizing exactly the shape HumanLayer improvised in b13 (`el-ci-as-loop-runtime`, "we don't need a new cluster for this"). Read either as the strongest convention-not-new-infrastructure counter yet (GitHub itself says CI suffices) or as the convention *becoming* the productized durable layer. Recorded for the coin/reject decision; no edge emitted.
5. **Cross-corpus citation.** `sig-typing-was-five-percent` explicitly builds on Litt (b6, `ia-aie-litt-understanding-bottleneck`) — "yesterday Jeffrey Litt talked about understanding being the bottleneck" — extending it from the individual to the team ("us-level understanding"). Prose cross-reference only; no schema edge exists for artifact→artifact citation.
6. **The 5% study is uncited.** "A longitudinal study conducted on like 100 developers over thousands of hours" — no venue, authors or year given; treat the figure as speaker-asserted until sourced. It is load-bearing for `sig-typing-was-five-percent`, so verify before public-facing use.
7. **Signal-bar caveat.** Both products are the speaker's own prototypes (one public preview, one pre-release); adoption evidence is one open-source project (Home Assistant) and GitHub-internal spikes. The category bet ("bigger than interactive AI") is a strategic claim from a platform incumbent — weight it as positioning plus revealed roadmap, not as measurement.
