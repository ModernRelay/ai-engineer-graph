# SPIKE extraction — "First Steps Toward Automated AI Research" (Richard Socher, Recursive) — FOR REVIEW

Source transcript: `transcripts/socher-recursive-automated-research.txt` (auto-captions — quotes are paraphrases, not verbatim; this transcript is unusually clean).
Video: https://youtu.be/pWXUkLP9uWM — AI Engineer, published 2026-07-31.
`stagingTimestamp` for the artifact and all signals: 2026-07-31 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-socher-automated-research` | First Steps Toward Automated AI Research (Richard Socher, Recursive — AI Engineer) | youtube | https://youtu.be/pWXUkLP9uWM |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-richard-socher`.

## Experts (2 new — 1 non-presenting)

| slug | name | edges |
|---|---|---|
| `exp-richard-socher` | Richard Socher (CEO, Recursive; also runs you.com, whose web-search product is rebuilt for LLM agents; long-time NLP researcher — the talk's "we moved from linguists to learned features" arc is his own field history; book on the Eureka machine forthcoming) | `AffiliatedWithCompany → co-recursive-ai` |
| `exp-tim-rocktaschel` | Tim Rocktäschel (co-founder at Recursive; the talk's evolution/open-endedness framing and several slides are credited to him) | `AffiliatedWithCompany → co-recursive-ai` |

⚠ `exp-tim-rocktaschel` **does not get a `ContributedByExpert` edge** — he is credited as a co-founder and slide source, not a presenter (batch-11 `exp-bereket-habtemeskel` precedent is the reverse case: co-author, byline-listed). Drop the node if your bar requires either presenting or a byline.

## Companies (3 new)

| slug | name | type | note |
|---|---|---|---|
| `co-recursive-ai` | Recursive | research | Socher's automated-AI-research venture (recursive.com), co-founded with Tim Rocktäschel. Building the "Eureka machine": an automated scientific-discovery system, with AI research itself as the first target domain. First public airing of the program in this talk |
| `co-you-com` | You.com | developer | Socher's search company, referenced for its agent-facing web search — "agents can read thousands of very long snippets, rather than just 10 blue links with a very short snippet" — as the worked example of rebuilding a human-era layer for AI consumers |
| `co-aix-ventures` | AIX Ventures | investor | Credited as a collaborator on the work ("joint work with a lot of amazing folks at Recursive.com and even some folks at AIX Ventures"). ⚠ Thin — one mention, no described contribution; drop to prose if you want a higher bar |

**[registry]** reused: `co-nvidia` — the CUDA-kernel result is measured against NVIDIA's own benchmark leaderboard, and Recursive worked with NVIDIA to confirm the discovered kernels weren't reward hacks.

## Elements (6 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-eureka-machine` | The Eureka machine | concept | — | Socher's architecture for automating scientific discovery across physics, chemistry, biology, neuroscience, medicine, economics and astrophysics. **Four pillars:** (1) a foundational model of the knowledge humanity already has; (2) all scientific *measurement data* ingested into the machine; (3) **simulations** for what cannot yet be measured — "anything you can simulate, you can verify, and you can then solve with AI"; (4) a physical, industrial-scale automated lab for the experiments that survive the first three. On top of all of it, an **agent swarm** orchestrating the knowledge sources, data, experiments and rewards. A book on the idea is forthcoming |
| `el-recursive-self-improvement` | Recursive self-improvement (strict definition) | concept | training | Socher's line between two things that get conflated. **Auto research** = a system that improves *some other* AI or process (e.g. optimizing a 5-minute NanoChat run) — valuable, an important milestone, but "a weak form". **True RSI** = an AI with a sense of self-awareness of its own shortcomings, **full access over its entire arsenal — pre-training, RL training, harnesses, everything — that then updates that whole system into the next version of itself**. Newly feasible for one reason: AI is code and AI can now code over long horizons, an ability that "has really only happened in the last 6 to 8 months". ⚠ merge-check vs `el-recursive-model-improvement` **[registry]** (batch 5, Cursor) |
| `el-nanochat` | NanoChat | technology | training | Community speedrun target: train a very small chat model in under five minutes and minimize bits-per-byte. Used as auto-research proof point #1 — the community best had been driven to 0.93 |
| `el-nanogpt-speedrun` | NanoGPT speedrun | technology | training | Community wall-clock speedrun benchmark for training NanoGPT, worked on by people (often already with AI assistance) for over a year. Used as auto-research proof point #2 — record improved by more than two seconds, to ~70 seconds |
| `el-agent-native-web-search` | Agent-native web search | concept | context | Rebuilding search for LLM agents instead of people: agents can read *thousands of very long snippets*, where the human product is ten blue links with short snippets — so the retrieval product's shape, not just its API, changes. Generalized in the talk into a program: every layer built for humans (the internet, browsers, search, GPUs) can be rethought and rebuilt as **infrastructure for superintelligence**, and there is a startup in each layer |
| `el-open-ended-evolution` | Open-ended evolution as a design frame | concept | — | The talk's intellectual frame: evolution as an open-ended search process running through biology → technology → science → AI, joined to Popper's account of science as natural selection over theories (propose a hypothesis/explanation, subject it to rigorous empirical testing, let the fittest survive). The engineering read-across is that an automated researcher needs a *variation* step (ideation), a *realization* step (implementation) and a *selection* step (validation) — and, for theory competition, retrieval/web search so the system can find the rivals it must beat |

Element edges: all six `IdentifiedInArtifact → ia-aie-socher-automated-research`; `el-eureka-machine` `DevelopedByCompany → co-recursive-ai`, `UsesElement → el-autoresearch` **[registry]**, `UsesElement → el-open-ended-evolution`; `el-recursive-self-improvement` `UsesElement → el-autoresearch` **[registry]**; `el-agent-native-web-search` `DevelopedByCompany → co-you-com`; `el-autoresearch` **[registry]** `UsesElement → el-nanochat`, `UsesElement → el-nanogpt-speedrun` (the two benchmarks are what the system was run against — flip the direction at review if you read `UsesElement` as dependency rather than consumption); `el-eureka-machine` `EnablesPattern → pat-accelerated-research` **[registry]**; `el-recursive-self-improvement` `EnablesPattern → pat-accelerated-research` **[registry]**; `el-agent-native-web-search` `ExemplifiesPattern → pat-model-not-bottleneck` **[registry]**.

**[registry]** reused (also `IdentifiedInArtifact → ia-aie-socher-automated-research`):
- `el-autoresearch` **[seed]** — the seed element for automated research systems; this talk is its first primary-source, results-bearing artifact in the corpus. Three demonstrated runs (below) are `el-autoresearch` in operation, and Socher's ideation → implementation → validation triple is the cleanest description of the loop the corpus has.

## Signals (6 new)

All: `SpottedInArtifact → ia-aie-socher-automated-research`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | name / brief | domain | FormsPattern | other edges |
|---|---|---|---|---|
| `sig-eureka-machine-program` | First public statement of Recursive's program: build a machine that "will eventually invent pretty much all future inventions for humanity", structured as four pillars (knowledge model, measurement data, simulation, physical automated lab) plus an agent swarm, aimed across physics → economics. Route to it is deliberately indirect — get the machine good at **AI research first**, then point it at other sciences. Framed as the speaker's "version of going to Mars", with a book forthcoming | training | `FormsPattern → pat-accelerated-research` **[registry]** | `OnElement → el-eureka-machine`, `el-autoresearch` **[registry]**; `RelevantCompany → co-recursive-ai` |
| `sig-autoresearch-beats-community-records` | **Three dated proof points, all from an auto-research system run for days, not months.** (1) **NanoChat**: community best bits-per-byte 0.93 → **0.91** after "a little more than a day or two" — and, importantly, not by hyperparameter tuning: the system found genuinely novel mechanisms (hash bi-gram and tri-gram embeddings with lookup tables, mixed into the attention value paths through learned gates). (2) **NanoGPT speedrun**: beat a record that people — often already working *with* AI — had pushed on for over a year, by more than two seconds, to ~**70 seconds**. (3) **CUDA kernels**: after a couple of days it discovered kernels better than the leaderboard best on NVIDIA's own benchmark site "by quite a sizable margin **across all the different categories**", with **no CUDA-kernel experts on the team** — and Recursive worked with NVIDIA to confirm there were no reward hacks. Context given for why kernels matter: large MoE training clusters costing billions still run at ~30% utilization | training | `FormsPattern → pat-accelerated-research` **[registry]** | `OnElement → el-autoresearch` **[registry]**, `el-nanochat`, `el-nanogpt-speedrun`; `RelevantCompany → co-recursive-ai`, `co-nvidia` **[registry]** |
| `sig-rsi-distinguished-from-autoresearch` | Definitional correction aimed at the discourse: "a lot of people think [NanoChat optimization] is already recursive self-improvement" — it isn't. Auto-research improves *another* system; **true RSI** requires self-awareness of its own shortcomings, full access across pre-training, RL training and harnesses, and the ability to update that entire system into its own next version. The enabling condition is dated tightly: AI is code, AI can now code, and the ability to code over **longer and longer time horizons "has really only happened in the last 6 to 8 months"** | training | `FormsPattern → pat-accelerated-research` **[registry]** | `OnElement → el-recursive-self-improvement` |
| `sig-science-limited-by-researcher-headcount` | The economic argument for automating discovery, via Stanisław Lem: exponential growth in science is eventually **halted by the lack of people able to work on it** — the scope of science has widened into so many niche subfields that the number of people focused on any one section has fallen, and "it's very hard to get a million people to work on that particular thing". Chained to the growth case (more science → more technology → more growth → more human flourishing; technology as the only perpetual source of economic growth, citing Andreessen), it makes automated discovery the lever rather than more graduate students | training | `FormsPattern → pat-accelerated-research` **[registry]** | `OnElement → el-eureka-machine` |
| `sig-rebuild-every-layer-for-agents` | Structural claim with a worked example: every technology layer built for humans — the internet, browsers, search, GPUs — is being rethought and rebuilt as **infrastructure for superintelligence**, and "there are a lot of startups possible in rethinking every single one of the layers". The example is You.com's agent-facing web search, where the product shape changes because agents consume thousands of long snippets rather than ten blue links | infra | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-agent-native-web-search`; `RelevantCompany → co-you-com` |
| `sig-ai-engineers-become-managers` | Provocation delivered as a conclusion, extending the old machine-translation joke ("whenever I fire a linguist, my accuracy goes up"): "**it may be true that we should fire all the AI engineers and have them mostly manage an actual AI engineer that is AI and works on AI**" — with the softening that most will be happy to become managers of such an AI rather than doing the nitty-gritty. Backed by the field-history argument: linguists → learned word vectors and end-to-end learning → architecture engineering → learned architectures; "over and over again in AI, when we take out a manual process and replace it with a learned system, improvements will follow" | training | `FormsPattern → pat-value-of-judgement` **[registry]** | `OnElement → el-recursive-self-improvement`, `el-autoresearch` **[registry]** |

## Insights (3 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-remove-the-last-manual-process` | The field's most reliable heuristic, applied to itself: every time a hand-built stage was replaced by a learned one — linguistic features → word vectors, pipeline NLP → end-to-end backpropagation, architecture engineering → learned/unified architectures — results improved. The remaining manual process is **the research loop itself**, so the highest-leverage move is to have the system build itself. "The best way to build it is to have it built itself" | `HighlightsPattern → pat-accelerated-research` **[registry]** | `ReliesOnElement → el-recursive-self-improvement`, `el-autoresearch` **[registry]** |
| `ins-simulation-is-the-verification-frontier` | The load-bearing line of the four-pillar design: "anything you can simulate, you can verify, and you can then solve with AI." That makes the reach of automated science a function of **how much of a field can be brought into simulation** — knowledge and measurement data feed it, the physical lab is the expensive fallback for what escapes it, and everything in between is an optimization problem the moment a simulator exists. It is `pat-verification-gap` read as a research program rather than a complaint | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-eureka-machine`, `el-open-ended-evolution` |
| `ins-autoresearch-proof-points-are-narrow-by-construction` | **Our reading, flagged as interpretation, not the speaker's claim.** All three demonstrated wins share a shape: a dense, cheap, fast-to-evaluate, tamper-checkable scalar (bits per byte on a five-minute run; wall-clock seconds; kernel throughput on a vendor leaderboard) — precisely the RLVR-easy regime that this batch's other four talks say covers almost none of real work. What the results *do* establish is significant and shouldn't be discounted: the system found **novel mechanisms**, not tuned hyperparameters, and beat expert human effort in domains where the team had no experts. What separates them from the Eureka machine is exactly the verifier for everything else — which is why pillar 3 is simulation | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-nanochat`, `el-nanogpt-speedrun`, `el-autoresearch` **[registry]** |

## KnowHow (1 new)

`SourcedFromArtifact → ia-aie-socher-automated-research`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-run-auto-research-on-a-benchmark` | Pick targets where the metric is dense, cheap and hard to fake | Choose a target whose evaluation is fast and scalar (a five-minute training run's bits-per-byte, wall-clock seconds, kernel throughput) so the loop can run thousands of times; structure the system as the three-step scientific loop — **ideation → implementation → validation** — which holds across essentially every scientific field; give it retrieval/web search so it can find the competing ideas it has to beat; budget days rather than hours (all three published wins landed in one to a few days); verify wins **with the metric's owner** (Recursive worked with NVIDIA on the kernel results) specifically to rule out reward hacking; and judge success by whether the system produced *novel mechanisms* rather than tuned hyperparameters — hash bi-gram/tri-gram embedding tables mixed into attention value paths through learned gates is the example of what a real find looks like | `ReferencesElement → el-autoresearch` **[registry]**, `el-nanochat`, `el-nanogpt-speedrun`, `el-recursive-self-improvement` |

## Dropped

- **Marc Andreessen / the techno-optimist manifesto** — cited approvingly for "technology is the only perpetual source of economic growth" and "no material problem that cannot be solved with more technology". Framing; prose inside `sig-science-limited-by-researcher-headcount`. No Expert node (quoted, not contributing).
- **Karl Popper and Stanisław Lem** — the philosophical scaffolding (theory selection as natural selection; science halted by headcount). Popper is folded into `el-open-ended-evolution`; Lem carries `sig-science-limited-by-researcher-headcount`. No nodes, per the corpus's cited-figure convention (`exp-karpathy`/`exp-terence-tao` are seed exceptions).
- **NanoChat / NanoGPT authorship** — both are Karpathy artifacts in public record, but the talk never says so; the elements carry **no `DevelopedByCompany` edge** and no link to `exp-karpathy` **[registry]** (the schema has no Element→Expert edge anyway). Note at seeding if you want provenance in the briefs.
- **The historical arc** — 3.5-billion-year evolution, world-product exponentials, hunting/farming/Enlightenment/Industrial Revolution, population growth, Wright brothers 1903 → Apollo 1969 within one lifetime, "too late to explore Earth, too early for the stars", green revolution / lighting / heating / air conditioning. Rhetorical build; no nodes.
- **"Spaces of intelligence"** — the closing argument that intelligence is a volumetric object of ~10 multi-dimensional spaces and we are "astronomically far" from its upper bounds, so the exponential has room. ⚠ Named but never enumerated ("we won't have time to go into all of these"); too thin to coin, and I deliberately did **not** make an element out of a taxonomy the talk withholds. Prose only. Coin `el-intelligence-spaces` at review if the book supplies the list.
- **MoE utilization (~30% on billion-dollar clusters)** — a striking number but delivered as motivation for the kernel work; kept inside `sig-autoresearch-beats-community-records`.

## Review notes

1. **Patterns: zero coined.** Four `FormsPattern → pat-accelerated-research` (this is the densest single-talk use of that seed pattern in the corpus — it has been thin since seeding, and this file roughly doubles its evidence), one `→ pat-model-not-bottleneck`, one `→ pat-value-of-judgement`, two insights on `pat-verification-gap`.
2. **`pat-accelerated-research` [seed] is the right home, and it now has hard numbers.** Prior corpus evidence was mostly assertion (AlphaEvolve, Erdős tools, the Weco/AIDE line, Prime Intellect's "age of research"). This file adds three dated, quantified, adversarially-checked results — including a novel-mechanism discovery and a leaderboard sweep in a domain the team had no experts in. Recommend widening the seed pattern's brief at review with the auto-research-vs-RSI distinction, which is currently absent from it.
3. **Merge-check: `el-recursive-self-improvement` vs `el-recursive-model-improvement` [registry]** (batch 5, Robinson/Cursor — models improving the tools that build the next models). Socher's node is a *definition and a bar*, Robinson's is an *observed practice*, and Socher explicitly classifies practices like Robinson's as the weak form. My recommendation is to keep both and add `el-recursive-self-improvement` `UsesElement → el-recursive-model-improvement` at seeding — but a single node with both readings in the brief is defensible.
4. **Adjacent registry elements I checked and did *not* edge:** `el-aide` / `el-aiden` (Weco, batch 3 — the closest sibling in the corpus: an auto-research agent optimizing training code against a metric; Socher never mentions Weco, so no edge, but the reconciler should read the two together — `el-autoresearch` is their common parent), `el-alphaevolve` **[seed]** (same family, unmentioned), `el-code-selfplay` and `el-process-supervision` (batch 3, Han/Unsloth — unmentioned).
5. **Uncoined candidate touched:** batch-3's "built-for-humans infrastructure breaks under agent actors" (madabhushi) gets a second, much broader statement here — Socher generalizes it from "our infrastructure breaks" to "**every human-era layer is a startup opportunity when rebuilt for AI consumers**". Homed on `pat-model-not-bottleneck` for now; if that candidate is ever coined, `sig-rebuild-every-layer-for-agents` is a strong rehoming target and `el-agent-native-web-search` its cleanest mechanism.
6. **`ins-autoresearch-proof-points-are-narrow-by-construction` is analytic, not stated** — flagged in the insight text itself. It exists because this file lands in a batch whose other four talks are entirely about the non-verifiable regime, and the contrast is the most useful thing the corpus can say about these results. Drop it if you want insights restricted to speaker claims; the two other insights stand alone.
7. **`co-aix-ventures` is the weakest node in the file** (one mention, no described contribution). Drop-option: delete the company and keep the credit as prose in the artifact description.
8. **Caption garbles normalized** (this transcript is unusually clean — few needed): "Recursive.com" ✓; "Tim Rocktäschel" ✓ (rendered correctly, unusually); "Nanochat"/"nano GPT speed run" ✓; "hash bi-grams and tri-gram embeddings … mixing that into various value paths of the intention through variety of learned gates" → **attention**, not "intention"; "Stanisław Lem" ✓; "an RSI that builds itself" ✓. Names deliberately left unverified: "AIX Ventures" (⚠ spelling from captions).
