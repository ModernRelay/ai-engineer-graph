# SPIKE extraction — "SimulationMaxxing: How we ship agents 20× faster" (Aman Gupta, Nubank + Shreya Rajpal, Snowglobe) — FOR REVIEW

Source transcript: `transcripts/gupta-rajpal-nubank-simulationmaxxing.txt` (auto-captions — quotes are paraphrases, not verbatim). Two speakers alternating; the `>>` markers in the captions are speaker changes.
Video: https://youtu.be/KMR_RBoCa4M — AI Engineer World's Fair, published 2026-07-31.
`stagingTimestamp` for the artifact and all signals: 2026-07-31 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.

`co-nubank` is defined in **`palma-nubank-vetting-skills.md`** (same batch, same publish date) and reused here — no second node.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-gupta-rajpal-simulationmaxxing` | SimulationMaxxing: How we ship agents 20× faster (Aman Gupta, Nubank + Shreya Rajpal, Snowglobe — AI Engineer World's Fair) | youtube | https://youtu.be/KMR_RBoCa4M |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-aman-gupta`; `ContributedByExpert → exp-shreya-rajpal`.

## Experts (2 new)

| slug | name | edges |
|---|---|---|
| `exp-aman-gupta` | Aman Gupta (principal machine learning engineer at Nubank; owns the production customer-support agents and the KDD paper behind this talk) | `AffiliatedWithCompany → co-nubank` **[this batch]** |
| `exp-shreya-rajpal` | Shreya Rajpal (CEO of Snowglobe, the agent-simulation platform used in this work) | `AffiliatedWithCompany → co-snowglobe` |

⚠ The transcript states **only** that Rajpal is CEO of Snowglobe. Her Guardrails AI association is public record but is never mentioned on stage — no Guardrails company node, no edge (Review note 1). ⚠ `exp-aman-gupta` is the **fourth** distinct Gupta in the corpus (`exp-sachin-gupta` b3/b5, `exp-nishant-gupta` b8, `exp-soumya-gupta` b12) — unrelated; do not merge.

## Companies (1 new)

| slug | name | type | note |
|---|---|---|---|
| `co-snowglobe` | Snowglobe | developer | Agent-simulation platform for evals — SDK + simulation runner + judges, sold as eval-data infrastructure. Transcript supports: Rajpal is CEO, the product does what `el-snowglobe` describes, and Nubank is a production user. ⚠ captioned "Snow Globe"; ⚠ its relationship to Guardrails AI (public record) is not stated on stage — see Review note 1 |

Reused **[registry / this batch]**: `co-nubank` — here as an AI *builder*: leading digital bank in Latin America, 135M customers across Brazil, Mexico and Colombia with a US launch imminent, quarterly revenue past $5B in Q1 2026, five customer-support agents in production splitting work with human support ("AI handles a lot of routines end to end, humans focus on the hardest and long-tail cases").

## Elements (4 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-snowglobe` | Snowglobe | product | harness | Simulation platform that manufactures agent eval data on demand. Inputs: point the SDK at an existing agent with **no code changes**, and declare which tools must be mocked; then steer the run with personas, use cases and the data points you want covered. Output: thousands of multi-turn conversations against the real agent with tools mocked and grounding data held consistent, judges run per turn over that dataset, and the labelled result pipes straight into the existing evaluation pipeline. Positioning: the infrastructure layer that turns evaluation data from something you wait for into something you request |
| `el-grounded-synthetic-persona` | Grounded synthetic persona | concept | harness | The unit of a simulation: a fully synthetic user given identity and situation (e.g. "Maria Souza, 34, designer, first-time credit-card customer wanting to order a card"), a communication style (tone, voice — the transcript's example persona produces curt one-line messages, and the resulting conversation matches), **and generated grounding data** — a fake address, a fake credit card, account data — that stays consistent across every mocked tool call in the trajectory, so the agent's identity lookups and verifications return coherent results and the run is actually evaluable. ⚠ merge-check vs `el-simulated-user` (b12), which is the same role without the grounding-consistency requirement |
| `el-sim-to-real-gap` | Sim-to-real gap (closing it) | concept | harness | The calibration discipline that makes simulated eval data bankable, stated as a precondition rather than an afterthought: compare eval metrics computed on simulated data against the same metrics on real production data and check the correlation, and add human review — domain experts labelling whether simulated conversations are usable data — across both mature agents and greenfield ones. Nubank's reported result: correlation "pretty high", 80% of domain-expert labels confirming sim data is usable. Without this step, simulation only relocates the trust problem |
| `el-simulation-first-release-loop` | Simulation-first release loop | concept | harness | The self-improvement loop the talk builds: ship → observe → build robust evals (LLM-as-judge metrics aligned to human labels, refined with automated prompt optimization) → run simulations → pipe simulated *and* real data through the same evals → use the resulting signal to optimize the **agent harness** → ship again. The economic move is that simulation replaces the first five or six A/B tests of a quarter: teams iterate against sim output and only launch the variant they are already happy with, so a release cycle of days-to-weeks (offline evals on hand-curated data = days; production A/B = "forever", because feedback-form signal is sparse and noisy) collapses to hours. ⚠ merge-check vs `el-closed-loop-auto-tuning` (b12) and `el-eval-driven-development` (b8) |

Element edges: all four `IdentifiedInArtifact → ia-aie-gupta-rajpal-simulationmaxxing`. `el-snowglobe` `DevelopedByCompany → co-snowglobe`; `el-snowglobe` `UsesElement → el-grounded-synthetic-persona`, `UsesElement → el-simulated-user` **[registry]**, `UsesElement → el-judge-as-classifier` **[registry]**. `el-simulation-first-release-loop` `UsesElement → el-snowglobe`, `UsesElement → el-sim-to-real-gap`, `UsesElement → el-gepa` **[registry]**. `el-sim-to-real-gap` `UsesElement → el-judge-human-calibration` **[registry]**. `el-snowglobe` and `el-simulation-first-release-loop` `EnablesPattern → pat-verification-gap` **[registry]**; `el-simulation-first-release-loop` `EnablesPattern → pat-harness-over-model` **[registry]**.

### Elements reused **[registry]** (no new nodes)

| slug | why it appears |
|---|---|
| `el-simulated-user` **[registry, batch 12]** | exactly the mechanism — an LLM standing in for the human counterpart because a real user cannot be in a repeatable experiment; this talk is its largest production deployment in the corpus |
| `el-trace-to-simulation` **[registry, batch 12]** | the corpus's other route to the same goal, and the one this talk argues past: Snorkel derives simulations *from* production traces, Nubank generates them *instead of* waiting for traces. Same "traces can't give you apples-to-apples comparisons" critique, opposite starting point |
| `el-judge-as-classifier` **[registry, batch 3]** / `el-judge-human-calibration` **[registry, batch 12]** | the metrics half of the talk: "LLM-as-a-judge-style classifiers aligned with human judgment", iteratively improved — described as the *solved* part, in contrast to data |
| `el-gepa` **[registry, batch 11]** | the automated prompt-optimization algorithm in the self-improvement loop, captioned "Japa" — the **third independent corroboration** of that garble resolution (b11 coin, b13 confirm, here) |
| `el-replayable-learning-environment` **[registry, batch 8]** | the single-log-inferred cousin of this pipeline (personas + mocked tools + inferred evaluators from one production log); Nubank's version is persona-driven and generated at volume rather than inferred from one observation |

## Signals (5 new)

All: domain `harness`, `SpottedInArtifact → ia-aie-gupta-rajpal-simulationmaxxing`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | name / brief | FormsPattern | other edges |
|---|---|---|---|
| `sig-nubank-sim-20x-agent-shipping` | Headline production claim with numbers attached: generating eval data in simulation instead of waiting on production data lets Nubank ship agents **20× faster** — the loop drops from weeks to under a day, sometimes hours. Breakdown given: changing the agent harness (prompt, tools, anything around the LLM) still takes a few hours; running offline evals goes from days to fast because a simulated user replaces hand-curated data; and confidence that a version will do well in production goes from an A/B test that "takes forever" (sparse, noisy feedback-form signal) to a few hours. Production evidence: average TNPS across five production customer-support agents rose over a few quarters from "not so great" to approaching human quality, with the presenter noting the chart is stale and several now exceed it; one agent's TNPS **doubled**; self-service rate improved across the board and by 4% in one case; simulation caught a regression and a self-service-rate drop before either reached production. A KDD paper on the work is to be presented in Korea in August | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-snowglobe`, `el-simulation-first-release-loop`; `RelevantCompany → co-nubank` **[this batch]**, `co-snowglobe` |
| `sig-eval-data-is-the-bottleneck` | Diagnosis behind the whole talk: evals are metrics plus data, and **metrics now have a playbook** (LLM-as-judge classifiers aligned to human judgment, improved with auto-optimization and auto prompt tuning) while **data remains unsolved and is the bottleneck**. Why agents made it worse: structured ML rows and single-turn QA were tractable, but one agent data point is a *trajectory* with many internal tool calls whose state must stay consistent across the whole run — expensive to generate and to annotate, far worse to scale to a sample set. The two default sources both fail: manual authoring means hand-planning trajectories and state updates; production traces are almost free but you are testing on live users every time and cannot run many parallel experiments the way an ML workflow requires | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-trace-to-simulation` **[registry]**, `el-judge-as-classifier` **[registry]** |
| `sig-sim-data-matches-production-signal` | The measurement that licenses the rest: comparing evals computed on simulated data against evals on real production data gives "pretty high" correlation, and **80% of domain-expert human labels confirmed that simulations produce usable data** — for greenfield agents being brought up from scratch as well as for mature agents being improved. Framed as the necessary trust step, not a nice-to-have: the gains are only real once the sim-to-real gap is closed and measured offline, online and by human review | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-sim-to-real-gap`, `el-grounded-synthetic-persona`, `el-simulated-user` **[registry]** |
| `sig-sim-enables-open-model-bakeoff` | The harness is held fixed and the model is treated as the swappable part: with simulation, Nubank throws the recent wave of open-source models at its existing agent harness and eval setup to see which versions work, "saving multiple weeks of effort and probably underselling it", with A/B tests running live at the time of the talk to find out whether open models match frontier ones. Simulation is what makes model selection an experiment you run rather than a bet you place | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-snowglobe`, `el-simulation-first-release-loop`; `RelevantCompany → co-nubank` **[this batch]** |
| `sig-enterprise-rsi-is-data-and-metrics` | Deflationary answer to the conference's self-improving-agents theme, from a team that ships them: in an enterprise setting, recursive self-improvement "really does come down to two things — data and metrics". With metrics aligned well enough to catch the signals you care about and a reliable way to generate data those metrics can score, assembling a continuously self-improving loop is easy; without either, RSI is untestable. Their instantiation: ship → observe → robust evals (auto prompt optimization) → simulate → pipe sim + real through the evals → optimize the agent harness → ship, which enabled "many, many A/B tests per agent" across many agents | `FormsPattern → pat-verification-gap` **[registry]**, `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-simulation-first-release-loop`, `el-gepa` **[registry]** |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-eval-data-manufactured-not-harvested` | Production traces look free — you get them anyway, you never pay to author them — but they are paid for in two currencies teams don't price: every test runs on live users, and experiments serialize because production has one timeline. Simulation converts eval data from a *harvested* scarce resource into a *manufactured* on-demand one, and that conversion, not any model improvement, is what unlocks high-throughput experimentation. The corollary that makes it work for agents specifically: the expensive part of an agent data point is trajectory-wide state consistency, which a simulator can guarantee and a hand-author cannot | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-snowglobe`, `ReliesOnElement → el-grounded-synthetic-persona` |
| `ins-sim-gains-require-calibration` | Simulation does not remove the trust problem, it relocates it: the moment eval data is synthetic, the question becomes whether the sim predicts production. The speakers make closing that gap an explicit precondition of the 20× claim — correlate sim-derived metrics against production metrics, add human expert review of sim conversations, and only then gate launches on sim results. A simulation programme without a calibration programme is a faster way to be confidently wrong | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-sim-to-real-gap`, `ReliesOnElement → el-judge-human-calibration` **[registry]** |

## KnowHow (2 new)

All `SourcedFromArtifact → ia-aie-gupta-rajpal-simulationmaxxing`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-run-agent-simulations` | Manufacture agent eval data with simulations | Wrap the agent you already have — point the simulation SDK at it, no code changes — and decide which tools have to be mocked for the run to be meaningful. Drive the simulation with three inputs: personas, use cases, and the data points you want covered. For each persona generate the grounding data the agent will actually look up (identity, address, payment instrument, account state) and hold it consistent across every mocked tool call in the trajectory, or the run is not evaluable. Give personas tone and voice as well as intent, so conversation style varies the way real traffic does. Run judges per turn over the resulting conversations, and pipe the labelled dataset straight into the existing eval pipeline rather than building a parallel one | `ReferencesElement → el-snowglobe`, `el-grounded-synthetic-persona`, `el-simulated-user` **[registry]** |
| `how-close-the-sim-to-real-gap` | Earn the right to trust simulated evals | Before acting on simulated results, measure the sim-to-real gap: compute your eval metrics on simulated data and on real production data and check the correlation; have domain experts review sampled simulated conversations and label whether the data is usable; run the check for greenfield agents too, not only mature ones where you have production data to compare against. Once calibrated, change the release gate rather than adding a step: iterate against simulation and launch only the variant you are already happy with in sim, so the quarter's ten A/B tests become one. Keep metrics honest in parallel — align judge classifiers to human judgment and keep improving them with automated prompt optimization | `ReferencesElement → el-sim-to-real-gap`, `el-judge-human-calibration` **[registry]**, `el-gepa` **[registry]** |

## Dropped

- Nubank's corporate stats (135M customers, $5B quarterly revenue, NPS) — folded into `co-nubank`'s reuse note, no signal; they are scene-setting for why customer support is the agent use case.
- TNPS / SSR as metrics — kept as prose in the signals; not coined as elements (they are the company's existing CSAT and self-service measures, not AI-native constructs).
- The ML-data-work history aside ("circa 2018, ML work is 85% data work") — rhetorical framing.
- The KDD paper — cited by QR code only; recorded inside `sig-nubank-sim-20x-agent-shipping`, not modelled as a separate artifact (no link available).
- "Maria Souza" example persona — illustration, folded into `el-grounded-synthetic-persona`.

## Review notes

1. **Shreya Rajpal / Guardrails AI — flagged, not modelled.** The transcript's only affiliation claim is "I am the CEO of Snow Globe". Her Guardrails AI founding is public record (and Snowglobe is publicly a Guardrails AI product), but nothing on stage supports it, so this file coins `co-snowglobe` alone and adds no Guardrails node or edge. If reconciliation prefers the public-record view, the fix is one company node plus a `DevelopedByCompany` re-point for `el-snowglobe` — do it deliberately, not by inference from this file. Name garbles: captions render her "Shrea", "Shria" and "Fria"; the byline spelling is used.
2. **Name garbles (Nubank side):** "Ammon", "Amen", "Amen was showing" → Aman Gupta; "new"/"New Bank" → Nubank throughout; "Japa" → GEPA (`el-gepa` **[registry]**); "gamecher" → game changer; "circuit this timeline" → short-circuit; "TNPS"/"TPS" → TNPS (transactional NPS); "SSR" → self-service rate; "KD paper" → KDD paper.
3. **Numbers are speaker-reported and partly unquantified.** The TNPS chart was shown without absolute values and described as stale; "20× faster" is the speakers' headline framing, not an audited measurement; the 80% expert-label figure and the 4% SSR improvement are single-sentence claims. The regression and self-service catches are anecdotes without identifiers. Treat as vendor+customer testimony delivered jointly — the two presenters are supplier and customer, which is worth noting when weighing the evidence.
4. **Direct tension with `el-simulation-awareness` / Andon Labs (b11) — flagged for review, no edge.** Batch 11 recorded Petersson's claim that behavioural evals are "kind of doomed" because models detect that they are being simulated (with Anthropic system-card corroboration), and that the remedy is `el-real-to-sim-forking` from live deployments. This talk asserts the opposite in practice: sim/real correlation is high and 80% of expert labels say sim data is usable. The claims are not strictly contradictory — Andon measures *agent behaviour under evaluation* in open-ended agentic settings, Nubank measures *eval-metric agreement* on bounded customer-support conversations — but a reviewer weighing `pat-benchmark-trust-crisis` should see them side by side. No `ContradictsPattern` edge is proposed, since `pat-verification-gap` accommodates both.
5. **`pat-adaptive-harness` candidate evidence (noted, not coined).** `sig-enterprise-rsi-is-data-and-metrics` and `el-simulation-first-release-loop` describe optimizer-driven harness improvement from eval signal, which is the same shape as RELAI (b8), DSPy Flex (b11), AutoAgent (b9) and Uber's reflect/synthesize optimizer (b12). It is a *weaker* data point than those: the loop is human-gated at ship time and the optimization named is prompt-level, not harness-rewriting. Log it, don't over-count it.
6. **`pat-environments-economy` — deliberately not linked.** The simulation infrastructure here is bought as a product and used internally; nothing is sold, traded or standardized as an environment asset, so this is a vendor adjacency rather than evidence. The Snowglobe-as-supplier framing is closer to `pat-model-not-bottleneck` (periphery productization) than to the environments-economy claim; no edge added either way.
7. **Merge-checks worth resolving at seeding:** `el-grounded-synthetic-persona` vs `el-simulated-user` (b12) — same role, this one adds the trajectory-wide grounding-consistency requirement; `el-simulation-first-release-loop` vs `el-closed-loop-auto-tuning` (b12) and `el-eval-driven-development` (b8); `el-sim-to-real-gap` vs `el-judge-human-calibration` (b12), which is the judge-level special case of the same calibration idea. Also check `el-snowglobe` against `el-trace-to-simulation` (b12) at the product/concept boundary — Snorkel's is a technique, this is a platform.
8. **`sig-sim-enables-open-model-bakeoff` and `pat-sovereign-ai`.** The open-source-model bake-off is motivated as cost/quality experimentation, not independence from frontier labs, so no `pat-sovereign-ai` edge — unlike `sig-harness-for-local-models` (b7, Bhargava), which argued the same mechanism explicitly as a sovereignty play. Adjacency noted for whoever reads the two together.
