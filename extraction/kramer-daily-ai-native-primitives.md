# SPIKE extraction — "The New Primitives: Building AI Native Software" (Kwindla Kramer, Daily) — FOR REVIEW

Source transcript: `transcripts/kramer-daily-ai-native-primitives.txt` (auto-captions — quotes are paraphrases, not verbatim; the speaker's own name is the worst garble in the file, see review note 2).
Video: https://youtu.be/LZuWZRze3MU — AI Engineer World's Fair (voice-track opening keynote), published 2026-08-07.
`stagingTimestamp` for the artifact and all dated nodes (signals, knowhows): 2026-08-07 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.
Shape of the talk: the Pipecat creator opens the voice track with an 80-year history of computing primitives (Bush's memex → languages → interactivity → databases → PC/VisiCalc → web → mobile → cloud) to place the present: we are building *agents* and "agents plus" (Nadella's org-level harnesses), and the mature category — **AI-native software**, with inference at the core of every interaction — is still unbuilt, standing to agents as today's internet stands to 1995's web pages. Closes with Gradient Bang, an LLM-core multiplayer game built as a canvas for that category.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-kramer-ai-native-primitives` | The New Primitives: Building AI Native Software (Kwindla Kramer, Daily — AI Engineer World's Fair) | youtube | https://youtu.be/LZuWZRze3MU |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-kwindla-kramer`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-kwindla-kramer` | Kwindla Hultman Kramer (Daily — developer infrastructure for real-time audio/video/AI; leads the team behind Pipecat; co-founded a startup with John Underkoffler in 2006 to commercialize the Minority Report interface — Oblong Industries per public record — and calls the multimodal, multi-device, ubiquitously connected computer "still what I'm working on 15 years later") | `AffiliatedWithCompany → co-daily` **[registry]** |

⚠ Registry check performed as instructed: Kramer was **prose-only** in the corpus until now — cited (not coined) in `agrawal-wirjo-aws-voice-interrupts.md` (b2) as the Daily co-founder behind the Pipecat latency data. This node retroactively gives that citation a referent. Kept as prose here (no nodes): **Satya Nadella** (played clip — see review note 4), **John Underkoffler** (MIT Media Lab; Minority Report / Iron Man UIs), Vannevar Bush, Ivan Sutherland.

## Companies (1 new)

| slug | name | type | note |
|---|---|---|---|
| `co-tavus` | Tavus | developer | AI video/avatar company (⚠ captioned "Tavis"); appears for one load-bearing dated fact — weeks before this talk it released a reimagined Knowledge Navigator video "entirely built on real and available technology," a 4-minute single take. ⚠ Thin — single-mention company, drop-option documented in review note 6 |

Reused: `co-daily` **[registry, b2]**, `co-microsoft` **[registry, b2]**, `co-aws` **[registry, b2]**, `co-nvidia` **[registry, b2]**, `co-anthropic` **[registry, seed]**, `co-github` **[registry, b1]**, `co-apple` **[registry, b13]** (Knowledge Navigator, 1987).

## Elements (4 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-agents-plus` | Agents plus | concept | harness | Kramer's name for the current frontier just past bare agents, anchored on the played Nadella clip: **multi-model harnesses** ("you want the harness to define the models, the data and the tools, so you have a loop across those three") with tool access, progressive disclosure of tools for token efficiency, and rich context feeding; software copilots embedded in every piece of software; **organization-level harnesses**. The through-line from last year's loops/tool-calls/context-engineering to this year's emerging ideas |
| `el-ai-native-software` | AI-native software | concept | harness | The proposed post-agents category: software built from the ground up with **LLM inference at the core of every interaction at every moment** — not a model bolted onto conventional software. Positioned by analogy: AI-native software is to today's agents what the modern internet (web apps, native mobile) is to the hand-written web pages of 1995 — and, per the talk's method, it will be *discovered by building*: "we now actually have the ability to build the memex, and Jarvis from Iron Man, and Knowledge Navigator, for real, completely working — and by building those things we'll figure out what we want to build next" |
| `el-knowledge-navigator` | Knowledge Navigator (1987 concept video) | concept | harness | Apple's 1987 concept film — foldable tablet, touchscreen, a conversational voice assistant with strong personality, global + personal information access, real-time vision and video generation, seamless calls, **delegation of complex tasks for autonomous execution**, and what "we might call today continual learning." Kramer's yardstick artifact: it "divides our timeline neatly in half," the web was "a conscious attempt to make that video real," and Tavus's weeks-old remake — one 4-minute take on real, available technology — is the talk's evidence that the target is now buildable |
| `el-gradient-bang` | Gradient Bang | product | harness | Massively-multiplayer game Kramer is building with friends "as a canvas to really think about what AI native software can be": LLMs at the core of every interaction, **hundreds of inference calls** in play, real-time agent orchestration — "we couldn't have built anything like this even a year ago." Demonstrated patterns, per its own narration: asynchronous non-blocking context compression; long-running sub-agents that share context; progressive skills loading; dynamic user-interface generation; conversational voice ("Eagle is on five trade loops, Hawk and Raptor are on five exploration loops each — your fleet is busy") |

Element edges: all four `IdentifiedInArtifact → ia-aie-kramer-ai-native-primitives`; `el-agents-plus` `UsesElement → el-harness-engineering` **[registry, b2]**, `UsesElement → el-progressive-disclosure` **[registry, b2]**; `el-ai-native-software` `UsesElement → el-agents-plus` (the stage it grows out of); `el-knowledge-navigator` `DevelopedByCompany → co-apple` **[registry]**, `EnablesElement → el-ai-native-software` (the design target that defines the category); `el-gradient-bang` `UsesElement → el-runtime-llm-gameplay` **[registry, b6]**, `UsesElement → el-context-compaction` **[registry, b6]**, `UsesElement → el-agent-skills` **[registry, b1]**, `UsesElement → el-generative-ui` **[registry, b2]**, `UsesElement → el-pipecat` **[registry, b2]** (conversational voice; Daily's own stack — inferred from authorship, flagged in review note 5).

Reused elements (no new nodes): `el-pipecat`, `el-harness-engineering`, `el-progressive-disclosure`, `el-context-compaction`, `el-runtime-llm-gameplay`, `el-agent-skills`, `el-generative-ui` **[registry]**.

## Signals (4 new)

All: `SpottedInArtifact → ia-aie-kramer-ai-native-primitives`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | other edges |
|---|---|---|---|---|
| `sig-nadella-org-level-harnesses` | harness | Microsoft's CEO, in a clip Kramer plays as the state of the art (No Priors / Latent Space crossover, ~2 weeks prior): "you kind of want the **harness to define the models, the data and the tools**, so that you have a loop across those three" — and every Microsoft product, "whether it's GitHub Copilot or the security copilot… or the discovery for science," is being built as a **multi-model harness** with tool access, progressive disclosure of tools for token efficiency, and rich context. Kramer's gloss: the frontier is "agents plus" — copilots embedded in every piece of software and organization-level harnesses | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-agents-plus`, `el-progressive-disclosure` **[registry]**, `el-harness-engineering` **[registry]**; `RelevantCompany → co-microsoft`, `co-github` **[registry]** |
| `sig-pipecat-neutral-voice-layer` | harness | The voice-agent orchestration layer is consolidating on vendor-neutral open source: Pipecat is stated as "the **most widely used framework for building voice agents today**," open source, used by AWS, Nvidia and Anthropic "and thousands of startups and scaleups and enterprises" — frontier labs and hyperscalers building their voice products on a neutral third-party harness rather than in-house pipelines | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-pipecat` **[registry]**; `RelevantCompany → co-daily`, `co-aws`, `co-nvidia`, `co-anthropic` **[all registry]** |
| `sig-knowledge-navigator-now-buildable` | harness | The 40-year vision claims are now build claims: "we actually have the ability to build the memex, and Jarvis from Iron Man, and Knowledge Navigator from that 1987 video **for real, completely working**" — evidenced by Tavus's weeks-old Knowledge Navigator remake, a 4-minute single take "entirely built on real and available technology" that Kramer says feels "both familiar and brand new." Paired with the category framing: agents today are the web pages of 1995 — still important, but the transitional artifact of an era whose mature software category (AI-native software) hasn't been invented yet | — **HELD PATTERN-LESS** (candidate evidence — see review note 1) | `OnElement → el-ai-native-software`, `el-knowledge-navigator`; `RelevantCompany → co-tavus`, `co-apple` **[registry]** |
| `sig-gradient-bang-llm-at-every-interaction` | harness | An existence proof for the category, built by the voice-infrastructure vendor as research: a multiplayer game with **LLMs as the core of every interaction at every moment — hundreds of inference calls** — showcasing asynchronous non-blocking context compression, long-running context-sharing sub-agents, progressive skills loading, dynamic UI generation and conversational voice, with the explicit timing claim "we couldn't have built anything like this even a year ago" | — **HELD PATTERN-LESS** (candidate evidence — see review note 1) | `OnElement → el-gradient-bang`, `el-runtime-llm-gameplay` **[registry]**, `el-context-compaction` **[registry]**; `RelevantCompany → co-daily` **[registry]** |

## Insights (2 new)

| slug | name / brief | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-agents-are-the-web-pages-of-1995` | The historically-informed read on the present: every platform era began with a primitive that felt like the whole story (HTML pages in 1995) and matured into a category nobody had named yet (web apps, native mobile). Agents are this era's web pages — and since the models are already sufficient to rebuild Knowledge Navigator "for real," the remaining work is category invention: the primitives (org-level harnesses, realtime voice, context machinery, dynamic UI) are this cycle's compilers and databases, and the mature form gets discovered by building against the old visions | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-ai-native-software`, `el-knowledge-navigator`, `el-agents-plus` |
| `ins-visicalc-against-ai-unemployment` | Offered explicitly as "a counterargument to the fear that AI leads to mass unemployment": VisiCalc took work that "required a room full of people" and specialized knowledge and made it generally accessible — and it did **not** put accountants out of business; it made "much much much more accounting-like work possible" plus categories of work previously inconceivable. The precedent's claim applied to AI: industrializing execution expands the addressable work and moves humans up the stack rather than out of it | `HighlightsPattern → pat-value-of-judgement` **[registry]** | — (historical-analogy argument; no element coined for VisiCalc — see review note 3) |

## KnowHow (1 new)

All: `SourcedFromArtifact → ia-aie-kramer-ai-native-primitives`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-build-llm-native-experiences` | Build software with inference at the core, not bolted on | Treat the LLM as the substrate of every interaction rather than a feature behind a button (expect hundreds of inference calls in a session and design for it); compress context **asynchronously and non-blockingly** so the hot path never waits on housekeeping; run long-lived sub-agents that share context instead of monolithic per-request calls; load skills progressively rather than front-loading capability; generate UI dynamically as part of the interaction loop (show/hide the map on request) instead of pre-building every screen; make conversational voice a first-class modality, not an accessibility layer; and orchestrate all of it in real time — the patterns Gradient Bang exists to exercise | `ReferencesElement → el-gradient-bang`, `el-context-compaction` **[registry]**, `el-agent-skills` **[registry]**, `el-generative-ui` **[registry]**, `el-pipecat` **[registry]**, `el-progressive-disclosure` **[registry]** |

## Dropped

- **The full 80-year timeline** (Bush's As We May Think predictions; 1950s languages/compilers; 1960s interactivity, Sketchpad, Star Trek/HAL; 1970s relational databases, declarative languages, Smalltalk; Mac/Windows; 1990s "the web was multimodal from the very beginning"; 2000s mobile; 2010s cloud-as-AI-groundwork) — framing narrative; the analytic payoff is `ins-agents-are-the-web-pages-of-1995`. The memex is kept prose (rhetorical ancestor), only Knowledge Navigator is coined (it does load-bearing work: yardstick + remake evidence).
- **Movie-clip material** (Star Trek computer, HAL 9000, Minority Report, Iron Man/Jarvis) and **John Underkoffler's g-speak lineage / the 2006 startup** (Oblong per public record; unnamed in transcript) — biography and imagination-history; folded into the Expert brief.
- **VisiCalc as an Element** — see review note 3.
- **"Latent space / No Priors crossover" as a SourceEntity** — the clip's provenance is prose inside `sig-nadella-org-level-harnesses`; coining a podcast source for one embedded clip is over-structure.
- **Booth/socials outro** — logistics.

## Review notes

1. **Prompt question answered — held pattern-less, and NOT `pat-ai-native-org`.** The talk's thesis is about software artifacts, not organizational structure, so the coined `pat-ai-native-org` would be a false home; nothing here is edged to it. `sig-knowledge-navigator-now-buildable` and `sig-gradient-bang-llm-at-every-interaction` are held pattern-less as two data points for an uncoined **"AI-native software category"** candidate — software with inference at the core of every interaction as the post-agent form. Ledger adjacencies if it accretes: ten Teije's pipeline-is-dead / `pat-adaptive-software` candidate (b7 — one stem, live divergences), Browne's products-as-markdown (b6), An/Hoe's `el-runtime-llm-gameplay` (b6 — reused here), and this same batch's Varda per-user gadgets. Alternative homing documented: both signals could defensibly land on `pat-model-not-bottleneck` (the models suffice; the category/periphery is what's missing — that reading is carried at insight altitude instead) or `pat-harness-over-model` for Gradient Bang's pattern list. Held pattern-less to keep the candidate's evidence clean.
2. **Garbles.** "Quinn Low Holman Kramer" → **Kwindla Hultman Kramer** (byline: Kwindla Kramer); "Pipcat" → Pipecat; "Satia Nadella" → Satya Nadella; "Vanavar/Venavar Bush" → Vannevar Bush; "the mimics" → **the memex** (Bush's device — high confidence from context); "Visical" → VisiCalc; "John Underoffer" → **John Underkoffler**; "Tavis" → **Tavus** (AI video company — matches the released remake); "Kubric" → Kubrick; "gooies" → GUIs; "Moors law" → Moore's law; "multimodalorked" → multimodal networked; "Wake up, Daddy Sean" → the Iron Man "wake up, daddy's home" Jarvis scene. Speaker-name and Tavus resolutions are load-bearing; both are high confidence but note the Tavus company node rests on the resolved name.
3. **VisiCalc deliberately not coined; insight carries no `ReliesOnElement`.** The b13 precedent (`el-antikythera-mechanism`) shows historical artifacts *can* be coined when load-bearing, but VisiCalc here powers exactly one argument (the labor-market analogy), which lives whole in `ins-visicalc-against-ai-unemployment`. The schema does not require the edge; coin `el-visicalc` at review if you want the precedent symmetrical.
4. **Nadella kept prose despite an extended played clip.** Corpus discipline keeps cited/quoted figures as prose (Calvin French-Owen b5, Ido Salomon in b8 before this batch resolved him). The clip is nonetheless this file's strongest harness-over-model evidence and is fully captured in `sig-nadella-org-level-harnesses`; if review prefers quoted-at-length figures as Experts, `exp-satya-nadella` (AffiliatedWithCompany → co-microsoft, no ContributedByExpert) is the one-node change.
5. **One inferred edge, flagged.** `el-gradient-bang UsesElement → el-pipecat` is inferred from authorship (Daily's own game showcasing conversational voice), not stated in the transcript — downgrade to prose if the bar is transcript-stated-only. All other Gradient Bang element edges quote its own narrated pattern list.
6. **`co-tavus` thinness.** Coined for one dated, checkable fact (the Knowledge Navigator remake, published ~2 weeks before this talk). If review drops it, `sig-knowledge-navigator-now-buildable` keeps the fact as prose and loses only the RelevantCompany edge.
7. **Corpus cross-checks performed per the brief.** `co-daily`, `el-pipecat`, `el-smart-turn`, `el-silero-vad`, `el-turn-detection` all pre-exist (b2) and were reused, not re-coined; this file adds the missing Expert for the b2 prose citation of Kramer. `sig-pipecat-neutral-voice-layer` upgrades b2's secondhand Pipecat framing with first-party adoption claims (AWS/Nvidia/Anthropic) — the "most widely used" superlative is vendor-stated; treat as such externally.
8. **Signal-bar caveat.** Half the talk is history and played media; the four signals isolate its dated, falsifiable content (Nadella's harness doctrine, Pipecat adoption, the Tavus remake, Gradient Bang's existence and timing claim). Gradient Bang is a personal/research project without a ship date — weight `sig-gradient-bang-llm-at-every-interaction` as a capability demonstration, not a product signal.
