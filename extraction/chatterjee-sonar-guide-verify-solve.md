# SPIKE extraction — "Guide, Verify, Solve" (Anirban Chatterjee, Sonar) — FOR REVIEW

Source transcript: `transcripts/chatterjee-sonar-guide-verify-solve.txt` (auto-captions — quotes are paraphrases, not verbatim).
Video: https://youtu.be/03l29gJXpCE — AI Engineer World's Fair, published 2026-08-09.
`stagingTimestamp` for the artifact and all signals: 2026-08-09 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.

Context: Sonar's **second** corpus talk — a product-marketing deep-dive on the day after CEO Tariq Shaukat's keynote (`ia-aie-shaukat-verifiers-king`, batch 2), unpacking the same material: the CMU study, the model-quality leaderboard, the ACDC loop, and two product launches (Sonar Vortex GA, the Gitar acquisition). This is verification-gap home turf from a vendor that **sells code-quality gates** — the same both-witness-and-instance structure as Heiner/Surge (see Review note 1). Caption garbles: "Tarik"/"Tarek" → **Tariq** (Shaukat), "SonarCube"/"Sonar Cube" → **SonarQube**, "Gitarr"/"Guitar" → **Gitar**, "Any Gravity" → likely **Antigravity** (see Review note 6).

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-chatterjee-guide-verify-solve` | Guide, Verify, Solve (Anirban Chatterjee, Sonar — AI Engineer World's Fair) | youtube | https://youtu.be/03l29gJXpCE |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-anirban-chatterjee`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-anirban-chatterjee` | Anirban Chatterjee (product marketing, Sonar; former software engineer — "writing code for servers, if you can believe it"; first time at this conference) | `AffiliatedWithCompany → co-sonar` **[registry]** |

Referenced, not a contributor: `exp-tariq-shaukat` **[registry, batch 2]** — cited throughout as "Tarik's keynote yesterday"; this talk explicitly expands that keynote's charts. No `ContributedByExpert` edge (he does not speak here); the cross-artifact relationship is prose.

## Companies (2 new)

| slug | name | type | note |
|---|---|---|---|
| `co-gitar` | Gitar | developer | AI code-review company based in San Mateo, **acquired by Sonar a few weeks before the talk** (~July 2026). Product: LLM-driven code review plus building/automating the full CI workflow — find issues, block merges on quality, write fixes, and (trust permitting) approve and merge PRs fully automatically. ⚠ captioned "Gitarr" and "Guitar" — normalized to Gitar |
| — | — | — | — |

Reused **[registry]**, edge-only:

| slug | reuse note |
|---|---|
| `co-sonar` **[batch 2]** | Second corpus talk; first from product marketing rather than the CEO. New facts for the brief: >7M developers use SonarQube; ~750B lines of code analyzed daily across their solutions; Gartner Magic Quadrant leader; acquirer of Gitar; runs the public "LLM Leaderboard" on its website |
| `co-cursor` **[batch 3]** | Doubly present: the AI tool identified in the CMU GitHub study ("in this case it was Cursor, although it could have been any AI tool") and the harness running the Sonar Vortex demo (Cursor calls the Vortex context tool, then the verification tool, and loops until it passes) |
| `co-anthropic` **[seed]** | Claude Opus 4.6 vs Sonnet 4.6 as the leaderboard's worked example; Claude Code named among Vortex integrations |
| `co-carnegie-mellon` **[batch 15]** | The GitHub productivity study is CMU's (reused as `RelevantCompany`; batch 2 carried CMU as prose before the node existed) |

## Elements (5 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-verification-debt` | Verification debt | concept | harness | The gap between the quality AI tools produce by default and the quality the application actually needs — a gap that **scales with criticality**. For experiments, solo projects and short-lived internal tools the gap is small enough to live with; for large codebases with many users (some adversarial), many concurrent changes and regulatory exposure, the needed quality is far above the default, and humans are pulled in to close the gap before shipping. Causes named: models are error-prone by construction; they are missing context (the wider codebase, the business, "the meeting you had 2 weeks ago"); and they don't know your objectives. ⚠ merge-check vs `el-reviewdebt` **[batch 5]** and `el-cognitive-debt` **[batch 5]** — see Review note 5 |
| `el-sonarqube` | SonarQube | product | harness | Sonar's zero-trust, multi-layered verification platform: syntax issues, data-flow issues, architectural issues, control-flow issues, across essentially any language; assigns grades across quality, security and maintainability and blocks PRs that don't pass the quality gate; provides "deep hooks" giving agents first-party access to verification. Scale stats stated: >7M developers, ~750B lines of code analyzed per day. ⚠ deliberately NOT coined in batch 2 (carried in the `co-sonar` brief) — promoted here because this talk makes the product itself load-bearing; see Review note 4 |
| `el-gitar` | Gitar (AI code review + CI automation) | product | harness | The acquired product: LLM-driven "superhuman review" living in the PR flow alongside SonarQube's computational review; can block merges when issues breach quality criteria, write fixes, and approve/merge PRs completely automatically — but defaults to find-and-show-and-discuss, with automation features turned on progressively as trust is earned |
| `el-sonar-remediation-agent` | Sonar remediation agent | product | harness | GA'd the week of the talk: point it at backlog issues, tech debt and legacy code and it improves the codebase "almost in the background" at a scale human developers don't have bandwidth for, while humans stay on front-end innovation work |
| `el-sonar-llm-leaderboard` | Sonar LLM Leaderboard | product | harness | Sonar's public model benchmark: every major new model gets ~4,000 coding tasks and is scored with SonarQube's own metrics — correctness, complexity, task-solve rate, maintainability, reliability, security — and graphed across those axes. The worked example: Sonnet 4.6 is strong on correctness/solve-rate; Opus 4.6 wins on maintainability, security and lower complexity — so users toggling models "to control token burn rates" are also toggling quality profiles. ⚠ vendor-owned benchmark — see Review note 1; batch 2 carried the same instrument as a signal only (`sig-sonar-model-quality-bench`) |

Registry element reuse (no new nodes, edges only):

| slug | reuse note |
|---|---|
| `el-sonar-vortex` **[registry, batch 2]** | **GA as of this week** (batch-2 brief said "launched at World's Fair 2026-07" — that was the announcement; this is availability). Widen-brief note: batch 2 described the *guidance* half (context + constraints, >30% token reduction); this talk adds the second half — **in-loop real-time verification**: the agent calls in mid-write, gets a live issue list, fixes immediately so issues don't propagate into future agentic loops, and won't proceed until it passes. Integrations: Cursor, Claude Code, Codex, "Any Gravity" (⚠ → Antigravity, Review note 6) |
| `el-multilayer-verification` **[registry, batch 2]** | Restated as the two properties of successful automated verification: **zero trust** (code could come from anywhere — human or any AI; never validate with the AI that wrote it; auditable, explainable, algorithmic, repeatable) and **multi-layered** (computational review + LLM-driven review "and everything else in between" — no one or two methods find every problem) |
| `el-generator-validator-separation` **[registry, batch 1]** | "Use a different methodology to review the code that was used to write the code" — the zero-trust half of the definition, third vendor statement of this thesis in the corpus |
| `el-automation-bias` **[registry, batch 7]** | The Wharton study is this node's strongest quantification in the corpus: participants followed AI advice 92.7% of the time when it was right — and **~80% of the time when it was confidently wrong**. Applied here to code review: at AI code volume, with multiple agents writing simultaneously, review load forces rubber-stamping "happening in all of your organizations" |

Element edges: all five new `IdentifiedInArtifact → ia-aie-chatterjee-guide-verify-solve`; the four reused elements above also `IdentifiedInArtifact → ia-aie-chatterjee-guide-verify-solve` (additional artifact each).
`el-sonarqube` `DevelopedByCompany → co-sonar` **[registry]**, `UsesElement → el-multilayer-verification` **[registry]**, `EnablesPattern → pat-verification-gap` **[registry]**;
`el-gitar` `DevelopedByCompany → co-gitar`, `EnablesPattern → pat-verification-gap` **[registry]**;
`el-sonar-remediation-agent` `DevelopedByCompany → co-sonar` **[registry]**, `UsesElement → el-sonarqube`;
`el-sonar-llm-leaderboard` `DevelopedByCompany → co-sonar` **[registry]**, `UsesElement → el-sonarqube`, `ExemplifiesPattern → pat-verification-gap` **[registry]**;
`el-verification-debt` `ExemplifiesPattern → pat-verification-gap` **[registry]**.

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-chatterjee-guide-verify-solve`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | extra edges |
|---|---|---|---|---|
| `sig-cmu-ai-productivity-spike-reverts` | harness | The CMU GitHub study, in more detail than the batch-2 keynote: projects sorted by metadata into traditional-tooling vs AI-tool (Cursor) cohorts show a **temporary productivity spike lasting ~3 months, then reverting** — while a **persistent increase in static-analysis warnings and code complexity** (measured with SonarQube) continues well past the 3-month mark. The debt is what reverts the spike: the accumulated issues "end up actually slowing developers down even more". ⚠ near-duplicate of `sig-cmu-velocity-fade` **[batch 2]** — same study, same vendor, second speaker; merge option in Review note 2 | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-verification-debt`; `RelevantCompany → co-carnegie-mellon` **[registry]**, `co-cursor` **[registry]**, `co-sonar` **[registry]** |
| `sig-humans-rubber-stamp-ai-code-review` | harness | Human review as the verification backstop is measurably compromised: in a Wharton study where the AI assistant was covertly instructed to confidently lie some of the time, participants followed correct AI advice 92.7% of the time — and wrong AI advice **nearly 80% of the time**. Extrapolated to code review under AI throughput (more code, multiple agents writing simultaneously, all merging into one application): "the load is just too great… there's a lot of rubber stamping that I'm sure is happening in all of your organizations. It's happening everywhere." Conclusion drawn: human review must be backstopped by automated verification, not the other way round | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-automation-bias` **[registry]**, `el-multilayer-verification` **[registry]** |
| `sig-model-quality-profiles-diverge` | harness | No two models are the same and their **quality issues are diverse along independent axes**: on Sonar's ~4,000-task leaderboard, Sonnet 4.6 is strong on correctness and task-solving while Opus 4.6 delivers better maintainability, security and lower complexity — so the common practice of toggling models to control token burn is silently a quality-profile decision, and model choice should be per-task-type. The standing consequence: "none of these models are ever going to be perfect. You're always going to have some kind of need for verification in the loop" | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-sonar-llm-leaderboard`; `RelevantCompany → co-anthropic` **[registry]**, `co-sonar` **[registry]** |
| `sig-verification-moves-into-both-loops` | harness | Verification is being re-architected from post-hoc human review into **both loops of AI development**: in the *inner agentic loop*, the agent gets guardrails and scoped context up front (guide), calls verification in real time as it writes, and fixes findings immediately so they don't propagate into later loops (solve) — demoed live with Cursor calling Sonar Vortex and refusing to proceed until it passes; in the *outer CI/CD loop*, an LLM-driven "superhuman review" (Gitar) runs alongside computational review (SonarQube), grades the PR across quality/security/maintainability, and **blocks it from production without a passing grade**, with a fix agent for what's found. Autonomy is a ratchet: Gitar defaults to find-show-discuss and earns its way to auto-fix/auto-merge | `FormsPattern → pat-verification-gap` **[registry]**; `FormsPattern → pat-harness-over-model` **[registry]** (claim-1/reliability scope — deterministic gates around the agent, see Review note 3) | `OnElement → el-sonar-vortex` **[registry]**, `el-gitar`, `el-sonarqube`; `RelevantCompany → co-sonar` **[registry]**, `co-gitar`, `co-cursor` **[registry]** |
| `sig-enterprises-demand-one-verification-rulebook` | security | What customers say drives verification adoption, per Sonar's customer conversations: (1) **consistency** — one standard rulebook across every project, team and AI tool, not per-team verification silos; (2) **tool effectiveness** — token efficiency and right-model-for-right-project discipline; (3) **shift-left security** — with CVEs now "announced and then immediately exploited… almost often the same day", issues must be caught at write time; (4) **compliance** — regulated industries need an audit trail proving verification ran constantly and consistently. Closing prescription: standardize on a *single independent* multi-layered verification platform to eliminate the blind spots created by tool silos | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-multilayer-verification` **[registry]**; `RelevantCompany → co-sonar` **[registry]** |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-verification-is-the-scaling-unlock` | The talk's core argument, and the cloud-computing analogy that frames it: AI coding is at the turning point "from experimentation to engineering", and what cloud needed to expand access — repeatability, scalability, consistency — AI coding gets from **safety and trust, i.e. a governance and verification regime**. The verification-debt gradient explains why: the default-vs-needed quality gap is negligible for experiments and decisive for critical systems, so verification is precisely the capability that determines how far up the criticality ladder AI-written software can climb. "A key enabler and a key unblocker", not overhead | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-verification-debt`, `el-multilayer-verification` **[registry]** |
| `ins-code-is-provable-software-is-not` | The comfort of code — write the function right and it runs the same way every time — does not survive scale: requirements and externalities shape behavior, accumulating code interacts unpredictably, and users (including adversaries) do things you didn't expect. "Software is not provable in the same way that code is provable — software can break in interesting and novel ways." So generation quality can never retire verification: as AI writes more software against bigger problems, you hit the unprovability limit more often, and automated verification is the risk control you buy back for the control you released over how the code gets written | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-multilayer-verification` **[registry]**, `el-generator-validator-separation` **[registry]** |

## KnowHow (2 new)

All `SourcedFromArtifact → ia-aie-chatterjee-guide-verify-solve`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-run-guide-verify-solve-loops` | Run agentic coding as guide → verify → solve, in both loops | Before any loop: write down and encode your **specs** (desired architecture and architectural constraints, coding standards and acceptable patterns, dependency allow/deny lists, syntax standards, logging/observability/tracing practices) and your **quality criteria** (acceptable security/quality/maintainability levels — ship-with defaults exist, adjust per org). *Guide*: give the agent scoped context and guardrails for the task at hand — never the whole codebase, which produces thrashing, exploring and token burn. *Verify*: in the inner loop, have the agent call verification in real time and fix findings immediately so they don't propagate into later loops; in the outer loop, run LLM-driven and computational review together, grade the PR across quality/security/maintainability, and hard-gate merges on a passing grade. *Solve*: give the agent the tools and the agency to remediate its own findings, then repeat the loop. Keep verification multi-layered and reasoning-based, spanning quality, security and compliance | `ReferencesElement → el-sonar-vortex` **[registry]**, `el-sonarqube`, `el-gitar`, `el-multilayer-verification` **[registry]** |
| `how-bound-agent-autonomy-with-verification` | Bounded autonomy: free generation, centralized verification | Establish bounded-autonomy guidelines: agents get freedom to generate, inside a centralized scheme of verification and constraints. Never let the generating model validate its own output — use a different methodology (and diversity of tools) to review than to write. Make verification zero-trust (source-agnostic — human or any AI), auditable, explainable, algorithmic and repeatable, so you can prove it ran the same way every time (compliance). Standardize on a **single independent multi-layered verification platform** across all projects, teams, developers and AI coding tools — silos of per-tool verification create blind spots. Ratchet automation trust: start at find-and-show-and-discuss, enable auto-fix and auto-merge only as confidence accumulates. Equip developers as orchestrators — they design the context frameworks and processes the agents run inside | `ReferencesElement → el-multilayer-verification` **[registry]**, `el-generator-validator-separation` **[registry]**, `el-gitar`, `el-verification-debt` |

## Dropped

- **ACDC as an Element** — followed the explicit batch-2 precedent (`shaukat-sonar-verifiers-are-king.md` Dropped: "vendor framework branding; its substance is carried by `el-multilayer-verification` and the knowhows"). This is now the framework's second, fuller appearance ("agentic agent-centric development cycle", guide/verify/solve phases) — promotion option flagged in Review note 4.
- Cloud-computing analogy as a standalone signal — framing device; carried inside `ins-verification-is-the-scaling-unlock`.
- "Turning point from experimentation to engineering" — same; framing prose.
- Gartner Magic Quadrant leadership, 7M developers, 750B lines/day — vendor scale stats; carried in the `co-sonar` reuse note and `el-sonarqube` brief.
- "Any Gravity" as an element — garbled integration name (Review note 6); the integration list is prose in the `el-sonar-vortex` reuse note.
- Wharton as a Company node — single study attribution, no institutional claims; prose (CMU got an edge only because `co-carnegie-mellon` already exists).
- Fable mention ("really excited to start playing with Fable") — color; no `el-claude-fable` edge, nothing is claimed about the model.
- The customer-booth pitch and closing slides — marketing color.

## Review notes

1. **⚑ Vendor incentive — the Heiner structure again (read with `heiner-surge-benchmaxxing.md` Review note 1).** Every empirical claim in this talk doubles as a pitch for what Sonar sells: the CMU study's debt data was **collected with SonarQube**; the model-divergence evidence comes from **Sonar's own leaderboard** scored with **Sonar's own metrics**; the rubber-stamping study motivates automated review the week Sonar GA's an automated-review product and announces an AI-code-review acquisition. None of this falsifies the claims — the CMU and Wharton studies are third-party, and the corpus independently corroborates every leg (velocity-reverts: b2; automation bias: b7 Duolingo; in-loop verification: b12 Arize/Uber; multi-agent review load: b5 ReviewDebt) — but the file should be read as *interested testimony*, like Surge on benchmarks. Also worth noting for the benchmark-trust ledger: `el-sonar-llm-leaderboard` is itself a **vendor-owned public benchmark** whose scoring instrument is the vendor's product — an instance of the economic-motive leg's structure (build the metric, sell the remedy), held as prose here because the talk gives no reason to doubt the scores themselves.
2. **Same-company duplication with batch 2 — three merge options for the reconciler.** (a) `sig-cmu-ai-productivity-spike-reverts` vs `sig-cmu-velocity-fade` [b2]: same study; this version adds the metadata-cohort method, the Cursor identification, the SonarQube instrumentation, and the *persistence* of the warning/complexity increase past the spike window; b2's adds the 3–5× magnitude. Merge into one signal with two artifacts, or keep both (per-talk convention). (b) `sig-model-quality-profiles-diverge` vs `sig-sonar-model-quality-bench` [b2]: b2 states the benchmark finding; this adds the leaderboard-as-product, the per-axis Sonnet/Opus split, and the token-burn framing. (c) `el-sonarqube` promotion — see note 4. I kept all as separate per-talk nodes per corpus convention (MSFT voice precedent), flagged here.
3. **Pattern homing.** All five signals form `pat-verification-gap` — this is the pattern's home vendor and nothing here is surprising. One dual edge: `sig-verification-moves-into-both-loops` also forms `pat-harness-over-model`, deliberately scoped to **claim 1** per the batch-15 FINDING-1 rescoping (reliability/control come from deterministic scaffolding around the model — quality gates, blocked merges, ratcheted autonomy). It makes no claim-2 (capability-from-harness) assertion. No `pat-benchmark-trust-crisis` material here beyond the note-1 aside — the leaderboard is presented un-critiqued.
4. **`el-sonarqube` promotion reverses a batch-2 restraint call.** Batch 2 kept SonarQube inside the `co-sonar` brief. Promoted here because this talk makes the *product* load-bearing (it is the computational-review layer in the outer loop, the grade-assigner, the quality gate, and the leaderboard's scoring instrument) and gives it scale stats. If you'd rather keep the restraint, drop the node and rehome its three element edges onto `el-multilayer-verification` — the signals survive intact. Same decision, opposite lean, for **ACDC**: still not coined (twice-stated branding is still branding), but if a third Sonar talk arrives, promote `el-acdc` and rehome the loop description from `how-run-guide-verify-solve-loops`.
5. **`el-verification-debt` merge-check.** Adjacent nodes: `el-reviewdebt` [b5] (review capacity falling behind generation), `el-cognitive-debt` [b5] (humans losing understanding of AI-written code), and b2's own `sig-cmu-velocity-fade` (debt generated as fast as code). Verification debt is the *criticality-indexed quality gap* — closest to reviewdebt but indexed on application criticality rather than review bandwidth. Kept separate; merge candidates flagged.
6. **Caption garbles.** "Tarik"/"Tarek" (×4) → **Tariq** (Shaukat, CEO — `exp-tariq-shaukat` [registry]); "SonarCube"/"Sonar Cube" → **SonarQube**; "Gitarr" → **Gitar**, then captioned "Guitar" (×2) later in the same talk; "Any Gravity" → **Antigravity** (⚠ best reading — Google's agentic IDE fits an integration list alongside Cursor/Claude Code/Codex; unverified, no corpus prior); "agentic agent-centric development cycle" → **ACDC, Agent-Centric Development Cycle**; "the solve state come" → the solve stage; "our LM approach" → our LLM approach; "from a standpoint" (truncated) → the third leaderboard axis is unrecoverable from captions (likely task-solve rate).
7. **Numbers to preserve exactly** (stage-stated): 92.7% / ~80% (Wharton follow rates, right/wrong); ~3 months (CMU spike duration); ~4,000 leaderboard tasks; >7M developers; ~750B lines/day.
