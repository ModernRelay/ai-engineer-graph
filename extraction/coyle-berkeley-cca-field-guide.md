# SPIKE extraction — "Anthropic's CCA Exam as a Field-Guide for Agentic Engineering" (Frank Coyle, UC Berkeley) — FOR REVIEW

Source transcript: `transcripts/coyle-berkeley-cca-field-guide.txt` (auto-captions — quotes are paraphrases, not verbatim; "Sam Bagwell", "Cloud Code", "co-supreme AI" garbles, see review note 2).
Video: https://youtu.be/Z-c11pV_uvU — AI Engineer World's Fair, published 2026-08-08.
`stagingTimestamp` for the artifact and all dated nodes (signals, knowhows): 2026-08-08 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.
Shape of the talk: a 30-year CS educator's **second corpus talk** (`exp-frank-coyle` **[registry, b10]**) reads Anthropic's new Claude Certified Architect exam as a field guide — "Anthropic knows how people are using their system and what the issues are going to be" — walking its production scenarios and, deliberately, their **anti-patterns**, on the thesis that "understanding what you should not do is the key to what you should do." Framed by an employability claim: "computer science is no longer the magic pathway to a job."

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-coyle-cca-field-guide` | Anthropic's CCA Exam as a Field-Guide for Agentic Engineering (Frank Coyle, UC Berkeley — AI Engineer World's Fair) | youtube | https://youtu.be/Z-c11pV_uvU |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-frank-coyle` **[registry, b10]**.

## Experts (0 new)

Reused: `exp-frank-coyle` **[registry, b10]** — second talk (first: `ia-aie-coyle-agentic-ontologies`); the b10 node's brief already covers the 30+-years-CS-educator identity, now at Berkeley. Kept in prose (no nodes): Boris Cherny (⚠ "Cherney" — "he doesn't write code, his job is to write loops"), Peter Steinberger quoted secondhand (`exp-peter-steinberger` **[registry, b6]** exists but did not contribute this artifact — no edge), Sam Bhagwat via his handed-out book (`exp-sam-bhagwat` **[registry, b11]**, same no-edge reasoning), Böhm and Jacopini (1966 theorem authors), Sister Corita Kent and Thomas Edison (epigraphs).

## Companies (0 new)

Reused: `co-anthropic` **[seed]** (exam owner), `co-uc-berkeley` **[registry, b10]** (speaker affiliation, already edged from the expert node).

## Elements (4 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-cca-exam` | Claude Certified Architect (CCA) exam | product | harness | Anthropic's certification for agentic engineering, released March (⚠ 2026 presumed; verify official name/date — captions say "Claude Certified Architect"/"Claude Certified Exam"). Scenario-based, timed, proctored; offered to companies in the Anthropic ecosystem, and individuals can pay $99 to sit it once every 6 months. Multiple-choice, but grounded in realistic constraints. **Five weighted domains**: agentic architecture 27%; Claude Code configuration & workflow 20%; prompt engineering / structured output (JSON everywhere); tool design & MCP integration; context management & reliability. **Six production scenarios** — customer-support resolution agent, code generation with Claude, multi-agent research system, developer productivity, Claude Code for CI, structured data extraction — of which each sitting randomly draws four and centers all questions on them |
| `el-stop-reason-loop-control` | Stop-reason loop control | concept | harness | The exam's canonical agentic loop: `while true`, call the model with the message history, then **branch on `stop_reason`** rather than consuming whatever came back. `tool_use` means the LLM — "a probabilistic next-word predictor; it can't execute tools, it can't do anything except talk back to you" — has emitted parameters for *your code* to execute; run the tool, append the result, continue; loop exit is followed by a confidence check with human escalation. The trap the exam tests: an **out-of-tokens stop reason**, where a truncated response still looks like an answer — "it's going to give you a response, but you need to take action." Anti-pattern: fire the agent, take the response, use it. ⚠ merge-adjacent to `el-loop-flow-control` **[registry, b13]** — see review note 4 |
| `el-context-forking` | Context forking with summary-only return | concept | context | Subtask isolation as the exam teaches it: fork the agent's work into a separate context ("a separate thread — whatever the agent does and thinks and adds tokens to does not come back and pollute the main context"), then merge back **only the summation**. Explicitly analogized to multi-threaded programming: shared memory forces synchronization and locks, so keep threads — and agents — independent. Paired with budget hygiene: check the token count, and past a threshold (~150K in his example) run compaction. Anti-patterns: every subtask dumping full output into the primary thread; letting context grow unbounded — "a million-token context window? No, don't put everything in there… the more context, the more confused the LLM" |
| `el-agent-anti-patterns` | Agent anti-patterns (design-patterns redux) | concept | harness | The talk's method claim: the 1990s design-patterns movement has re-run for agents — "we now have patterns for agents, but there's also anti-patterns," and the anti-patterns are the pedagogy, often the key to the exam questions. The walked set: consume responses without checking `stop_reason`; the overloaded generalist agent ("you hire a carpenter and the guy shows up with plumbing tools, carpenter tools, electrical tools — 'I can do anything'; maybe you want a professional carpenter" — specialize to one job, one or two tools); subtask context spilling into the main thread; unbounded context growth; sharing the generator's reasoning with critic agents (groupthink); interactive modes inside CI pipelines. ⚠ merge-check vs `el-pattern-anti-pattern-catalog` **[registry, b14]** — see review note 5 |

Element edges: all four `IdentifiedInArtifact → ia-aie-coyle-cca-field-guide`; `el-cca-exam` `DevelopedByCompany → co-anthropic` **[seed]**, `UsesElement → el-claude-code` **[registry, b5]**, `UsesElement → el-mcp` **[seed]**, `ExemplifiesPattern → pat-value-of-judgement` **[registry]**; `el-stop-reason-loop-control` `UsesElement → el-agent-loops` **[registry, b6]**, `ExemplifiesPattern → pat-harness-over-model` **[registry]**; `el-context-forking` `UsesElement → el-context-compaction` **[registry, b6]**; `el-agent-anti-patterns` `UsesElement → el-generator-validator-separation` **[registry, b1]**.

Reused elements (no new nodes): `el-claude-code` **[registry, b5]**, `el-mcp` **[seed]**, `el-agents-md` **[registry, b6]** (the hierarchical CLAUDE.md guidance — root / project folder / per-directory — is the steering-file family that node already carries; reused rather than coining `el-claude-md`, per the hanchett-b9 precedent), `el-context-compaction` **[registry, b6]**, `el-generator-validator-separation` **[registry, b1]**, `el-agent-loops` **[registry, b6]**. Prose only: `el-mastra` **[registry, b11]** (Bhagwat's book, page 32: extend his base class for custom context-compression logic), `el-ralph-loop` (loops-zeitgeist adjacency, not discussed), batch API (kept inside the KnowHow — a pricing feature, not a node).

## Signals (3 new)

All: `SpottedInArtifact → ia-aie-coyle-cca-field-guide`, `SourcedFromSource → source-aie-yt` **[registry]**; all `RelevantCompany → co-anthropic` **[seed]**.

| slug | domain | name / brief | FormsPattern | other edges |
|---|---|---|---|---|
| `sig-anthropic-certifies-agentic-engineering` | harness | A frontier lab now runs a **proctored professional certification for agentic engineering**: the Claude Certified Architect exam (released March) — scenario-based, timed, $99 per individual sitting every 6 months, offered through the Anthropic ecosystem, with five weighted domains (architecture 27%, Claude Code 20%, prompting/structured output, tool+MCP design, context management & reliability) drawn over six production scenarios. The messenger matters: a 30-year CS educator assigns it because "computer science is no longer the magic pathway to a job" and the exam is his best proxy for what employable agentic judgment looks like — "Anthropic knows how people are using their system and what the issues are going to be" | `FormsPattern → pat-value-of-judgement` **[registry]** | `OnElement → el-cca-exam`, `el-claude-code` **[registry]**, `el-mcp` **[seed]**; `RelevantCompany → co-uc-berkeley` **[registry]** |
| `sig-exam-canon-is-harness-discipline` | harness | What the certification actually tests is scaffolding discipline, not model capability: branch every loop iteration on `stop_reason` (including the out-of-tokens trap); configure hierarchical CLAUDE.md rule files; specialize agents to one job with one or two tools; fork subtask contexts and merge back summaries only; watch token counts and compact; strip interactive prompts from CI; batch non-urgent work. The model itself is dismissed as the object of study — "the LLM can't do anything; it is just a probabilistic next-word predictor; it can't execute tools" — everything examinable lives in the harness around it | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-cca-exam`, `el-stop-reason-loop-control`, `el-context-forking`, `el-agents-md` **[registry]** |
| `sig-agent-groupthink-breaks-critics` | harness | A multi-agent failure mode with an exam-blessed fix: collaborating agents "seem to devolve into one idea" the way party guests cave on pizza — so a critic/validator agent must **not** see the generator's thought process. Pass it only the claim and the evidence ("every agent gets its own slice"), keeping the judgment independent of the reasoning that produced the artifact — the generator/validator separation applied inside multi-agent topologies, with hub-and-spoke orchestration and how-much-should-each-agent-know as explicit exam material | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-agent-anti-patterns`, `el-generator-validator-separation` **[registry]** |

## Insights (2 new)

| slug | name / brief | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-loop-is-the-turing-step` | Why "loops are the new thing" is deeper than fashion: Böhm–Jacopini (1966) proved sequence + conditional + iteration suffice for Turing completeness. Prompt-era LLM systems had sequences and some branching; the agentic loop supplies the third primitive — which is exactly why the field's figureheads describe their job as writing loops (Cherny; Steinberger: "I don't code anymore, I just design loops that prompt agents"). The excitement is the arrival of general computability in the LLM stack — and it is a property of the *harness structure*, not of the model inside it | `HighlightsPattern → pat-harness-over-model` **[registry]** | `ReliesOnElement → el-stop-reason-loop-control`, `el-agent-loops` **[registry]** |
| `ins-vendor-exams-reveal-production-truth` | A lab's certification exam is a leaked map of its production reality: the scenarios are the deployments it actually sees, the domain weights are where its users actually spend effort, and the anti-patterns are the failures its support and safety teams actually field. Reading the CCA is therefore field intelligence even for engineers who never sit it — the syllabus tells you, from the vendor with the best vantage point, what agentic systems break on ("whether you take the exam or not, this is going to help you get ready for whatever the agentic world throws at you") | `HighlightsPattern → pat-value-of-judgement` **[registry]** | `ReliesOnElement → el-cca-exam`, `el-agent-anti-patterns` |

## KnowHow (2 new)

All: `SourcedFromArtifact → ia-aie-coyle-cca-field-guide`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-run-stop-reason-loops` | Run agentic loops on stop reasons | Structure the loop as `while true` → call the model with the accumulated messages → branch on `stop_reason`; remember the LLM only *emits tool parameters* — your code executes the tool and appends the result; continue until the model stops requesting tools; then gate the final answer on a confidence check and escalate to a human below threshold; **always handle the out-of-tokens stop reason** — a truncated response looks like an answer and must not be used as one; never consume a response without reading why generation stopped | `ReferencesElement → el-stop-reason-loop-control`, `el-agent-loops` **[registry]**, `el-claude-code` **[registry]** |
| `how-manage-context-the-cca-way` | Manage agent context the way the exam expects | Layer rule files hierarchically (project root, project folder, per-directory CLAUDE.md) so control is inherited, not repeated; specialize each agent to one job with one or two tools — no carpenter arriving with plumbing gear; fork subtask contexts and merge back only summaries, never raw output; give critic agents the claim and evidence but not the generator's reasoning, to break groupthink; monitor token count and compact past your threshold (~150K in the walkthrough; compaction logic is customizable — e.g. extending Mastra's base class); disable interactive permission prompts in CI pipelines so runs go straight through; and push non-urgent work through the batch API — 50% fewer token costs with results promised within 24 hours | `ReferencesElement → el-context-forking`, `el-agents-md` **[registry]**, `el-context-compaction` **[registry]**, `el-agent-anti-patterns`, `el-generator-validator-separation` **[registry]** |

## Dropped

- **The epigraphs** — Sister Corita Kent ("nothing is a mistake… there's only make") and Edison's 10,000 ways — pedagogy framing; the substantive experiment-experiment-experiment stance needs no node.
- **The AV fumbling and closing bio** (jazz fandom, the Coltrane-named website — ⚠ "co-supreme AI" garble, domain unrecoverable) — logistics/color.
- **The structured-data-extraction scenario** — named as scenario six but never walked; nothing to extract.
- **The book-giveaway aside** — carried as the `el-mastra` prose mention only; the speaker disclaims any connection to Bhagwat.
- **Scenario numbering minutiae and the pizza/carpenter analogies as such** — the analogies' content is inside `el-agent-anti-patterns` and `sig-agent-groupthink-breaks-critics`.

## Review notes

1. **Prompt questions answered.** (a) `exp-frank-coyle` reused — this is his second corpus talk; note the register shift from b10 (ontologies advocacy) to practitioner pedagogy. (b) No prior CCA/certification entity exists anywhere in the corpus (grepped); `el-cca-exam` is coined fresh. (c) `pat-agent-supply-chain` considered and **not** edged: the exam covers tool design and MCP integration as *competence domains*, but the talk makes no claim about skills/servers as a distribution or exploitation surface — nothing at the supply-chain thesis's altitude. Value-of-judgement carries the credentialing claim instead, twice (signal + insight).
2. **Garbles.** "Sam Bagwell" → **Sam Bhagwat** (the handed-out book with the context-compression base class is his; `el-mastra` b11); "Boris Cherney" → Boris Cherny; "Cloud Code"/"Cloud" → Claude Code/Claude throughout; "contact management" → *context* management (domain five, rendered correctly elsewhere); "co-supreme AI" → the speaker's Coltrane-tribute website, exact domain unrecoverable; "coil at Berkeley" → his Berkeley email. Exam name and March release date are as-captioned — **verify the official Anthropic certification name and date before seeding** (flagged on the node).
3. **The exam-content claims are secondhand.** Everything stated about the CCA (domains, weights, scenarios, price, cadence, proctoring) is one educator's account of a proctored exam he is characterizing publicly — plausibly from official prep materials, but not vendor documentation in the transcript. The signal is written to hedge accordingly; verify weights/price against Anthropic's published certification page at seeding.
4. **`el-stop-reason-loop-control` vs `el-loop-flow-control` (b13).** The b13 element is about *when to break* a loop (explicit break metrics, per-use-case stopping rules); this one is about *how each iteration branches* on the API's stop signal, including the truncation trap. Adjacent but mechanically distinct — coined separately with a flag; merge at seeding if you read both as "loop termination discipline."
5. **`el-agent-anti-patterns` vs `el-pattern-anti-pattern-catalog` (b14).** Netflix's b14 element is a company-internal performance catalog agents consume as input; Coyle's is a pedagogical claim about the field's body of knowledge (anti-patterns as curriculum, echoing the 1990s patterns movement). Same word, different altitude and function — kept separate, flagged.
6. **Loops-zeitgeist cross-references.** The Cherny/Steinberger loop quotes independently corroborate the b3/b11 loops discourse (`el-ralph-loop`, the Great Loops Debate, Steinberger's OpenClaw material) — a CS-theory grounding (`ins-loop-is-the-turing-step`) the corpus's loop talks assert but never derive. Prose-level only; no cross-file edges (neither talk is cited by this one beyond the quotes).
7. **Signal-bar caveat.** Three signals is deliberate restraint: the talk is a walkthrough of someone else's artifact, and its independent claims (employability, anti-patterns-as-pedagogy, groupthink) are argued from teaching experience and analogy rather than data. The hard facts worth external citation are the exam's existence, price, cadence and domain structure — all pending verification per note 3.
