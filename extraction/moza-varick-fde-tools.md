# SPIKE extraction — "AI Tools for Forward Deployed Engineering / The Next Bottleneck" (Vasuman Moza + JD Puit, Varick Agents) — FOR REVIEW

Source transcript: `transcripts/moza-varick-fde-tools.txt` (auto-captions — quotes are paraphrases, not verbatim).
Video: https://youtu.be/l0FLhNqBOic — AI Engineer World's Fair, published 2026-07-28.
`stagingTimestamp` for the artifact and all signals: `2026-07-28T00:00:00Z` (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node. **[this batch]** = defined in another file of this 4-talk FDE set.

Context: the **tooling** talk of the FDE cluster and the only one that tries to automate the function itself. Two speakers: the CEO frames the thesis ("the next bottleneck" is how deep you can go into a customer without scaling headcount exponentially), then hands to his head of engineering for the internals of their in-house FDE assistant. Last talk of the cluster, and it explicitly builds on the earlier ones (it echoes Cursor's revenue/cost/risk framing — "as was so eloquently stated previously" — and the high-IQ/high-EQ unicorn hiring problem). ⚠ The company name is garbled throughout — see Review note 1.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-moza-fde-tools` | AI Tools for Forward Deployed Engineering (Vasuman Moza + JD Puit, Varick Agents — AI Engineer World's Fair) | youtube | https://youtu.be/l0FLhNqBOic |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-vasuman-moza`; `ContributedByExpert → exp-jd-puit`.

## Experts (2 new)

| slug | name | edges |
|---|---|---|
| `exp-vasuman-moza` | Vasuman Moza (CEO, Varick Agents — bespoke agentic transformation of large enterprises "from the inside out"; goes by "Voss" in the captions and on stage) | `AffiliatedWithCompany → co-varick-agents` |
| `exp-jd-puit` | JD Puit (head of engineering / platform lead, Varick Agents — presented the FD-agent architecture: engagement agent, workflow agent, post-training and the RL environment for graph traversal). ⚠ name from captions only, see Review note 1 | `AffiliatedWithCompany → co-varick-agents` |

## Companies (1 new)

| slug | name | type | note |
|---|---|---|---|
| `co-varick-agents` | Varick Agents | developer | AI transformation firm doing department-wide agentic re-engineering for large enterprises: begins every engagement with an on-site audit by forward deployed engineers/strategists, then implements agents on its own platform layered on top of the customer's existing systems of record. Explicitly positions itself as *not* a traditional consultancy (its own tooling is what keeps headcount sublinear) and *not* a product company ("what we're building for cannot be solved with just a product"). ⚠ captioned **Veric/Varick** throughout — the spelling here follows the official listing |

Reused **[registry]**, edge-only: `co-sap` **[seed]** and `co-salesforce` **[seed]** — named as two of the four systems of record enterprises will not migrate off (`RelevantCompany` on `sig-agents-on-systems-of-record`). Named without nodes: **NetSuite** and **Microsoft Dynamics** (the other two systems of record — `co-microsoft` **[registry]** exists but the mention is a product name in a list, so no edge is placed; there is no Oracle node in the corpus), **Cursor / Factory** (peer-vendor name-drops), **Granola** (the note-taking tool the engagement agent ingests from), **MIT** (source of the pilot-failure statistic). See Dropped.

## Elements (3 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-fde-agent` | FD agent (forward-deployed-engineering agent) | product | harness | Varick's in-house "Codex for our FDEs", built because forward deployed engineers were uploading ~**150 pages** of client documentation to Claude, waiting ~2 minutes and getting verbose, incorrect analysis. Three stages, the third still unbuilt: **(1) engagement agent** — the FDE's assistant, ingesting Granola meeting notes, documentation and PowerPoint decks, answering the questions FDEs actually ask all day ("who is responsible for this process?", "the Sarah in this email and the Sarah in this Slack message — are these the same person?"); **(2) workflow agent** — the same agent embedded inside the platform while the FDE builds a workflow, flagging missed edge cases and process ownership so the built workflow actually shadows the real process, running alongside whichever coding agent the engineer uses; **(3) autonomous agent** (in development) — receives client change requests by email, queries the existing model of the company, and ships the workflow change without the FDE, reserving human time for on-site interviews and process discovery |
| `el-varick-os` | Varick OS | product | harness | The delivery platform: spin up and monitor agents with governance and evals built in, deliberately **layered on top of the customer's existing systems of record** (NetSuite, Dynamics, SAP, Salesforce) rather than requiring migration off them — the stated wedge for enterprises "too large to move". Hosts the workflows FDEs build and, increasingly, the FD agent that helps build them |
| `el-process-dependency-graph` | Process dependency graph | concept | context | The single source of truth for how a company actually functions, used as the substrate the FD agent reasons over. Claim about shape: enterprise workflows are "remarkably linear, they just have a lot of cycles in them", and process owners want them dependency-driven (person C should not act before A and B have approved) — so a dependency graph fits better than a document store, and the storage choice is deliberately un-opinionated ("five companies downstairs will sell you a graph DB; you can just use Postgres — it doesn't really matter"). What it must capture beyond the documented golden path: the exception routes ("Sarah in AP handles it this way, but when it breaks she sends it to Chris, who takes four days to reconcile a PO against an invoice"), which are unique per company and are the stated reason agents can't yet run end-to-end. Hard part is **traversal**, not storage |

Element edges: all three `IdentifiedInArtifact → ia-aie-moza-fde-tools`. `el-fde-agent` `DevelopedByCompany → co-varick-agents`, `UsesElement → el-process-dependency-graph`, `UsesElement → el-varick-os`, `UsesElement → el-agentic-environment` **[registry, batch 11]**; `el-varick-os` `DevelopedByCompany → co-varick-agents`; `el-process-dependency-graph` `ExemplifiesPattern → pat-context-graphs` **[registry]**; `el-fde-agent` `EnablesPattern → pat-value-of-judgement` **[registry]** (it automates the minutiae specifically so the human keeps the interviewing and process-design judgment); `el-forward-deployed-engineering` **[registry, batch 5]** also `IdentifiedInArtifact → ia-aie-moza-fde-tools`.

Registry element reuse (edges only, no new nodes):

| slug | reuse note |
|---|---|
| `el-agentic-environment` **[registry, batch 11]** | "We create an RL environment where we expose our own custom tools designed to traverse our knowledge graph" — an environment built for an internal *product* capability rather than for benchmarking or for sale, which is a new use-shape for this node in the corpus |
| `el-mcp` **[seed]** | Named as part of what dissolved the execution bottleneck ("harnesses… whether browser-use tooling or API tooling with very robust MCPs that let us execute work with near perfection") |
| `el-claude-code` **[registry, batch 5]**, `el-codex` **[registry, batch 6]** | The reference points for the internal tool ("the Codex for our FDEs"; the FD agent "works next to Claude or Codex, whatever model you're using") and the baseline it was built to beat (150-page uploads to Claude producing verbose, incorrect analysis) |

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-moza-fde-tools`, `SourcedFromSource → source-aie-yt` **[registry]**, `RelevantCompany → co-varick-agents`.

| slug | domain | name / brief | FormsPattern | extra edges |
|---|---|---|---|---|
| `sig-execution-solved-understanding-is-bottleneck` | harness | The talk's thesis, stated as a before/after: in 2024, asking a room whether AI had completed an end-to-end task for them would have drawn almost no hands; in 2026 everyone's hand goes up. Models plus harnesses (browser-use tooling, API tooling, robust MCPs) have moved past the execution bottleneck — **"knowledge work is almost entirely solved"** — so **the next bottleneck is how deeply you can understand a specific business** and re-engineer its work around AI without scaling headcount exponentially. Supporting observation: a healthcare company's sales department operates nothing like a SaaS company's, and extracting that context out of employees into a model call is the part that breaks | `FormsPattern → pat-model-not-bottleneck` **[registry]**; `FormsPattern → pat-value-of-judgement` **[registry]** | `OnElement → el-forward-deployed-engineering` **[registry]**, `el-mcp` **[seed]** |
| `sig-ai-pilots-fail-on-broken-process` | harness | External statistics deployed as the market diagnosis: the **MIT figure that ~95% of generative-AI pilots fail to reach production**, and a second (~**87%**) that most pilots produce no measurable ROI — both attributed to **AI being slapped on top of broken processes** rather than to model capability. Amplifying claim: even engineers with goal loops and current tooling can't say "refactor this codebase" and walk away, so handing the same tooling to non-technical operators in finance, sales, marketing, procurement and logistics cannot produce an engineer's ROI. Consequence: the deliverable has to be the re-engineered process, not the tool | `FormsPattern → pat-saaspocalypse` **[registry]** | `OnElement → el-forward-deployed-engineering` **[registry]** |
| `sig-agents-on-systems-of-record` | harness | Enterprise-reality constraint with a quoted number: enterprises are married to their systems of record — **NetSuite, Dynamics, SAP, Salesforce** — and one client is quoted as having spent **$5 million and 5 years** migrating to NetSuite. Pitching AI that lives separate from those systems means "they'll tell you to get out"; there is no appetite for another migration. Varick's answer is to build agents **on top of** the systems of record, with governance and evals in its own platform — "that is where enterprise needs AI the most, because they're too large to move" | `FormsPattern → pat-saaspocalypse` **[registry]** | `OnElement → el-varick-os`; `RelevantCompany → co-sap` **[seed]**, `co-salesforce` **[seed]** |
| `sig-fde-agent-tooling` | harness | A vendor building tooling to automate its *own* forward deployed function, with the failure it was built from: the platform team was "hanging out with Codex and Claude" while the FDE side of the office was sleep-deprived and miserable, uploading **~150 pages** of documentation to Claude per engagement and getting verbose, incorrect output two minutes later. The FD agent's stated goal is **one FDE or strategist covering several client engagements** — clients emailing 24/7, every process lead (AP relies on AR relies on reconciliation relies on FP&A) pulling in a different direction — **without hiring 50 people**, which is what separates the model from a traditional consultancy. Third stage (autonomous change-shipping) explicitly not built yet | `FormsPattern → pat-value-of-judgement` **[registry]** | `OnElement → el-fde-agent`, `el-claude-code` **[registry]**, `el-codex` **[registry]**, `el-forward-deployed-engineering` **[registry]** |
| `sig-frontier-verbosity-forces-post-training` | training | Concrete negative result on frontier models: asked whether extracted context yields high-quality FDE analysis out of Claude, the answer is "honestly no, which is kind of surprising" — frontier models are **extremely verbose for long analysis and have no concept of which detail the client actually cares about**, which is precisely the consultant's skill ("I only started believing in consultants once we started hiring them"). Response: **post-train open-source models** for the detail/clarity balance (they name a Kimi K2-series model as a favourite), and separately **train graph-traversal tools in an RL environment** — entity resolution across the many "Sarahs" in a company, plus detection of redundancy cycles and DAG violations in the knowledge graph — because reliably extracting the *right* context from the graph is the second half of the problem | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-agentic-environment` **[registry]**, `el-process-dependency-graph`, `el-fde-agent` |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-department-wide-beats-point-solution` | Point solutions cap out: automating prospecting inside sales, or AP inside finance, returns ~5–10% for that function, which is why so much deployed AI shows no measurable ROI. Transforming a whole department at once — classifying each step of an 8-step process as fully autonomous, human-in-the-loop, or human-only (risk too high, or too bespoke for an agent to add value) — is what produces the 25/50/75% numbers, denominated in the same three currencies the Cursor talk named: revenue uplift, cost savings, risk mitigation. The binding design constraint is adoption, not capability: the new process must differ enough to capture the ROI but not so much that operators can't recognize it (replacing an 11-step workflow with one step gets handed back) | `HighlightsPattern → pat-saaspocalypse` **[registry]** | `ReliesOnElement → el-forward-deployed-engineering` **[registry]**, `el-varick-os` |
| `ins-exception-paths-are-the-context` | Enterprise documentation describes the golden path and maybe one edge case; the operational truth is the exception route — who it goes to when it breaks and how many days that costs — and it is different at every company. That undocumented layer, not model capability, is why agents can't run enterprise processes end to end, which makes "what happens when things go wrong?" the highest-yield question an FDE asks and makes the resulting dependency graph (with its cycles and its real owners) the actual deliverable of the discovery phase. Storage is incidental; reliable traversal is the hard problem, and it is worth an RL environment of its own | `HighlightsPattern → pat-context-graphs` **[registry]** | `ReliesOnElement → el-process-dependency-graph`, `el-fde-agent` |

## KnowHow (2 new)

All `SourcedFromArtifact → ia-aie-moza-fde-tools`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-map-and-re-engineer-a-department` | Audit first, re-engineer the process, then deploy agents | Start every engagement with an audit, not an install: embed forward deployed engineers/strategists on site and scope to a single department rather than the whole enterprise. Interview **every** process lead in it (in finance: AP, AR, card reconciliation, banking, billing, FP&A) and ask about both the documented golden path and — the part that matters — what happens when things go wrong, by name and by cycle time. Then re-engineer around AI step by step: mark each step fully autonomous, human-in-the-loop, or human-only where risk is too high or the work is too bespoke for an agent to add value. Keep the redesigned process recognizable — different enough to capture ROI, not so different that adoption collapses. Deploy on top of the systems of record the customer already paid millions to adopt; never make migration a precondition. Report value in revenue uplift, cost savings and risk mitigation, and target department-wide rather than single-workflow numbers | `ReferencesElement → el-forward-deployed-engineering` **[registry]**, `el-varick-os`, `el-process-dependency-graph` |
| `how-build-fde-tooling` | Build the assistant your forward deployed engineers actually need | Watch the FDE side of your own office before building: the symptom is a 150-page document dump into a chat model and a verbose, wrong answer two minutes later. Ingest what they really work from — meeting notes, documentation, slide decks, email, Slack — and answer the questions they really ask (who owns this process; are these two mentions the same person). Represent the company as a **dependency graph** as the single source of truth; don't over-think the store (a graph DB and Postgres both work), because the hard part is traversal. Expect frontier models to be too verbose and to have no sense of what a client cares about, so **post-train an open-source model** for that balance, and **train the traversal tools in an RL environment** — entity resolution across duplicate names, redundancy-cycle and DAG-violation detection. Embed the assistant where the workflow is actually built so it flags missed edge cases and ownership in place, and let it run beside whatever coding agent the engineer already uses. Automate the routine client minutiae (a mailed request to reroute a report) *last*, so human time goes to on-site interviews and process design | `ReferencesElement → el-fde-agent`, `el-process-dependency-graph`, `el-agentic-environment` **[registry]** |

## Dropped

- **Kimi K2.6** as an element — captioned "Kimmy K26" and named as the open-source base they favour for post-training. The registry has `el-kimi-k25` **[seed]** (Kimi K2.5), a *different* version, so no edge is placed rather than asserting a wrong version onto an existing node; the claim survives in `sig-frontier-verbosity-forces-post-training` as "a Kimi K2-series model". Coin `el-kimi-k26` or widen the seed node at review if you want the model link — see Review note 3.
- **NetSuite / Microsoft Dynamics** — named in the systems-of-record list. No nodes: NetSuite would require an Oracle company node the corpus doesn't have, and the Dynamics mention is too incidental to justify a `co-microsoft` **[registry]** edge. The $5M/5-year migration quote (which is about NetSuite) is preserved in `sig-agents-on-systems-of-record` without a node.
- **Cursor and Factory** ("industry giants… I'm sure you're mostly here for them") plus "we're not the Cursor, Anthropic or OpenAI of the world" — peer name-drops framing the company's position; no edges. A third name in the same list is garbled beyond recovery ("Dropm").
- **Granola** — the meeting-notes tool the engagement agent ingests from; single mention as an input source, kept in the `el-fde-agent` brief.
- **MIT** as a SourceEntity — the pilot-failure statistic is cited as "the MIT review"; the number is retained in `sig-ai-pilots-fail-on-broken-process` with attribution in prose. Coining a source node from a second-hand citation in auto-captions would be over-reaching (precedent: the corpus coins `source-*` nodes centrally, not from talk files).
- The **hiring pitch** and closing sales offer.

## Review notes

1. **Caption garbles.** Company: **Veric** throughout the captions (≈15 times), never "Varick" — the spelling here follows the official listing given centrally; verify before public-facing use. Speaker: the CEO introduces himself as "**Voss**", which is how he is addressed on stage; the file uses **Vasuman Moza** per the official listing and records "Voss" in the Expert brief. Second speaker: "**JD Puit**" appears once, spoken by the CEO — this is the least verified name in the batch and the surname could easily be Pruitt/Puitt/Pouit. If you don't want an Expert node on one garbled introduction, drop `exp-jd-puit` and its two edges; the talk's technical content is unaffected (it stays on the artifact). Others: "Netswuite" → NetSuite, "Kimmy K26" → Kimi K2.6, "Codeex/Cloud/claw" → Codex/Claude, "goal loops" → agentic loops (probably "agent loops"), "FTEEs/FDS/FD" → FDEs, "Postgress" → Postgres, "desperate from these systems" → *separate from*, "run a muck" → run amok, "evalu baked in" → evals baked in, "high IQ, high EQ" is captioned correctly.
2. **Two-speaker artifact.** Two `ContributedByExpert` edges on one artifact, following the batch-2 MSFT-voice and batch-12 Gupta+Chopra precedents. The CEO carries the thesis signals; the head of engineering carries `sig-frontier-verbosity-forces-post-training` and most of `sig-fde-agent-tooling`.
3. **Model-version decision.** The `el-kimi-k25` non-reuse is the one place this file declines an obvious registry link. Reusing a K2.5 node to carry a K2.6 claim would put a false version fact into the graph, and the corpus already has version-specific model nodes (`el-claude-opus-47`, `el-glm-52`, `el-gemini-31-pro`), so the clean options are coining `el-kimi-k26` (with `DevelopedByCompany → co-moonshot-ai` **[seed]**) or leaving the claim in prose as it is here. The substantive point — that a *post-trained open-source* model beats frontier models on this task — does not depend on which version.
4. **FDE thesis proposal (one sentence, NOT coined — same text in all four files):** *As models industrialize execution, the vendor's scarce work moves inside the customer — embedding engineers (and now FDE-support agents) in the customer's own processes to discover, define and guarantee outcomes — which collapses product engineering, services and go-to-market into a single function and drags pricing from seats toward outcomes.* **This file is the strongest evidence for the parenthetical**: it is the only talk in the cluster that treats the FDE motion itself as the thing to be automated, with a three-stage internal agent, custom post-training and an RL environment built for it. If the pattern is coined, `sig-execution-solved-understanding-is-bottleneck` is its cleanest statement of the mechanism (execution solved → understanding the business is the bottleneck) and would be a natural `FormsPattern` source alongside its current two edges.
5. **Dual `FormsPattern` on `sig-execution-solved-understanding-is-bottleneck`.** It states both registry theses at once — the value/failure has migrated off the model (`pat-model-not-bottleneck`) and the durable human work is judgment about what should be done (`pat-value-of-judgement`). Precedent for a two-pattern signal: `sig-cross-harness-benchmark-divergence` in `cai-state-of-data.md`. Drop the second edge if you want one pattern per signal.
6. **`pat-saaspocalypse` fits here better than anywhere else in the batch.** `sig-agents-on-systems-of-record` is close to a literal statement of the pattern's mechanism from the deployment side — agents layered over the SaaS system of record, with the SaaS vendor's UI bypassed and the value accruing to the agent layer — and `sig-ai-pilots-fail-on-broken-process` supplies the market-level failure statistic for tool-shaped (rather than work-shaped) AI, which is the services-is-the-new-software argument in negative form.
7. **`el-process-dependency-graph` versus registry context nodes.** It overlaps `el-org-work-graph` **[batch 9, Nx Polygraph]** and `el-company-world-model` / `el-monday-world-model` **[batch 9/10]** — all four are "a graph of how the company actually works". Kept separate because this one is scoped to *process execution paths with exception routes and approval dependencies* rather than to work items or a general world model, and because the talk's load-bearing claim is about traversal difficulty. Flagging it for the merge-check list rather than asserting a cross-file edge.
8. **Signal bar.** This is the strongest external-fact file of the four (the MIT 95%/87% statistics, the $5M/5-year migration quote, the 150-page workflow, the three-stage architecture with a named unbuilt stage). No pattern-less holds were needed here — every signal found an honest registry home.
