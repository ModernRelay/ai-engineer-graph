# SPIKE extraction — "Always-on agents run production without the on-call tax" (Justin Smith, Resolve AI) — FOR REVIEW

Source transcript: `transcripts/smith-resolve-always-on-agents.txt` (auto-captions — quotes are paraphrases, not verbatim).
Video: https://youtu.be/vSx5IULvBns — AI Engineer World's Fair, published 2026-08-09.
`stagingTimestamp` for the artifact and all signals: 2026-08-09 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.

Context: this is the batch-16 pending premiere ("Resolve AI, vSx5IULvBns") now published. A production-operations agent vendor's positioning talk: AI wave 1 (coding) raised change velocity, so the constraint moved to *running* production — and Resolve sells always-on cloud agents (on-call, incident, and the talk's focus, **background agents**) to absorb it. Bears directly on three ledger candidates: `pat-durable-execution` (this is the **dedicated-layer vendor side** of the stock-infrastructure argument), `pat-agent-memory-layer` (the learning system as the stated moat), and the provider-commoditization pair (a one-line token-price corroboration). All held pattern-less per instructions — see Review notes 1–3.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-smith-always-on-agents` | Always-on agents run production without the on-call tax (Justin Smith, Resolve AI — AI Engineer World's Fair) | youtube | https://youtu.be/vSx5IULvBns |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-justin-smith`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-justin-smith` | Justin Smith (founding product engineer, Resolve AI; 15+ years in monitoring/observability/production operations — architect on Splunk's observability suite, ~10 years at VMware; self-described product-design and front-end-architecture person) | `AffiliatedWithCompany → co-resolve-ai` |

Prior employers (Splunk, VMware) kept as prose in the brief per the batch-6 ex-employer precedent (`exp-nuno-campos`) — transcript-stated but no claims made about the firms.

## Companies (1 new)

| slug | name | type | note |
|---|---|---|---|
| `co-resolve-ai` | Resolve AI | developer | Production-operations AI: on-call agent (per-alert triage + full root-cause investigation), incident agents (cross-team sync/impact coordination), and always-on **background agents** for the operational long tail — running on an agent architecture of models + context + reasoning + actions over a continuously-updated learning/knowledge system. Founding hypothesis stated on stage: AI-driven coding velocity would create an influx of production issues, so AI would be needed to *operate* the systems, not just build them. Customers described as "world-class engineering teams… at crazy scale"; surface-of-record is Slack/MS Teams; capabilities exposed over MCP for grafting into customers' own harnesses |

Reused: none (Splunk/VMware prose-only; Kafka/GitHub/granola are tool mentions — see Dropped).

## Elements (5 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-background-agents` | Background agents (ops long tail) | concept | infra | Always-on agents for the production work that has no owner and no page: watching deploys out to health, morning incident digests, chasing the returned P99 drift nobody alerts on, capacity reports against quarterly goals, recurring health checks "not waiting for a customer to complain first". Trigger model: schedules, weekly ceremonies (Thursday on-call handover), event streams (CI/CD deployments, Slack messages), or direct message ("go do some work"). Runtime model: always running, in the cloud ("if you close your laptop, it's okay"), inside a sandbox with a filesystem underneath so the agent self-organizes its work; a task system plus imported skills plus integrations tell it what to do; a shared learning loop reflects on each task and carries lessons across tasks |
| `el-production-context` | Production context vs execution | concept | context | The talk's task decomposition: a task = **execution** (knowing what to do, tool access, loading the dashboard) + **production context** (the judgment that "that metric smells off… this feels wrong and I don't know if I can even explain why"). Execution is necessary but commodity; production context is what makes a background agent's work worth anything — it decides what is important, not just what is checkable |
| `el-production-learning-system` | Production learning system | concept | context | The stated hard part and moat: not model smarts ("models have gotten incredibly capable… especially over the last 6 months") but *truly understanding your environment* — how services interact, where the hotspots are, the current causal chains — captured in an underlying learning/memory system that must **grow as the system evolves**, because the system now evolves faster than ever. Reflection after each task feeds it; knowledge learned in one task applies to others; "we spend so much time on our knowledge system" is the company's differentiation claim. ⚠ pattern-relevant to the uncoined `pat-agent-memory-layer` — no pattern edge, Review note 2 |
| `el-adaptive-deployment-verification` | Change-aware deployment verification | concept | infra | Deployment monitoring beyond fixed CI/CD checks: on each release event (a GitHub tag in Slack), the agent reads the actual changes, works out **which telemetry would evaluate those changes**, and builds a customized check plan for that specific release — e.g. checkout replaces the currency service → watch checkout latency and error rates *and* the Kafka pipeline as the causal chain. Nothing hard-coded: the agent chooses its own re-check cadence and watch window ("maybe I'll come back in 3 days"), and the same mechanism covers change systems that bypass CI/CD entirely — feature flags, infra changes — which today get no monitoring beyond "trusting that an alert might fire" |
| `el-resolve` | Resolve AI platform | product | infra | The productized stack: on-call agent (alert triage + root-cause investigation), incident coordination agents, and background agents over the shared learning system; visibility UI (task inspection, previous runs, per-task work traces) but the first-party surface is Slack/MS Teams, "where you live"; conversational setup (the agent explores your environment, asks clarifying questions, drafts the recurring task, you test then share); behavior tuned by replying in-thread ("this is too verbose — make it shorter" persists into the task definition); exposed via MCP servers so internal harnesses can graft in Resolve's execution and learned production context; bring-your-own skills supported ("don't go duplicate a bunch of stuff") |

Registry element reuse (no new nodes, edges only):

| slug | reuse note |
|---|---|
| `el-mcp` **[registry, seed]** | The integration surface: "everything that I showed is accessible through MCP servers… you can graft Resolve into any system that you have" — including augmenting a customer's own agents with Resolve's learning system |

Element edges: all five new `IdentifiedInArtifact → ia-aie-smith-always-on-agents`; `el-mcp` also `IdentifiedInArtifact → ia-aie-smith-always-on-agents`.
`el-resolve` `DevelopedByCompany → co-resolve-ai`, `UsesElement → el-background-agents`, `UsesElement → el-production-learning-system`, `UsesElement → el-mcp` **[registry]**;
`el-background-agents` `UsesElement → el-production-context`;
`el-adaptive-deployment-verification` `UsesElement → el-production-context`, `EnablesPattern → pat-verification-gap` **[registry]**.
Deliberately **no** pattern edges from `el-background-agents` or `el-production-learning-system` — their only fitting patterns are the uncoined `pat-durable-execution` and `pat-agent-memory-layer` candidates (davis-temporal precedent; Review notes 1–2).

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-smith-always-on-agents`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | extra edges |
|---|---|---|---|---|
| `sig-70pct-engineering-time-is-ops` | infra | The ops-economics quantification: per a survey study, **70% of an engineer's time is not spent writing code** — it goes to running what shipped: maintaining platforms, scaling infra, debugging incidents, on-call, hot fixes, alerts, runbooks, restoring services, escalations, cross-team questions. "Really, coding was never the big bottleneck." AI wave 1 accelerated exactly the 30%, and the talk's conclusion is the model-periphery claim verbatim: "we need full stack AI — it's not just about the models anymore, it's about the context around the models and what the models can do inside of a specific domain" | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-production-context`; `RelevantCompany → co-resolve-ai` |
| `sig-ai-velocity-outruns-prod-structures` | infra | The wave-1 aftermath from the ops side: bigger PRs, code shipped much faster, and now "maybe even from non-developers that don't actually know the code or what it's doing or the operating principles behind it"; "AI is creating a lot more issues in production as AI code goes through," and "it's not clear we have the right structures in place to deal with the amount of changes coming through." A cost-pressure rider: "unlimited tokens is coming to an end… they're starting to clamp down, prices are going up, companies are getting a lot more stringent" (see Review note 3) | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-background-agents` |
| `sig-background-agents-absorb-ops-long-tail` | infra | The dedicated-layer vendor positioning, complete: the operational work with no owner ("doesn't show up in your job description") becomes agent workload via four proven workloads — deployment monitoring, scheduled health/anomaly checks, operational reports/handoffs, first-responder to engineering questions — delivered as **always-on cloud agents in a sandbox with a filesystem, a task system, and a shared learning loop**, composable across schedules, event streams and Slack triggers. Includes the emergent trust mechanic: the first-responder agent watches critical channels passively, answers only above a confidence bar, and will **DM a human first** — "I think I know the answer to this, but I'm not sure. Can you confirm before I respond?" | — (**HELD PATTERN-LESS** — `pat-durable-execution` ledger, dedicated-layer side; see Review note 1) | `OnElement → el-background-agents`, `el-resolve`; `RelevantCompany → co-resolve-ai` |
| `sig-ops-agent-value-is-learned-context` | context | The moat claim, stated as an engineering necessity: models are "incredibly capable… especially over the last 6 months," but for an ops agent to succeed it "has to have an underlying learning system" that captures how *your* system operates — service interactions, hotspots, current causal chains — and keeps that understanding current as the system evolves faster and faster. Every company is unique, so the knowledge system is where Resolve says it spends its time; the same learned context is offered over MCP to augment customers' own agents. Execution loads the dashboard; the learned context says the metric smells off | `FormsPattern → pat-agent-memory-layer` (attached on coin 2026-08-14 — the clearest *vendor-moat* framing in the ledger; see Review note 2) | `OnElement → el-production-learning-system`, `el-production-context`; `RelevantCompany → co-resolve-ai` |
| `sig-agents-write-their-own-deploy-checks` | infra | Verification of production change is becoming **synthesized per change** rather than pre-authored: on each release the agent reads the diff, derives the telemetry that would validate it, builds a bespoke check plan (including causal-chain checks like the Kafka pipeline behind a currency-service swap), and sets its own watch window — an hour, or back in 3 days for slow-burn failure modes. This also extends verification to the changes CI/CD never sees (feature flags, infra changes), where today the only safety net is "trusting that an alert might fire and an on-caller will wake up and say 'who changed what?'". Explicitly positioned as patching, not replacing, the CI/CD pipeline — the alternative, "a single engineer just focused on watching all the things on every release", is "really expensive" cognitive load | `FormsPattern → pat-verification-gap` **[registry]** | `OnElement → el-adaptive-deployment-verification`; `RelevantCompany → co-resolve-ai` |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-execution-is-commodity-context-is-the-product` | The talk's task decomposition is a market claim: with capable models, the execution half of any ops task (load the dashboard, run the check, call the tool) is commodity — what's scarce is production context, the learned, environment-specific judgment that a metric "feels wrong" before anyone can articulate why. That is why an ops-agent vendor's differentiation lives in the learning/knowledge system rather than the model, why the models slide is one bullet and the learning slide gets the "we spend so much time here", and why the sellable interface (MCP) exports *learned context*, not model access. The ops-agent market is a context market | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-production-context`, `el-production-learning-system` |
| `ins-every-change-gets-a-bespoke-verifier` | Production verification is escaping the pipeline: instead of a fixed battery of checks authored in advance, each change event gets a verification plan generated *for it* — derived from the change's content, scoped to its causal chain, scheduled on its risk profile — and change paths that never had verification (feature flags, infra edits) get it for free. Combined with confidence-gated human confirmation (the agent DMs before it answers publicly), this inverts on-call: the always-on agent watches continuously and the human becomes the escalation path, rather than the pager being the system's first responder | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-adaptive-deployment-verification`, `el-background-agents` |

## KnowHow (1 new)

`SourcedFromArtifact → ia-aie-smith-always-on-agents`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-hand-ops-work-to-background-agents` | Start background agents on the four proven workloads | Begin with the workloads proven in production: (1) deployment monitoring — watch every change event, including the ones CI/CD doesn't pipe (feature flags, infra changes); (2) scheduled health/anomaly checks — routine morning sweeps, plus time-boxed worry-watches ("watch this third-party service for the next week, then stop"); (3) operational reports and handoffs — incident digests, capacity-vs-goal reports, weekly on-call handover prep summarizing the week's trends for the next rotation; (4) first-responder to engineering questions — passive channel watching with a confidence gate and human DM-confirmation before public answers. Pick the trigger per workload: schedule, weekly ceremony, event stream, or direct message. Set up conversationally — let the agent explore the environment and ask its clarifying questions; test before sharing with the team. Tune by replying in-thread ("too verbose — make it shorter" should persist into the task). Keep the surface where people already work (Slack/MS Teams), with a UI for inspecting tasks, runs and work traces. Graft into existing harnesses over MCP and bring existing skills rather than duplicating. Then get creative — the primitives compose well beyond the four | `ReferencesElement → el-background-agents`, `el-adaptive-deployment-verification`, `el-production-context`, `el-mcp` **[registry]** |

## Dropped

- **"Thank you, granola"** — the meeting-notes app surfacing mid-talk, plus the audience-poll answers (meeting review, market research, therapy) — color. The therapy exchange ("we are humans here today") is memorable but carries no industry claim.
- Splunk / VMware as Company nodes — speaker biography only (prose in the Expert brief).
- Kafka, GitHub tags, Grafana-class dashboards — tool mentions inside the demo narrative; prose.
- The demo environment walkthrough ("fake environment… demo Slack") — mechanics folded into `el-adaptive-deployment-verification` and `el-resolve`.
- On-call-is-a-nightmare texture (rotation every few weeks, unfamiliar changes/systems) — folded into the `el-background-agents` and signal briefs.
- "Enterprise stuff underneath" — unelaborated; dropped.
- A `sig-` for the token-price clampdown — one sentence; held as a rider inside `sig-ai-velocity-outruns-prod-structures` and Review note 3.

## Review notes

1. **⚑ `pat-durable-execution` ledger — this is the missing dedicated-layer counterpart to the convention counters (NOT coined, signal held pattern-less).** The candidate's live tension is stock-infrastructure-as-convention (HumanLayer b13, Netflix b14, FlyersSoft b15, and Temporal's own convention-shaped delivery b16) versus durable-runtime-as-product. Resolve is the cleanest **product-side** data point yet: always-running cloud agents, sandbox + filesystem state, a task system with schedules/triggers/resumable recurring work, sold as a platform — exactly the layer the counters say you can assemble from stock parts. Two details sharpen the ledger rather than just thickening it: (a) Resolve's durability is *bundled with a learned-context moat*, suggesting the durable-runtime layer may not be sellable alone — which would explain why the pure-runtime vendors (Temporal b16) deliver convention-shaped; (b) the MCP graft-in path means Resolve will happily be the durable layer under *your* harness — a layer, not an app. `sig-background-agents-absorb-ops-long-tail` is held pattern-less and ready to rehome on a coin/reject decision.
2. **`pat-agent-memory-layer` candidate — ~12th data point, vendor-moat flavor (held pattern-less).** After Rallabandi's purest statement (b15: "the memory is what the agent becomes") and the Netflix markdown-in-git counter (b14), Smith adds the commercial framing: the learning system is *the stated moat* of an ops-agent company, and it's what gets exported over MCP. Note it is also this candidate's second *ops-domain* point (first: Netflix — and they disagree: Netflix says markdown in git suffices, Resolve builds a proprietary learning system). Same either/or as the durable-execution ledger: refutation pair, or convention-vs-product ends of one curve.
3. **Provider-commoditization paired set — one-line third corroboration (prose only).** "Unlimited tokens is coming to an end, the token max… starting to clamp down. Prices are going up. Companies are getting a lot more stringent" — the demand-side half of Cline's b16 subsidize→lock-in→gouge arithmetic, from a bystander vendor with no routing product to sell. Too thin for a signal; logged here for the b16 paired-set decision. ⚠ "the token max" is ambiguous in captions — best reading is subscription max-tier clampdowns (the $200-sub arithmetic of b16), not a context-window claim.
4. **`pat-ai-native-org` soft add (prose only).** "Beginning to see [shipping] maybe even from non-developers that don't actually know the code" — the org-blur claim from the ops side, consistent with Yaron's 81%/one-third stats (b8). Carried inside `sig-ai-velocity-outruns-prod-structures`; no rehoming needed.
5. **`pat-adaptive-harness` adjacency (prose only).** `el-adaptive-deployment-verification` is a runtime-*generated* verification plan — harness output, not harness input — which rhymes with the adaptive-harness candidate (Chandegra b7, DSPy Flex b11, Uber b12). I did not hold a signal for it because the generated artifact here is a monitoring plan, not the agent's own scaffolding; flag only.
6. **Verification-gap framing check.** Two signals form `pat-verification-gap` from the *production* side (post-merge, runtime) rather than the corpus-dominant code-review side. That is a genuine extension of the pattern's surface — same thesis (generation industrialized, verification re-architected around it), new venue (deploys, feature flags, infra changes). If the reconciler tracks sub-scopes, tag these "runtime verification" with b12's Uber closed-loop material.
7. **Caption garbles.** "CI ICD pipeline" → CI/CD pipeline; "passing contact context off" → passing context off; "the token max" — see note 3; "half pause" (on the learning bullet) — kept as stated; "granola" is the product Granola (not a garble). Speaker name, company and title are clean throughout. No unresolved entity names in this transcript — the cleanest captions of this batch's three.
8. **Numbers to preserve exactly**: 70% (engineer time on ops, survey-attributed, survey unnamed); ~15+ years / ~10 years (speaker background); "last 6 months" (model-capability step-change window); watch windows quoted (15 minutes rejected as hard-coding; 1 hour; 3 days).
