# SPIKE extraction — "Serving 2 Million Models Without Melting: Scaling the Hugging Face Hub" (Arek Borucki, Hugging Face) — FOR REVIEW

Source transcript: `transcripts/borucki-huggingface-2m-models.txt` (auto-captions — quotes are paraphrases, not verbatim; moderate technical-term garbling, see Review note 4).
Video: https://youtu.be/lyL5QhgIOxc — AI Engineer World's Fair, published 2026-07-28.
`stagingTimestamp` for the artifact and all signals: 2026-07-28 (publish date).
Entities marked **[registry]** already exist — edges link to them, no new node.
Speaker is a machine-learning-platform and database engineer at Hugging Face; the talk is a first-party infrastructure walkthrough of the Hub's metadata, search, and scaling architecture. ⚠ The title says **2 million** models; the talk says **3 million** throughout (see Review note 1).

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-borucki-hub-scaling` | Serving 2 Million Models Without Melting: Scaling the Hugging Face Hub (Arek Borucki, Hugging Face — AI Engineer World's Fair) | youtube | https://youtu.be/lyL5QhgIOxc |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-arek-borucki`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-arek-borucki` | Arek Borucki (machine learning platform & database engineer, Hugging Face — owns the Hub's MongoDB/search infrastructure) | `AffiliatedWithCompany → co-hugging-face` **[registry]** |

⚠ Captions render the name as "Ar Borutki"; spelling follows the talk byline.

## Companies (0 new, 2 registry)

| slug | name | type | note |
|---|---|---|---|
| `co-hugging-face` **[registry]** | Hugging Face | developer | reused — first talk in the corpus given *from inside* Hugging Face's infrastructure team. Prior appearances were all as a distribution channel (`brick-google-tiny-lms-edge`, `abdin-mchardy-poolside-synthetic-data`, `clyburn-redhat-structuring-unstructured`) or via `exp-thom-wolf` (batch 11). Recommend widening the registry brief with the platform numbers in `sig-hf-hub-scale-2026` |
| `co-mongodb` **[registry]** | MongoDB | developer | reused — the Hub's metadata source of truth (Atlas, Atlas Search, replica sets, upcoming sharding). Batch-8 brief ("document DB + Atlas vector search") is corroborated and extends to full-text/Lucene search at this scale |

## Elements (7 new, 1 reused)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-hugging-face-hub` | Hugging Face Hub | product | infra | The model/dataset hosting platform itself, treated here as a distributed system rather than a community: **3M public models, 1M datasets, 50,000 organizations, 14M users**, with metadata in MongoDB Atlas and artifacts in cloud object storage, fronted by a Kubernetes-hosted Hub API. Growth is release-driven — "every big release like Llama or DeepSeek generates thousands of new models on top" — so the infrastructure problem is set by the derivative-model long tail, not by any single large model |
| `el-metadata-artifact-separation` | Metadata / artifact separation | concept | infra | The Hub's foundational split: the database stores *everything about* the models (users, repositories, model and dataset records, bucket/space information, configuration, billing, access control) and never the models themselves; weights, tokenizer files, model-card assets and config files live in cloud object storage (AWS S3). Consequence: metadata, binary storage and compute scale — and get optimized — independently of each other |
| `el-precomputed-search-tokens` | Insert-time search tokenization | concept | infra | Pay the tokenization cost on write, never on query: a model name (`meta-llama/Llama-3.1-8B`) is split into small tokens at insert time and stored as an array on the document, in a **denormalized read-only copy of the main repo collection** built purely for reads and listings. Query-side becomes an autocomplete lookup over the token array, sorted by a **trending score recomputed every 5 minutes** from the last ~7 days of downloads and likes |
| `el-atlas-search` | MongoDB Atlas Search (Lucene-backed full-text) | technology | infra | Full-text search delivered by a separate `mongot` process wrapping **Apache Lucene** alongside the MongoDB server (core MongoDB provides no full-text engine); transparent to the caller — the same unified query API, with a `$search` stage in an aggregation pipeline naming the Lucene index. Adopted at the Hub to replace regex matching over the token array, which "doesn't scale well" and produced the latency problems that appeared as the dataset grew |
| `el-replica-read-routing` | Replica-set read routing | concept | infra | Workload placement across a **7-node MongoDB replica set** as an explicit engineering discipline rather than a driver default: all writes and only strong-consistency reads on the primary; everything tolerating slight staleness on secondaries; heavy aggregation pipelines (large scans, sorts, groups, transforms) and **change streams** (cache invalidation, sync to Redshift, event-driven workloads) off the primary; and one **hidden analytics node** — invisible to the driver, still replicating from the primary, connected to directly for reporting and ad-hoc/experimental queries so they never touch production traffic. Stated rule: "primary should focus on what only primary can do" |
| `el-database-sharding` | Horizontal database sharding | concept | infra | The next rung once a single replica set stops being enough: cut the dataset into pieces and give each shard its own replica set (primary + secondaries), letting a balancer distribute data across shards — replication multiplied rather than replaced. Scales CPU, memory, storage, reads and writes together, at the cost of choosing a shard key ("not a trivial operation"). Named as in-flight future work at Hugging Face, not yet shipped |
| `el-two-layer-autoscaling` | Two-layer Kubernetes autoscaling | ops | infra | Pod layer plus node layer. Pod layer: horizontal pod autoscaler scaling the Hub deployment **from 10 to 500 pods** on CPU/memory thresholds — with a planned migration to **KEDA** (event-driven autoscaling) because HPA cannot see application-level signals like requests per second or event-loop utilization, and "a pod can have low CPU but a high request queue". Node layer: **CAST AI** adds cluster nodes when pods are pending because the scheduler has no capacity. Explicit goal is elasticity *without* overprovisioning — cost control as well as availability |
| `el-kubernetes` **[registry]** | Kubernetes | — | — | reused: the Hub API runs on it, and both autoscaling layers are Kubernetes constructs. The batch-6 brief framed Kubernetes as the substrate an *agent fleet* should stack onto; this is the same substrate carrying a 14M-user AI platform |

Element edges: all seven new elements `IdentifiedInArtifact → ia-aie-borucki-hub-scaling`; `el-kubernetes` **[registry]** likewise `IdentifiedInArtifact → ia-aie-borucki-hub-scaling`.
`el-hugging-face-hub` `DevelopedByCompany → co-hugging-face` **[registry]**; `el-atlas-search` `DevelopedByCompany → co-mongodb` **[registry]**.
`el-hugging-face-hub` `UsesElement → el-metadata-artifact-separation`, `UsesElement → el-atlas-search`, `UsesElement → el-replica-read-routing`, `UsesElement → el-two-layer-autoscaling`, `UsesElement → el-kubernetes` **[registry]**.
`el-atlas-search` `UsesElement → el-precomputed-search-tokens`.
`el-two-layer-autoscaling` `UsesElement → el-kubernetes` **[registry]**.
`el-database-sharding` `UsesElement → el-replica-read-routing` (each shard is itself a replica set — transcript-stated).
`el-hugging-face-hub` `ExemplifiesPattern → pat-sovereign-ai` **[seed]** (the open-weight supply side the pattern's thesis depends on).
`el-precomputed-search-tokens` `ExemplifiesPattern → pat-model-not-bottleneck` **[registry]** (see Review note 3).

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-borucki-hub-scaling`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | OnElement / RelevantCompany |
|---|---|---|---|---|
| `sig-hf-hub-scale-2026` | infra | First-party Hub numbers as of 2026-07: **14M users** ("growing very fast, especially in the last couple of months"), **3M public models**, **1M datasets**, **50,000 organizations**, and **more than 30% of the Fortune 500** using Hugging Face as part of their AI workflows. Growth framing: ~**20,000 models a few years ago → 3M today, ~150×**, driven by derivative uploads — "every big release like Llama or DeepSeek generates thousands of new models on top" — which is the load the infrastructure is designed around ⚠ talk title says 2M (Review note 1) | `FormsPattern → pat-sovereign-ai` **[seed]** | `OnElement → el-hugging-face-hub`; `RelevantCompany → co-hugging-face` **[registry]** |
| `sig-hf-dataset-growth-curve` | data-eng | Dated dataset-count series from the platform owner: **10K in 2022 → 100K in 2024 → 500K less than a year ago → 1M today (2026-07)** — i.e. the count doubled inside the last year after 5× the year before. Stated consequence: "all this data must be stored, indexed, and also must be searchable, and that's the hardest part" — the dataset curve, not the model curve, is what forced the search rewrite | `FormsPattern → pat-sovereign-ai` **[seed]** | `OnElement → el-hugging-face-hub`, `el-metadata-artifact-separation`; `RelevantCompany → co-hugging-face` **[registry]** |
| `sig-hf-search-regex-to-lucene` | infra | Search rearchitecture at the Hub, with the reasoning stated: at 20,000 models any query is fast even without an index and "no one would notice"; at 3M the same approach breaks. The old path — a MongoDB `find` with a **regex** operator over a search-token array, sorted by trending score — stopped scaling as the dataset grew and produced user-visible latency. The new path precomputes tokens at insert time into a **denormalized read-only collection** and queries **Atlas Search / Apache Lucene** autocomplete via a `$search` aggregation stage, still sorted by a trending score recomputed every 5 minutes over ~7 days of downloads and likes. Reported outcome: "we don't have any more latency issues in our search bar." The stated design target is **P99, explicitly over P50**, because at 14M users even 1% of traffic is **140,000 people** hitting a slow search — and the competing hub is one click away | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-atlas-search`, `el-precomputed-search-tokens`, `el-hugging-face-hub`; `RelevantCompany → co-mongodb` **[registry]**, `co-hugging-face` **[registry]** |
| `sig-hf-read-topology-and-sharding` | infra | How a 3M-model AI platform actually distributes database load: a **7-node replica set** with writes and strong-consistency reads pinned to the primary, stale-tolerant reads spread across secondaries, heavy aggregations and **change streams** (cache invalidation, sync to AWS Redshift, event-driven workloads) deliberately kept off the primary, and a **hidden analytics node** — invisible to the MongoDB driver, still tailing the oplog — reserved for reporting and ad-hoc heavy queries. Named as insufficient going forward: "with 14 million users, 3 million models and our growth, soon a single MongoDB replica set will not be enough", so **sharding is the next step**, with shard-key selection called out as the hard, unfinished decision | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `OnElement → el-replica-read-routing`, `el-database-sharding`; `RelevantCompany → co-mongodb` **[registry]**, `co-hugging-face` **[registry]** |
| `sig-hf-two-layer-autoscaling` | infra | Two independent elasticity layers keep the Hub up without overprovisioning: the Hub deployment scales **10 → 500 pods** on the Kubernetes horizontal pod autoscaler, and **CAST AI** adds nodes underneath when pods go pending because the scheduler has no capacity. Announced migration: HPA → **KEDA**, on the grounds that CPU/memory thresholds are the wrong signal for this workload — "a pod can have low CPU but a high request queue" — so scaling should be driven by requests per second and event-loop utilization instead of resource utilization | — (pattern-less; see Review note 3) | `OnElement → el-two-layer-autoscaling`, `el-kubernetes` **[registry]**; `RelevantCompany → co-hugging-face` **[registry]** |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-metadata-is-the-model-hub-workload` | The surprising fact about the world's largest model host is that its database holds **no models**. Hosting weights is an object-storage problem and effectively solved; making 3M of them findable, listable, billable and access-controlled is a metadata problem, and that is where the engineering lives. Separating the two lets metadata, binaries and compute be scaled and tuned independently — and it means the platform's growth pressure shows up first as query latency and read distribution, not as storage cost | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-metadata-artifact-separation`, `ReliesOnElement → el-hugging-face-hub` |
| `ins-p99-is-the-retention-surface` | At platform scale the tail *is* the product: 1% of 14M users is 140,000 people, so P99 matters more than P50, and a slow search bar simply sends developers elsewhere. The generalizable move is to shift work from query time to write time and from the primary to everywhere else — precompute tokens on insert, keep a denormalized read-only copy, refresh ranking on a 5-minute cadence, and route anything that can tolerate staleness away from the one node that cannot be replicated around. The user-facing goal is invisibility: "you never have to think about it… keeping the user experience simple no matter how complex it gets under the hood" | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-precomputed-search-tokens`, `ReliesOnElement → el-replica-read-routing`, `ReliesOnElement → el-atlas-search` |

## KnowHow (2 new)

All `SourcedFromArtifact → ia-aie-borucki-hub-scaling`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-scale-model-hub-metadata` | Scale an AI hub's metadata layer, not its blob layer | Keep model artifacts (weights, tokenizers, cards, configs) in object storage and put only *metadata* in the database, so the two scale independently. For search: maintain a denormalized read-only copy of the main collection purely for reads and listings; tokenize names at **insert** time into an array rather than matching at query time; move off regex `find` to a Lucene-backed full-text index (`$search` in an aggregation pipeline) before the dataset makes regex latency user-visible; precompute the ranking signal on a cadence (trending score every 5 minutes over ~7 days of downloads and likes) instead of computing it per query. Optimize for P99, not P50 — at 14M users, 1% is 140,000 people. For read scaling: pin only writes and strong-consistency reads to the primary and audit that list continuously; push stale-tolerant reads, heavy aggregations and change streams to secondaries; keep a hidden analytics member for reporting and ad-hoc queries so they never touch production traffic; and plan sharding (with a deliberately chosen shard key) as the next rung when a single replica set runs out | `ReferencesElement → el-metadata-artifact-separation`, `ReferencesElement → el-precomputed-search-tokens`, `ReferencesElement → el-atlas-search`, `ReferencesElement → el-replica-read-routing`, `ReferencesElement → el-database-sharding` |
| `how-two-layer-autoscaling` | Autoscale at both the pod and the node layer, on the right signal | Run two layers, because the pod autoscaler is useless when the cluster has no free nodes: deployment-level autoscaling (the Hub runs 10–500 pods) plus infrastructure-level node autoscaling (CAST AI) that provisions capacity when pods go pending. Treat this as a cost control as much as an availability control — the point is handling spikes *without* permanent overprovisioning. Scale on application signals, not resource utilization: CPU and memory miss the case of a pod with low CPU and a deep request queue, so move from HPA to event-driven autoscaling (KEDA) driven by requests per second or event-loop utilization | `ReferencesElement → el-two-layer-autoscaling`, `ReferencesElement → el-kubernetes` **[registry]** |

## Dropped

- **AWS** as a company node — S3 (artifact storage) and Redshift (change-stream sync target) are named as substrates with no claim made about the vendor; carried in `el-metadata-artifact-separation` and `el-replica-read-routing` briefs. `co-aws` **[registry]** exists if you later want the edges.
- **CAST AI and KEDA as separate element nodes** — folded into `el-two-layer-autoscaling`, which is the mechanism the talk actually argues for; split out only if a later talk makes a vendor claim about either.
- **Apache Lucene / `mongot` as their own nodes** — the implementation detail *inside* `el-atlas-search`; kept in that brief because the talk makes a point of it ("core MongoDB doesn't provide full-text search").
- **Llama / DeepSeek** — named only as the release events that generate derivative-upload spikes; no model claim, no `el-*` node, no `co-deepseek` **[registry]** edge.
- **The `meta-llama/Llama-3.1-8B` document walkthrough and the two example results** (trending scores 33 and 14) — slide illustration of the token array; folded into `el-precomputed-search-tokens`.
- **"Hundreds of different services at Hugging Face using MongoDB to handle millions of queries"** — a scale aside with no further detail; folded into `sig-hf-read-topology-and-sharding`.

## Review notes

1. **Title/transcript mismatch: 2M vs 3M models.** The published title says "2 Million Models"; the speaker says three million at least six times, including the framing line ("we ended up serving 3 million models"). All extracted numbers use the transcript figure. Most likely the title was written for an earlier version of the deck — which, given the growth curve in `sig-hf-dataset-growth-curve`, is itself a data point. If you prefer the conservative number at seeding, edit `sig-hf-hub-scale-2026` and `el-hugging-face-hub` together and keep the flag.
2. **Registry element sweep — the assigned reuse list did not fit, deliberately.** Checked and *not* edged: `el-prompt-caching`, `el-prefix-caching`, `el-model-routing`, `el-small-language-models`, `el-dynamic-quantization`, `el-litert-lm`, `el-cost-per-capability-per-second`, `el-notion-workers`, `el-token-factory`, `el-opentelemetry`, `el-agentic-control-plane`. This talk contains **no inference, no serving, no model-level and no agent content** — "serving models" here means serving model *files and metadata*, not tokens. The one genuine reuse is `el-kubernetes` **[registry]**; `co-mongodb` **[registry]** and `co-hugging-face` **[registry]** carry the rest. Recorded so the absence reads as a decision rather than an oversight. `el-hybrid-search` **[registry]** (batch 8, MongoDB talk) was considered for the search material and rejected: that element is vector+lexical retrieval for RAG, this is lexical autocomplete over a token array — deliberately no link, same trap flagged in `clyburn-redhat-structuring-unstructured.md` note 3.
3. **Pattern homes are the weakest part of this extraction — read before accepting.** This is a pure database/infra talk with no thesis about AI itself, so both homes are interpretive: (a) `pat-sovereign-ai` **[seed]** on the two growth signals reads the 3M-public-model / 1M-dataset / 30%-of-Fortune-500 supply side as the substrate the own-your-stack thesis depends on — it is *enabling evidence*, not an argument for sovereignty, and it does partially offset the pattern's two existing counter-edges (Yaron survey, Brick DRAM economics); (b) `pat-model-not-bottleneck` **[registry]** on the search and read-topology signals reads the periphery claim literally — the thing that decides whether 3M models are usable is an index and a read-routing policy. **Drop-option:** leave all five signals pattern-less and keep the file as a pure element/knowhow contribution; nothing else in the file depends on these edges. `sig-hf-two-layer-autoscaling` is already left pattern-less on exactly that logic (standard Kubernetes ops, no thesis), which is the honest baseline the other four should be judged against.
4. **Caption garbles — resolved.** "Ar Borutki" → Arek Borucki; "premputee tokens" → precomputed tokens; "Apache lucine / Lucin" → Apache Lucene; "MongoDBT process / MongoD" → `mongot`; "reax operator" → regex operator; "cast AI / Cast AAI" → CAST AI; "keta / KDA" → KEDA; "horizontal pot autoscaler", "pots", "ports", "bots", "spots" → pods; "note autoscaling" → node autoscaling; "DHA is running on Kubernetes" → "the Hub is running on Kubernetes"; "source of true" → source of truth; "short key" → shard key; "AWSS3" → AWS S3; "Red Shift" → Redshift; "meta minus llama/lama 3.18b" → `meta-llama/Llama-3.1-8B`; "cart assets" → model-card assets; "scaling medium models" (closing line) → scaling millions of models. **Unresolved:** the trending-score window is hedged in the transcript itself ("as far as I remember from the last seven days") — kept with the hedge. The 7-node cluster count is stated once and not repeated.
5. **No uncoined pattern candidate advanced.** Nothing here touches `pat-environments-economy`, `pat-benchmark-trust-crisis`, `pat-agent-memory-layer`, `pat-durable-execution`, `pat-adaptive-harness`, `pat-ai-native-org` or `pat-agent-economy`. This is the rare corpus talk with zero agent content of any kind — worth noting as a baseline: the AI ecosystem's largest distribution point is run with 2015-vintage distributed-systems technique, and the speaker never once suggests an agent should be involved in any of it.
6. **`co-hugging-face` brief widening.** The registry entry was coined in batch 9 from outside references. This is the first insider account, and the platform numbers (14M users, 3M models, 1M datasets, 50K orgs, >30% of the Fortune 500) belong in the company brief as well as in `el-hugging-face-hub`. Same for `exp-thom-wolf` **[registry]** — no edge added here (Wolf is never mentioned), but the two experts now sit under one company from opposite ends of the org.
7. **`el-database-sharding` is deliberately generic.** It is a textbook technique, coined only because the talk devotes a full section to it as *unfinished work at Hugging Face* and because the shard-key decision is flagged as the hard part. If the registry prefers AI-native elements only (the standing question raised in `daga-tesla-enterprise-agents-structure.md` note 3 about `el-semantic-layer`), drop it and fold the material into `el-replica-read-routing`; `sig-hf-read-topology-and-sharding` survives either way.
