# SPIKE extraction — "Building Closed-Loop Evals for a Multimodal Agent at Scale" (Soumya Gupta & Jai Chopra, Uber) — FOR REVIEW

Source transcript: `transcripts/gupta-chopra-closed-loop-multimodal-evals.txt` (auto-captions — quotes are paraphrases, not verbatim).
Video: https://youtu.be/31GUkCBD-Uc — AI Engineer World's Fair, published 2026-07-24.
`stagingTimestamp` for the artifact and all signals: 2026-07-24 (publish date).
Entities marked **[registry]** / **[batch1]** already exist — edges link to them, no new node.
Entities marked **[this batch]** are defined in a sibling file of the same evals-track batch.
Two presenters, both on Uber's computer vision team. **Company resolution:** the captions garble Uber as "Aruba" and "Rue Ba"; resolved to Uber from "our delivery marketplace Uber Eats", "at Uber", and the video byline (see Review notes).

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-gupta-chopra-closed-loop-evals` | Building Closed-Loop Evals for a Multimodal Agent at Scale (Soumya Gupta & Jai Chopra, Uber — AI Engineer World's Fair) | youtube | https://youtu.be/31GUkCBD-Uc |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-soumya-gupta`; `ContributedByExpert → exp-jai-chopra`.

## Experts (2 new)

| slug | name | edges |
|---|---|---|
| `exp-soumya-gupta` | Soumya Gupta (Uber computer vision team; covers golden-set alignment, drift, and the auto-tuning loop) | `AffiliatedWithCompany → co-uber` **[registry]** |
| `exp-jai-chopra` | Jai Chopra (Uber computer vision team; covers the marketplace use case, orchestration, router evals, pairwise QA and the feedback loops) | `AffiliatedWithCompany → co-uber` **[registry]** |

⚠ `exp-soumya-gupta` is a **different person** from `exp-sachin-gupta` (batch 3/5) and `exp-nishant-gupta` (batch 8, co-meta) — three unrelated Guptas in the corpus.

## Companies (0 new, 1 registry)

| slug | name | type | note |
|---|---|---|---|
| `co-uber` **[registry]** | Uber | bigtech | reused. The registry brief frames Uber as an AI-*consuming* enterprise (the CTO token-budget story in `maruthavanan-kalmantic-cognitive-infrastructure.md`); this talk shows Uber as a *builder* of production multimodal agents. Recommend widening the brief at seeding rather than adding a node |

## Elements (6 new, 5 reused)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-closed-loop-auto-tuning` | Closed-loop agent auto-tuning | concept | harness | A production loop that re-tunes the agents themselves with no human in the loop: sample production data on a cadence → send it to human labelers under the same guidelines used for the golden set → diff agent output against labels → route mismatches to a diagnoser that localizes the failing component → a prompt-optimizer agent (a *reflect* sub-agent that denoises the mismatches and extracts systemic issues, feeding a *synthesize* sub-agent that rewrites the target agent's config) → re-benchmark against the golden dataset → on pass, register the new agent version in an agent store that the next production run picks up. Entirely config-driven, with guardrail observability and one-step rollback around the loop |
| `el-diagnoser-agent` | Diagnoser agent | concept | harness | The abstraction layer above the tuning loop: a single agent that accepts input from *any* feedback channel (human-label mismatches, internal dogfooding thumbs up/down and free-form comments, merchant and design-team feedback, production metrics), reflects on which agent inside the orchestration needs to change — one or several — and routes the fix to that component's config. Lets new feedback loops be added without building a new tuning path each time |
| `el-multi-dimensional-qa-gate` | Multi-dimensional QA gate | concept | harness | An independent QA agent scoring generated media on several named dimensions at once — faithfulness to the original, completeness, plating, colour, naturalness, realism — and emitting yes / no / **unsure**, where unsure rejects in production. Rejections return to the prompt-generation step with the original inputs, so the editor retries with the QA feedback in context |
| `el-pass-at-k` | pass@K (for gated iterative generation) | concept | harness | The metric for a generate → gate → retry loop: the pass rate at the Kth iteration. It should rise with K, because each iteration carries the QA gate's feedback back into the prompt. The two terminal outcomes are "passed at some K and published" or "took a coverage hit and never published"; K therefore trades compute against coverage rather than against quality |
| `el-swiss-cheese-gating` | Swiss-cheese gating | concept | harness | Deliberately layered, deliberately overlapping quality gates so no single hole passes a failure into production: an in-loop QA agent plus a final, more holistic publish-ready gate doing policy and quality checks. Redundancy between layers is accepted as the design, and a late gate catching something an earlier gate should have caught is read as a signal to tune upstream, not as wasted work |
| `el-flat-trace-log` | Flat end-to-end orchestration log | concept | harness | One flat JSON record covering every agent in an end-to-end run, rather than nested per-agent traces. Chosen for legibility: non-technical and technical staff alike can open a single case to diagnose it and roll the same field up in aggregate. Framed as the prerequisite step — "if you don't start with logging you have nothing to optimize for, let alone set up a self-learning loop" |
| `el-judge-as-classifier` **[registry]** | LLM judge as classifier | — | — | reused and extended: the routing agent is evaluated exactly this way — confusion matrix, precision/recall against human labels, with **recall as the declared guardrail metric** (no bad image may slip through). Extension worth noting on the node: when the router picks among more than two branches (e.g. routing to a smaller lower-latency model to save cost), the 2×2 becomes an n×n matrix scoring correct routing per branch |
| `el-golden-dataset` **[registry]** | Golden dataset | — | — | reused: human labels are the declared source of truth; the set is collected representatively (geography, dish type, image-quality tier) and labeled against objective guidelines to strip subjective bias. It is both the first-version alignment target and the standing ship gate — every auto-tuned agent must re-pass it before registration |
| `el-generator-validator-separation` **[batch1]** | Generator/validator separation | — | — | reused: the image-editing agent and the QA agent are separate agents, and the QA verdict — not the editor's own judgment — decides retry-or-publish |
| `el-pairwise-preference-eval` **[this batch]** | Pairwise preference evaluation | — | — | reused (defined in `bril-characterai-evaling-video-slop.md`): "is it better?" is scored as a pairwise comparison of input image vs output image, not an absolute quality score, with yes/no/unsure output |
| `el-active-learning` **[registry]** | Active learning (for agent QC) | — | — | considered and **not** edged: Uber samples production at a regular cadence, not by disagreement/uncertainty selection, which is the registry element's defining mechanism. Decision recorded so it isn't re-litigated |

Element edges: all six new elements `IdentifiedInArtifact → ia-aie-gupta-chopra-closed-loop-evals`; the four reused-and-edged elements (`el-judge-as-classifier`, `el-golden-dataset`, `el-generator-validator-separation`, `el-pairwise-preference-eval`) likewise `IdentifiedInArtifact → ia-aie-gupta-chopra-closed-loop-evals`.
`el-closed-loop-auto-tuning` `DevelopedByCompany → co-uber`; `UsesElement → el-diagnoser-agent`, `UsesElement → el-golden-dataset`, `UsesElement → el-flat-trace-log`; `EnablesPattern → pat-harness-over-model` **[registry]**.
`el-diagnoser-agent` `DevelopedByCompany → co-uber`.
`el-multi-dimensional-qa-gate` `UsesElement → el-pairwise-preference-eval`, `UsesElement → el-generator-validator-separation`; `EnablesPattern → pat-verification-gap` **[registry]**.
`el-pass-at-k` `UsesElement → el-multi-dimensional-qa-gate`.
`el-swiss-cheese-gating` `UsesElement → el-multi-dimensional-qa-gate`; `EnablesPattern → pat-verification-gap` **[registry]**.

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-gupta-chopra-closed-loop-evals`, `SourcedFromSource → source-aie-yt` **[registry]**, `RelevantCompany → co-uber` **[registry]**.

| slug | name / brief | domain | FormsPattern | OnElement |
|---|---|---|---|---|
| `sig-uber-eats-agentic-photo-enhancement` | Uber Eats runs a multi-agent image pipeline in production against a ~$90B run-rate marketplace adding millions of items a month across 10,000 cities: an understanding-and-routing agent describes each merchant photo into structured output and decides enhance-or-skip; an editing agent loops against a QA agent; a final publish-ready gate clears policy and quality before the image reaches the menu. The binding constraint is trust, not beauty — a meaningful fraction of consumers distrust anything visibly AI-generated, so faithfulness to the real dish, merchant brand preservation and marketplace-wide diversity (one prompt for every photo would collapse it) outrank aesthetic lift | harness | `FormsPattern → pat-verification-gap` | `OnElement → el-multi-dimensional-qa-gate`, `el-swiss-cheese-gating` |
| `sig-closed-loop-agent-self-tuning-prod` | Uber's agents re-tune themselves in production without a human in the loop: cadence-sampled production data goes to labelers under the golden-set guidelines, mismatches go to a diagnoser that localizes the failing component, a reflect/synthesize prompt-optimizer rewrites that agent's config, the rewrite is benchmarked against the golden set, and on pass it is registered in an agent store the next production run picks up. Stated rationale: an offline-tuned static model provably will not hold in the real system, so "every component in our system is able to tune itself for any drift online" — with observability on guardrails and quick rollback as the safety net | harness | `FormsPattern → pat-harness-over-model` | `OnElement → el-closed-loop-auto-tuning`, `el-diagnoser-agent`, `el-golden-dataset` |
| `sig-routing-errors-asymmetric-cost` | Router failures cost differently in each direction, so the guardrail metric is recall, not accuracy: a false-positive enhance on an already-good cheeseburger burns compute for zero quality lift *and* risks degrading a good image; a false-negative approval is worse — a photo showing six chicken wings under a dish name reading "eight pieces chicken wings" passed the router, and sending it downstream risks the editing model hallucinating two extra wings to match the description. That specific failure is the cut Uber's faithfulness metric is designed to catch | harness | `FormsPattern → pat-verification-gap` | `OnElement → el-judge-as-classifier` |
| `sig-qa-gate-induces-reward-hacking` | A QA gate that rejects a creative edit pushes the editing agent to oversteer on the next iteration: rejected once, it fell back to a generic ceramic bowl that clears the gate while making a nugatory change — raw pixels very different from the input, product value approximately zero. Named on stage as reward hacking, observed inside a production gated loop rather than in a benchmark | harness | `FormsPattern → pat-verification-gap` | `OnElement → el-multi-dimensional-qa-gate`, `el-pass-at-k` |
| `sig-frontier-model-flaws-leak-into-applied` | Failure modes of the frontier image-editing models Uber calls surface as defects in the applied product — object coherence and physics plausibility errors such as a plate rendered covering the sauce. Uber's own evals are what detect them, and the team coordinates with the frontier model teams to feed the problems back upstream: an applied eval suite acting as a defect channel into the labs | inference | `FormsPattern → pat-verification-gap`, `FormsPattern → pat-model-not-bottleneck` | `OnElement → el-multi-dimensional-qa-gate` |

## Insights (3 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-log-flat-before-you-optimize` | Logging is the precondition of every loop above it, and its *shape* decides who can use it: one flat end-to-end record per run — rather than nested per-agent traces — lets engineers, PMs and non-technical staff all drill into a single case and roll the same fields up in aggregate. Start there, because without it there is nothing to optimize for, let alone a self-learning loop to build | `HighlightsPattern → pat-harness-over-model` **[registry]** | `ReliesOnElement → el-flat-trace-log` |
| `ins-static-alignment-decays` | Aligning a model to a human-labeled golden set only buys the first version; the long tail keeps producing failures a static model cannot absorb. The shippable unit is therefore not a tuned agent but the loop that detects mismatch against fresh human labels, localizes which component caused it, rewrites that component's config, and re-benchmarks — the golden set becomes a standing gate rather than a launch milestone | `HighlightsPattern → pat-harness-over-model` **[registry]** | `ReliesOnElement → el-closed-loop-auto-tuning`, `ReliesOnElement → el-golden-dataset` |
| `ins-redundant-gates-beat-perfect-gates` | For generated media reaching real users, layered overlapping gates beat one well-designed gate: a Swiss-cheese arrangement optimizes for the probability that no failure gets all the way through, so redundancy between an in-loop QA agent and a final publish-ready check is the design, not waste. A later gate catching what an earlier one should have caught is a tuning signal for upstream — and rejecting on "unsure", taking a coverage hit rather than publishing, is the correct default when authenticity is the product constraint | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-swiss-cheese-gating`, `ReliesOnElement → el-multi-dimensional-qa-gate` |

## KnowHow (3 new)

All `SourcedFromArtifact → ia-aie-gupta-chopra-closed-loop-evals`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-closed-loop-agent-tuning` | Build the tuning loop, not just the agent | Log a flat end-to-end record from day one; declare one guardrail metric per component (recall for a router that must not pass bad inputs) and never ship below it; sample production on a fixed cadence to the same labelers under the same guidelines used for the golden set; diff agent output against those labels; route mismatches into a diagnoser that localizes the failing component rather than re-tuning everything; split the optimizer into reflect (denoise the mismatches, extract systemic issues) and synthesize (rewrite the config from that feedback); gate every rewrite on a golden-set re-benchmark; version agents in a store so production picks up the new config and rollback is one step; keep observability on the guardrails of the loop itself | `ReferencesElement → el-closed-loop-auto-tuning`, `ReferencesElement → el-diagnoser-agent`, `ReferencesElement → el-flat-trace-log` |
| `how-multimodal-generation-gates` | Gate generated media on named dimensions and iterate against the gate | Have the model describe the input and emit structured output before any routing decision; grade that structured output against explicit pass/fail rubric criteria; evaluate the router as a classifier — confusion matrix, precision/recall, n×n when there are more than two branches; generate a per-image prompt from the description plus the router's directives rather than one prompt for everything (a shared prompt collapses catalogue diversity); score the edit pairwise, input vs output, with yes/no/**unsure**, and let unsure reject; run the editor against an independent multi-dimensional QA agent for up to K iterations and track pass@K; add a final publish-ready policy-and-quality gate even though it overlaps the in-loop one; prefer a coverage hit over a bad publish | `ReferencesElement → el-multi-dimensional-qa-gate`, `ReferencesElement → el-pass-at-k`, `ReferencesElement → el-judge-as-classifier`, `ReferencesElement → el-swiss-cheese-gating` |
| `how-objective-human-labeling` | Make human labels a reusable source of truth | Collect the labeling set representatively across the cuts that matter (geography, item type, quality tier, user-generated vs merchant content); give labelers objective, example-backed guidelines whose purpose is explicitly to remove subjective bias and rater noise; use the *identical* guidelines for the initial golden set and for every later production sample, so offline and online labels stay comparable and mismatch means drift rather than instrument change; align the definition of "better" with product, design, policy and legal before it goes into the eval; hold the golden set as the ship gate for anything the loop retunes | `ReferencesElement → el-golden-dataset`, `ReferencesElement → el-pairwise-preference-eval` |

## Dropped

- Marketplace business metrics (conversion, add-to-cart, order completion; slicing by geo / device / dish type and tuning per segment) — real and interesting, but they are the outcome layer above the eval system; kept as prose, no Element.
- Internal dogfooding UI (thumbs up/down plus free-form feedback from merchants, design and product teams) — an input channel to `el-diagnoser-agent`, folded into that element's brief rather than coined separately.
- Merchant pain themes (lack of time, lack of know-how, cost of professional photo shoots; menus changing over time) — motivation, folded into `sig-uber-eats-agentic-photo-enhancement`.
- The deterministic-vs-agentic spectrum framing (rules-based is controllable but brittle; unconstrained agency is capable but unsafe; pick the balance) — a good statement of an existing corpus thesis, but it restates `el-deterministic-agentic-split` **[batch2]** without adding to it; no edge, recorded here.
- The definition of "a better image" — explicitly withheld on stage as proprietary. The dimensions named (faithful, complete, natural, realistic) are in `el-multi-dimensional-qa-gate`; the rubric internals are simply not in the transcript.

## Review notes

1. **Company resolution (high confidence).** Captions render Uber as "Aruba" ("the computer vision team at Aruba") and "Rue Ba" ("at least for us at Rue Ba"). Resolved via "our delivery marketplace Uber Eats", "at Uber", "Uber Eats app" and the video byline (*Soumya Gupta & Jai Chopra from Uber*). No new company node — `co-uber` reused, with the brief-widening note above.
2. **Speaker attribution.** Jai Chopra opens ("My name is Jay and I'm here with Sonya"), covering the use case, orchestration, router eval, pairwise comparison, failure modes and the feedback-loop summary; Soumya Gupta covers golden-set alignment, drift, and the auto-tuning loop, handing back for the enhancement section. Captions render her name "Sonya"/"Somya"; the byline spells **Soumya**. Neither speaker's title/role is stated — briefs say "computer vision team" only, per transcript.
3. **`pat-adaptive-harness` / `pat-adaptive-software` (uncoined paired candidate) — this is the strongest vendor data point yet, and no edge was made.** A production system where an optimizer agent rewrites the configs of the production agents from live human-label signal, benchmarks the rewrite, and versions it into a store the next run picks up — with no human in the loop — is the candidate's thesis stated operationally. Ranks with RELAI (batch 8), DSPy Flex (batch 11), AutoAgent (batch 9) and Agent RX (batch 9). `sig-closed-loop-agent-self-tuning-prod` is parked on `pat-harness-over-model` and is the signal to rehome if you coin.
4. **`pat-benchmark-trust-crisis` (uncoined candidate) — evidence added, no edge.** `sig-qa-gate-induces-reward-hacking` is production-observed reward hacking against an LLM quality gate (oversteer to a generic ceramic bowl that clears QA while delivering nothing). Complements the benchmark-side evidence from Desai/SWE-Marathon (batch 7) and Han (batch 3) with an applied-product instance.
5. **`el-judge-as-classifier` extension.** The n×n confusion-matrix generalization for multi-branch routers is new material on an existing node — worth folding into the registry element's brief at reconciliation rather than coining a sibling element.
6. **Cross-batch pairing.** `el-pairwise-preference-eval` arrives independently here and in `bril-characterai-evaling-video-slop.md` (Character.AI) in the same batch — two production multimodal teams concluding that absolute scores don't survive contact with raters. Defined once, in the Bril file.
7. **`co-uber` type.** Kept as the registry's `bigtech`; if you re-type it, note this talk supports `developer` as much as the Lyft precedent did.
