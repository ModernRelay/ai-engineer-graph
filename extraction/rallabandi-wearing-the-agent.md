# SPIKE extraction — "Wearing the Agent: From Group Chats to Glasses" (Sai Krishna Rallabandi) — FOR REVIEW

Source transcript: `transcripts/rallabandi-wearing-the-agent.txt` (auto-captions — quotes are paraphrases, not verbatim; two proper names and all four cited papers are caption-garbled, see review notes 2–3).
Video: https://youtu.be/s67bE2Ur3bY — AI Engineer World's Fair, final session of the final day, published 2026-07-31.
`stagingTimestamp` for the artifact and all dated nodes (signals, knowhows): 2026-07-31 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.
**No company is resolvable from this talk** — see review note 1; the Expert carries no `AffiliatedWithCompany` edge and no signal carries `RelevantCompany`.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-rallabandi-wearing-the-agent` | Wearing the Agent: From Group Chats to Glasses (Sai Krishna Rallabandi — AI Engineer World's Fair) | youtube | https://youtu.be/s67bE2Ur3bY |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-sai-krishna-rallabandi`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-sai-krishna-rallabandi` | Sai Krishna Rallabandi (builder of Judith, a multi-user agent deployed in group chats and on glasses for 8 months among friends and family; works on agent security layers, group memory, and on-device/small-model guards; academic background implied — refers to "one of my advisers") | — (no affiliation stated in the talk; see review note 1) |

## Companies (0 new)

None. Nothing in the talk names an employer, a startup, or a product vendor. Kept in prose only: the papers cited (see review note 3).

## Elements (8 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-judith-agent` | Judith (multi-user always-on agent) | product | harness | Personally built agent deployed for **8 months** in a real group setting — friends, family, and a conference attendee group — across group chat and glasses. Behaviours shown from production: answering a venue question by **DM instead of in the group** because the answer was privacy-sensitive; syncing multiple calendars to find a shared slot; noticing sleep-deprivation and adjusting; speaking to the wearer through the **glasses rather than the car's speakers** while driving so passengers wouldn't hear; curating a months-long evolving group conversation down to what stays relevant; and tutoring the speaker's three-year-old (capitals, numbers) while reporting progress to the parents and re-testing what she'd learned |
| `el-jatayu-guard` | Jatayu (action-surface guard) | technology | security | The agent's security layer (⚠ captioned "jetou"; reconstructed as the guardian figure from Indian mythology, review note 2). Premise: an agentic system cannot be secured the way an LLM is — an LLM's output can be inspected, but an agentic system *acts*, so its exposed surface is vastly larger (every web page crawled, every group message and pasted screenshot, every GitHub issue, every promotional email) and larger still in a group. Design consequence: **do not gate the input** — let the agent read everything and place a deterministic guard at the **action** boundary (reading bash or secret/config variables, exporting data), fast enough to add no latency, classifying actions into three buckets: allow, require approval, block |
| `el-skill-collision-attack` | Skill-collision attack | concept | security | Attack class where two agent skills that are individually benign — and whose static scans are "pristine" — become malignant **in composition at runtime**: an OCR skill extracts attacker-planted content from a page, and a reporting skill then exfiltrates the user's PII along with the report to a third party. The skills themselves are uncompromised; the attacker owns only the content the agent reads. Reported at ~**90% of observed attacks**, with the accompanying finding that static-scan-surviving code can break at runtime. Punchline: "we can't read our way — or model-check our way — to safety" |
| `el-data-instruction-channel-separation` | Data/instruction channel separation | technology | security | Learned second layer above the deterministic gate: a **LoRA-fine-tuned SLM**, supervised on the guard's own traffic, that separates the data channel from the instruction channel and forwards only demonstrably benign content — so that when the instruction is "summarize this page" and the crawled page carries an injection, the injected text is obscured and the task still completes. Motivated by a concrete evasion the deterministic layer cannot catch: text interspersed with dots (`L.i.k.e`) defeats regex and static approaches while a learned model catches it. Benchmarked against an agent-security eval where the naive baseline sits at ~50%, "a coin flip", and the learned model improves on it |
| `el-atomic-fact-memory` | Atomic-fact memory | concept | context | Group-chat memory strategy: instead of storing the whole transcript and compacting it once it becomes overhead, **extract atomic, high-value facts** as the conversation accumulates (a plan, a date, an added constraint) and store those. Requires purpose-built auto-raters, on five axes: did we extract high-value atomic facts; are they the relevant ones for this conversation; are they hierarchically related; is their **temporal** weighting right (some facts gain importance over time, some should be dropped); and is retrieval returning the right set — ideally intelligent, graph-organized retrieval rather than crawling everything |
| `el-continual-relevance-scorer` | Continual relevance scorer | concept | context | Forgetting mechanism for bloated memory: train a small ML model or SLM that scores stored context for importance **continually** rather than once, because in a group setting the context keeps evolving and different things matter at different times. The payoff is knowledge-based compaction — real token savings from dropping what has stopped mattering, instead of summarizing everything uniformly |
| `el-per-user-lora-permissions` | Per-user LoRA permissions | technology | security | Privacy architecture for a shared agent: one **common shared memory layer** with a separately trained **LoRA adapter per user** on top, so each person's permissions are baked into the weights that serve them — "baking the permissions not by using code but by using machine learning itself" — rather than enforced by an access-control layer over a single store. Explicitly analogized to how the human brain holds shared and private context |
| `el-recipient-routing` | Recipient routing | concept | harness | The third harness concern for multi-user agents, and explicitly **not** model routing to save tokens: routing the finished information to the right person, channel and moment — DM rather than the group when the answer is sensitive, glasses rather than the car audio when passengers are present, silence when nobody asked. Includes the observed failure mode (group-deployed agents are "over-articulative when they are not asked questions") and the proposed fix: a small learned classifier — "as simple as a RoBERTa classifier" — for role and speaking permission, in progress |

Element edges: all eight `IdentifiedInArtifact → ia-aie-rallabandi-wearing-the-agent`; `el-judith-agent` `UsesElement → el-jatayu-guard`, `UsesElement → el-atomic-fact-memory`, `UsesElement → el-recipient-routing`, `UsesElement → el-per-user-lora-permissions`, `ExemplifiesPattern → pat-harness-over-model` **[registry]**; `el-jatayu-guard` `UsesElement → el-constrain-effects-not-expression` **[registry]**, `UsesElement → el-deterministic-agentic-split` **[registry]**, `EnablesElement → el-data-instruction-channel-separation`; `el-data-instruction-channel-separation` `UsesElement → el-small-language-models` **[registry]**; `el-skill-collision-attack` `UsesElement → el-agent-skills` **[registry]**, `ExemplifiesPattern → pat-agent-supply-chain` **[registry]**, `ExemplifiesPattern → pat-new-cyber-threats` **[registry]**; `el-atomic-fact-memory` `UsesElement → el-semantic-episodic-memory` **[registry]**, `EnablesPattern → pat-context-graphs` **[registry]**; `el-continual-relevance-scorer` `EnablesElement → el-atomic-fact-memory`; `el-per-user-lora-permissions` `UsesElement → el-permissioned-inference` **[registry]**; `el-recipient-routing` `UsesElement → el-multiparty-turn-taking` **[registry]**.

Reused elements (no new nodes): `el-constrain-effects-not-expression`, `el-deterministic-agentic-split`, `el-agent-skills`, `el-multiparty-turn-taking`, `el-small-language-models`, `el-context-compaction`, `el-semantic-episodic-memory`, `el-permissioned-inference`, `el-prefix-caching` **[registry]**.

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-rallabandi-wearing-the-agent`, `SourcedFromSource → source-aie-yt` **[registry]**. No `RelevantCompany` on any signal — no company is identifiable (review note 1).

| slug | domain | name / brief | FormsPattern | OnElement |
|---|---|---|---|---|
| `sig-agents-move-from-one-user-to-groups` | harness | Framing claim from 8 months of production experience: "almost every agent we build today has a customer of size one" — including enterprise coding assistants — and the next one won't. The next agent serves **a group**, is **wearable**, and is **on all day**, and group deployment is qualitatively different, not a scaling factor: it adds three harness concerns the single-user case never had (a guard, group memory, and routing of the answer to the right person). The observation that the field has "won" at building single-user agents — "we can build an agent in an afternoon and it does useful things by evening" — is what makes the question shift | `FormsPattern → pat-harness-over-model` **[registry]** | `el-judith-agent`, `el-recipient-routing` |
| `sig-skill-collision-runtime-attacks` | security | Two cited papers: a **runtime skill audit** finding that static-scan-surviving skill code can break at runtime, and a "when safe skills collide" finding that two individually benign skills become malignant when run together — OCR extracts attacker-planted content, the reporting skill then ships the user's PII to a third party alongside the report. Reported at ~**90% of observed attacks**, i.e. the composition case is the *normal* case, not the tail. Consequence stated directly: you cannot review or model-check your way to safety, and you cannot remove everything at the door | `FormsPattern → pat-agent-supply-chain` **[registry]**; `FormsPattern → pat-new-cyber-threats` **[registry]** | `el-skill-collision-attack`, `el-agent-skills` **[registry]** |
| `sig-guard-at-the-action-surface` | security | Security design inversion for agentic systems: because the read surface is unbounded (every page, message, screenshot, GitHub issue, promotional email — all day, and multiplied in a group), **stop trying to gate the input and gate the action**. First layer: a deterministic regex/classical-NLP check at the action boundary (bash variables, secret/config reads, exports) sorting into allow / needs-approval / block — deterministic so it costs no latency. Second layer, because the deterministic one is beatable (dotted text `L.i.k.e` walks straight through regex): a LoRA-fine-tuned SLM separating data channel from instruction channel, measured against an agent-security benchmark where the naive baseline is a 50% coin flip, giving something to hill-climb | `FormsPattern → pat-verification-gap` **[registry]** | `el-jatayu-guard`, `el-data-instruction-channel-separation`, `el-constrain-effects-not-expression` **[registry]**, `el-deterministic-agentic-split` **[registry]** |
| `sig-memory-is-what-the-agent-becomes` | context | "The model is just the engine … **the memory is what the agent becomes**, and it's becoming in real time." In a group deployment naive memory (embeddings, files, store-everything-then-compact) fails, and the working design is: extract atomic high-value facts; build auto-raters for extraction quality, relevance, hierarchical relation, temporal weighting and retrieval; organize for intelligent graph-style retrieval instead of crawling; run a **continually adapting** relevance scorer ("learning what not to forget") so compaction is knowledge-based; and make the memory-injection engine KV-cache-aware, because "KV caches break when we try to do something cute with the model" — under both cloud and local serving. Less stored is also fewer tokens and lower latency | `FormsPattern → pat-agent-memory-layer` (rehomed from `pat-context-graphs` on coin 2026-08-14, per review note 4) | `el-atomic-fact-memory`, `el-continual-relevance-scorer`, `el-context-compaction` **[registry]**, `el-prefix-caching` **[registry]** |
| `sig-shared-agent-social-contract` | context | A shared assistant "is not just a large model shared between people — it's almost a new social contract": the same fact is public or private depending on the room, not on the data (a grocery list is benign for everyone; salary or health is not — "the data has not changed, the room in which it is deployed has changed"). Two mechanisms proposed: a common shared memory layer with a **per-user LoRA adapter**, so permissions are learned rather than coded ("the user as an engram" cited as the way to do it at scale); and a learned role/floor classifier for **when to speak and when not to**, since group-deployed agents are consistently over-proactive when unasked | `FormsPattern → pat-model-not-bottleneck` **[registry]** | `el-per-user-lora-permissions`, `el-permissioned-inference` **[registry]**, `el-recipient-routing`, `el-multiparty-turn-taking` **[registry]** |

## Insights (2 new)

| slug | name / brief | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-guard-the-effect-not-the-input` | An agentic system's read surface is unbounded and adversarial by default, so input filtering is the wrong control point — it either lets attacks through or gates everything and destroys the agent's usefulness. The tractable control point is the **action**: a small, fast, deterministic decision at the moment of effect, with a learned model layered on top for the evasions determinism cannot see. The corollary that makes this urgent is compositional: the units being audited (skills) are individually clean, so audit-per-unit is structurally blind and only runtime, effect-level checks can see the attack | `HighlightsPattern → pat-verification-gap` **[registry]** | `ReliesOnElement → el-jatayu-guard`, `el-constrain-effects-not-expression` **[registry]**, `el-skill-collision-attack` |
| `ins-multi-user-agents-need-a-social-layer` | Moving an agent from one user to a group changes the engineering problem, not its size. Three properties that are trivial or absent for a personal agent become the whole design: **what gets in** (a guard, against a surface that now includes everyone's messages and pastes), **what is kept** (memory that is curated, temporally weighted and continually re-scored, because a group's context evolves under you), and **who hears the answer** (channel, recipient and moment — DM vs group, glasses vs car speaker, and often silence). The privacy rule underneath is contextual, not classificatory: sensitivity is a property of the room, so permissions have to travel with the audience rather than with the datum | `HighlightsPattern → pat-harness-over-model` **[registry]** | `ReliesOnElement → el-recipient-routing`, `el-per-user-lora-permissions`, `el-atomic-fact-memory` |

## KnowHow (2 new)

All: `SourcedFromArtifact → ia-aie-rallabandi-wearing-the-agent`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-action-surface-guard` | Build the guard at the action surface, not the input | Let the agent read everything — gating the input gates the product; put the check where the agent takes an effect (bash/secret/config reads, exports, sends) and keep that first layer deterministic (regex/classical NLP) so it adds no latency; sort actions into three classes — allow, require approval, block — rather than binary pass/fail; expect the deterministic layer to be evaded (dotted or interspersed text defeats static approaches) and train a second, learned layer on the guard's own traffic — a LoRA-fine-tuned SLM that separates the data channel from the instruction channel and passes only benign content; benchmark it (a naive baseline is a coin flip, so there is real headroom to hill-climb); assume skills are individually clean and audit compositions at runtime, not units at scan time | `ReferencesElement → el-jatayu-guard`, `el-data-instruction-channel-separation`, `el-skill-collision-attack`, `el-constrain-effects-not-expression` **[registry]** |
| `how-group-agent-memory-and-routing` | Curate group memory and route the answer | Don't store the transcript and compact it later — extract atomic, high-value facts as the conversation goes; build auto-raters for the five axes (extraction quality, relevance, hierarchical relation, temporal weighting, retrieval quality) or you cannot tell whether the memory is working; organize memory for intelligent retrieval (graph-style) instead of crawling everything; train a **continually** adapting relevance scorer so forgetting is knowledge-based and tokens actually drop; keep the memory-injection engine KV-cache-aware — clever per-request memory surgery invalidates the cache; give each user a LoRA adapter over a shared memory layer so permissions are learned rather than policed; and route the output deliberately — DM instead of the group when it's sensitive, the personal device instead of the shared speaker, and train a role/floor classifier so the agent stops talking when nobody asked | `ReferencesElement → el-atomic-fact-memory`, `el-continual-relevance-scorer`, `el-per-user-lora-permissions`, `el-recipient-routing`, `el-multiparty-turn-taking` **[registry]**, `el-prefix-caching` **[registry]** |

## Dropped

- **The generic agent definition** (model + orchestration + tools, "we can build an agent in an afternoon") — framing only; the useful half is inside `sig-agents-move-from-one-user-to-groups`.
- **The four cited papers as their own nodes** — all four titles are caption-garbled and none is attributed to an author or venue (review note 3). Their findings are carried in the elements/signals; do not cite the titles externally without verification.
- **"Inspect agent" as a benchmark Element** — the eval name is too garbled to coin (review note 3); the 50%-baseline result is kept as prose in `el-data-instruction-channel-separation`.
- **The daughter/tutoring, calendar-sync and sleep-deprivation vignettes** — production illustrations, folded into `el-judith-agent`'s brief.
- **Cloud-vs-local serving** — one sentence, framed as "either way, mind the KV cache". Too thin for a `pat-sovereign-ai` edge; see review note 5.

## Review notes

1. **Company unresolvable — flagged as instructed.** The transcript names no employer, startup or lab. The only affiliation-shaped hints are "one of my advisers used to say" (an academic past) and the fact that Judith is deployed among the speaker's own friends and family, which reads as a personal project. I applied the corpus convention for this case — Expert node with no `AffiliatedWithCompany` edge — matching `exp-sachin-gupta` (b3), `exp-anna-marie-benzon` (b8) and `exp-matthew-berman` (b6). If you want the affiliation filled in from the conference programme or public record at seeding, that is a one-edge change; I did not guess, since a wrong employer on a talk about privacy architecture is a bad error to ship.
2. **Two reconstructed names.** (a) The security layer is captioned **"jetou"**, described as "a mythological character from Indian mythology" whose role is guarding — I have slugged it `el-jatayu-guard` (Jatayu, the vulture who tries to intercept an abduction in the Ramayana: a guardian who intervenes at the moment of the act, which fits the element exactly). ⚠ Verify before public-facing use; if it is wrong, the fallback slug is `el-action-surface-guard` with the mythological name dropped. (b) The agent name **"Judith"** is stated repeatedly and consistently, so I treat it as reliable. "robata classifier" → **RoBERTa**.
3. **All four cited papers are garbled; none is attributed.** As captioned: a "runtime skill audit" paper, "when safe skills collide", "learning what not to forget", and "the user as an engram". The middle two are plausible verbatim titles; the first and last may be paraphrases. Likewise the benchmark, captioned "**inspect agent**" — plausibly the UK AISI **Inspect** framework, or an agent-security suite of the AgentDojo family. **Do not cite any of these externally without verification**, and do not coin benchmark elements from them (same discipline as the batch-2 Altos-Labs paper-name flag).
4. **`pat-agent-memory-layer` (uncoined) — one of the strongest single statements in the corpus.** "The model is just the engine; the memory is what the agent becomes, and it's becoming in real time" is a purer statement of the candidate than Zep, BabyAGI or Omnara gave, and it arrives with an engineering programme (atomic facts, five-axis auto-raters, continual relevance scoring, KV-cache-aware injection). Per instruction I did **not** coin: `sig-memory-is-what-the-agent-becomes` is homed on `pat-context-graphs` (defensible — the talk explicitly proposes graph organization for retrieval) and should be **rehomed to `pat-agent-memory-layer` if that candidate is coined at review**. Note the tension with the two counters now on record (Netflix markdown-in-Git, b14; the FlyersSoft SLA counter in this same batch): this talk is the opposite pole — memory as the agent's identity, not as infrastructure.
5. **`pat-sovereign-ai` — deliberately not used.** Wearables, SLMs, LoRA adapters and "cloud or local serving" all gesture at it, but the talk never argues for local/sovereign deployment; privacy here is enforced by *architecture* (per-user adapters, action-surface guards, recipient routing), not by keeping data on-device. Homing `sig-shared-agent-social-contract` on `pat-model-not-bottleneck` instead reflects the actual claim — the value and the failure modes live in the guard/memory/routing periphery. Flip it to `pat-sovereign-ai` at review if you read wearable + SLM + per-user adapters as sovereignty evidence; it would partially offset that pattern's two counter-edges.
6. **Proposed cross-file edge at seeding (inferred, not stated):** `el-skill-collision-attack` `UsesElement → el-lethal-trifecta` **[registry]** — the OCR-plus-reporting exfiltration is textbook lethal trifecta (private data + untrusted content + exfiltration channel), but the talk never uses Willison's term, so I left it as a proposal rather than an edge.
7. **Corpus adjacencies worth noting at review (no edges created).** This is the corpus's third wearable/always-on data point after `el-bee` (b2, always-on capture, privacy-first) and `el-physical-ai-terminal` (b8, ESP32); the first to treat the *group* rather than the individual as the unit. `el-recipient-routing` and `el-multiparty-turn-taking` (JoinIn, b8) are independent arrivals at the same "when may the agent speak" problem from voice-meeting and group-chat directions respectively — a merge-check candidate if a third one shows up. `el-jatayu-guard` is the security-side twin of `el-constrain-effects-not-expression` (b7) and `el-deterministic-agentic-split` (b2); three talks now converge on "constrain the effect, not the generation".
8. **Signal-bar caveat.** `sig-skill-collision-runtime-attacks` is the only signal carrying external, citable findings (the ~90% figure), and its sources are the garbled papers of note 3. The other four are practitioner testimony from a single 8-month deployment. Strong talk, thin externally-verifiable base — treat the 90% figure as unconfirmed.
