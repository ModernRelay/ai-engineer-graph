# SPIKE extraction — "Agents, codebases, and teams" (Aditya Khandelwal, Amazon AGI Lab) — FOR REVIEW

Source transcript: `transcripts/khandelwal-amazon-agents-codebases-teams.txt` (auto-captions — quotes are paraphrases, not verbatim).
Video: https://youtu.be/aeTb5BdmTTc — AI Engineer World's Fair, published 2026-08-11.
`stagingTimestamp` for the artifact and all dated nodes: 2026-08-11 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.
Shape of the talk: an engineering lead's field report on the gap the corpus keeps circling — "there's so much content about how to set up your own code base to work well with agents, but it all breaks the moment you try to use it with your team in your actual production setup." Maps the enterprise adoption arc, plots individual developers on fear × utilization, names the diagnostic symptoms of a bad setup, argues it is a **leadership problem not an IC problem**, then gives the playbook his ~10-person team actually ran and the problems it hit. Caption garbles: "hardness" → **harness** (throughout), "Cloud MD" → **CLAUDE.md**, "self-tours"/"sev twos" → **sev-2s**, "gyan" → *gyan* (Hindi, "sermons" — the speaker glosses it himself), "grapping" → grepping, "gigantic reviews" → likely *agentic reviews*.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-khandelwal-agents-codebases-teams` | Agents, codebases, and teams (Aditya Khandelwal, Amazon AGI Lab — AI Engineer World's Fair) | youtube | https://youtu.be/aeTb5BdmTTc |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-aditya-khandelwal`.

## Experts (1 new)

| slug | name | edges |
|---|---|---|
| `exp-aditya-khandelwal` | Aditya Khandelwal (Amazon AGI Lab; led a team of ~10 engineers through the agent-adoption process the talk describes) | `AffiliatedWithCompany → co-amazon` **[registry]** |

## Companies (0 new)

Reused **[registry]**, edge-only: `co-amazon` **[b2]** — third corpus appearance for Amazon AGI Lab specifically (after Barth's Perception Agents in b11 and the b14 infra talk), and the first covering internal engineering practice rather than product. New facts: team-level harness engineering, a shared "ship it" skill, a nightly code-gardener agent.

## Elements (5 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-fear-utilization-map` | The fear × utilization map | concept | harness | The framing device for adoption as a human problem. Plot each developer on two axes — **fear** (is this coming for my job, or is it a handy tool?) and **utilization** (how much are they actually using it?) — and trace the arc: at first fear high and utilization low; then early adopters get outsized leverage and others try harder ("it looks like I'm still needed if I figure out how to use this thing"); then mandates and token-maxxing raise utilization without confidence; then slop and sev-2s lower fear but leave confidence flat. The goal is to move everyone toward low fear and high utilization, and the explicit instruction is that "fear is real, human emotions are real, we should recognize it" |
| `el-bad-setup-symptoms` | Symptoms of a bad agent setup | concept | harness | A diagnostic list, offered as things that *indicate* the setup is wrong rather than that the model is bad. **Babysitting agents** at all is the first tell. **"The model is being really dumb today"** — the model did not change; the harness may have, and a setup that cannot absorb small harness changes is not set up well. **Silently burning context and money** — blowing through 500k, 750k or a million tokens and hitting auto-compact on tasks that do not warrant it. **Long sessions needing constant intervention.** **A constant slop factory.** And **asking how other companies ship so fast** when model labs release on a six-to-eight-week cadence — "clearly they have something which we don't" |
| `el-team-harness-engineering` | Harness engineering as a team function | ops | harness | The reframing the talk exists to argue: "it isn't really an IC's job. It's a job for leadership. It's a job for the company." The mechanism is that the highest-leverage changes — how the codebase is organized for agent navigation — **require team buy-in and cannot be done by an individual**. The cost of not doing it is distributional: people who figure it out ship 10 PRs a day and look like gods, the one-to-two-PR people inherit the review burden, "and that's actually a really, really bad thing, because now not only can they not ship, they're going to see bad code and then curse the agents." Three principles: **smart prompt injection** (treat the whole codebase as a map so the right context arrives at the right time), **close the loop** (a self-healing pipeline, because "slop is inevitable"), and **iterate continuously** |
| `el-progressive-disclosure-codebase` | Progressive disclosure at codebase scale | ops | context | The concrete technique behind smart prompt injection, expanded in Q&A. Keep `CLAUDE.md`/`AGENTS.md` as **a thin index that points to the right files**, not one big overloaded document, because that is what loads on the agent's first prompt. Cap `SKILL.md` files at **~100 lines** on the reasoning that "your skill is really a folder." Put runbook-style documentation **in the code comments**, so an agent that greps its way into a file finds the pointer to everything relevant. The measurable test: watch what the first prompt costs — "I think like 20, 25K tokens get taken anyway, but if you're coming to 40K, 50K, something's wrong. That's not really progressive disclosure" |
| `el-ship-it-skill` | The "ship it" skill | technology | harness | The single high-value shared skill the team invested in, and their adoption lever: it takes over everything "from code done to PR ready for review" — opening the PR, writing the description, handling review comments and merge comments, and working through CI failures in a loop. Often runs **over an hour**, which initially scared people, "but once they saw the value they get invested, because it's one skill which tells them: this AI thing can actually work for me, I don't need to constantly babysit it, I can trust it." Paired with a **code gardener** that runs nightly over the repo checking organization against the codebase's own standards |

Element edges: all five `IdentifiedInArtifact → ia-aie-khandelwal-agents-codebases-teams`.
`el-team-harness-engineering` `UsesElement → el-progressive-disclosure-codebase`, `el-ship-it-skill`, `el-bad-setup-symptoms`;
`el-progressive-disclosure-codebase` `UsesElement → el-agents-md` **[registry, b6]**, `el-agent-skills` **[registry, batch1]**;
`el-ship-it-skill` `UsesElement → el-agent-skills` **[registry, batch1]**;
`el-fear-utilization-map` `EnablesElement → el-team-harness-engineering`;
`el-team-harness-engineering` `ExemplifiesPattern → pat-harness-over-model` **[registry]**.

Reused elements (no new nodes): `el-agents-md` **[b6]** and `el-agent-skills` **[batch1]** — both gain concrete authoring constraints here (thin-index discipline; ~100-line skill cap). `el-context-compaction` **[b6]** (auto-compact as a waste symptom). `el-reviewdebt` **[b5]** — the review-burden-falls-on-the-slow-adopters dynamic is a direct instance; edge left to review, see note 4.

## Signals (5 new)

All: `SpottedInArtifact → ia-aie-khandelwal-agents-codebases-teams`, `SourcedFromSource → source-aie-yt` **[registry]**; all `RelevantCompany → co-amazon` **[registry]**.

| slug | domain | name / brief | FormsPattern | other edges |
|---|---|---|---|---|
| `sig-individual-agent-setups-dont-survive-teams` | harness | The gap the talk names: an enormous body of content exists on configuring a personal repo for agents, "but it all seems to break the moment you actually try to use it with your team in your actual production setup." For individual repos the advice holds; at team scale it does not, because the highest-leverage changes are to shared codebase organization and **cannot be made by an individual contributor**. Reported from ~10 engineers over several months of iteration | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-team-harness-engineering`, `el-bad-setup-symptoms` |
| `sig-harness-engineering-is-a-leadership-job` | harness | The central claim: "making engineers work well with their agents is truly the most impactful thing you could do as an organization," and it belongs to leadership rather than to ICs. The argument against the figure-it-out-yourself default is distributional rather than moral — uneven adoption produces a class shipping 10 PRs a day and a class inheriting their review burden, who then "can't ship, are going to see bad code, and curse the agents," entrenching the gap. Practical corollaries include budgeting a standing **percentage of IC time** for shared-setup work that produces no PRs, and treating winning over skeptics — measured by whether they will edit the shared setup — as the real adoption signal | `FormsPattern → pat-value-of-judgement` **[registry]** | `OnElement → el-team-harness-engineering`, `el-fear-utilization-map` |
| `sig-adoption-arc-fear-then-slop` | harness | The enterprise arc traced from the inside: coding agents land, a few people get outsized leverage, "is this AGI?"; companies generalize it into mandates and token-maxxing, "clearly a galaxy-brain moment"; then "the inevitable happened — AI slop shipped, and there's a bunch of sev-2s," people retract, prices climb, "people figured out that tokens have to be paid for — you just can't token-max your way through life," and budgets get bolted on because "money is being lit on fire and the money has to come from somewhere." An unusually candid adoption post-mortem from a frontier-lab engineering org. **HELD PATTERN-LESS** — `pat-ai-native-org` ledger, dysfunction side | — (held pattern-less) | `OnElement → el-fear-utilization-map`, `el-bad-setup-symptoms` |
| `sig-progressive-disclosure-is-measurable` | context | The most immediately usable content: progressive disclosure has a **number attached**. Keep the agent-instruction file a thin index pointing to files rather than one loaded document, cap skill files at ~100 lines because "your skill is really a folder," and push runbook documentation into code comments so a grepping agent finds it in place. Then measure the first prompt — "20, 25K tokens get taken anyway, but if you're coming to 40K, 50K, something's wrong." Context discipline stated as an observable threshold rather than an aspiration | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-progressive-disclosure-codebase`, `el-agents-md` **[registry, b6]** |
| `sig-one-trusted-skill-converts-skeptics` | harness | The adoption mechanism that worked: rather than broad mandates, invest in **one high-value skill** — here "ship it," covering everything from code-complete to PR-ready including CI failure loops — and let its reliability do the persuading. That it often runs over an hour initially scared people and became the point: "once they saw the value they get invested, because it's one skill which tells them this AI thing can actually work for me, I don't need to constantly babysit it, I can trust it." Reframed explicitly as expectation-setting — "it's good if agents take too long, that means you can go off and do other things and you have confidence they're doing the right thing" | `FormsPattern → pat-harness-over-model` **[registry]** | `OnElement → el-ship-it-skill`, `el-agent-skills` **[registry, batch1]** |

## Insights (2 new)

| slug | name / brief | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-agent-adoption-is-a-distribution-problem` | The damage from uneven agent adoption is not that some people are slower but that the fast and slow are coupled through review: high-throughput adopters generate PRs that low-throughput colleagues must review, so the cost of one group's leverage lands on the other, who then have less time to adopt and more reason to distrust the tools. That feedback loop makes "figure it out yourself" actively harmful rather than merely suboptimal, and it is why the intervention has to be organizational — a shared setup, shared skills, and protected time — rather than individual enablement | `HighlightsPattern → pat-value-of-judgement` **[registry]** | `ReliesOnElement → el-team-harness-engineering`, `el-fear-utilization-map` |
| `ins-trust-is-bought-with-one-long-skill` | The counter-intuitive adoption lesson is that a skill running for an hour converts skeptics better than one running for a minute, because duration is what forces the question of whether the agent can be left alone — and answering that question affirmatively once is what ends babysitting. Teams optimizing agent adoption for responsiveness are optimizing the wrong variable; the thing that changes behaviour is a single end-to-end task the engineer is willing to walk away from, which means the highest-value shared skill is whichever one spans the most of a workflow, not the one used most often | `HighlightsPattern → pat-harness-over-model` **[registry]** | `ReliesOnElement → el-ship-it-skill`, `el-team-harness-engineering` |

## KnowHow (1 new)

All: `SourcedFromArtifact → ia-aie-khandelwal-agents-codebases-teams`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-roll-out-agents-across-a-team` | Roll agents out as a leadership programme, not an IC exercise | Treat making engineers work well with agents as an organizational responsibility, since the highest-leverage changes are to shared codebase organization and no individual can make them — and because uneven adoption is self-entrenching, with fast adopters' PR volume landing as review burden on slower colleagues who then have less time and more reason to distrust the tools; diagnose the setup rather than the model when things go wrong — babysitting, "the model is dumb today" (the model did not change), silently burning 500k–1M tokens and hitting auto-compact on simple tasks, long sessions needing constant intervention, and a steady slop output are all setup symptoms; **do progressive disclosure properly and measure it** — keep the agent instruction file a thin index that points to files rather than one loaded document, cap skill files around 100 lines since a skill is really a folder, put runbook documentation in code comments so a grepping agent finds it in place, then watch the first prompt's context cost and treat 40–50K where 20–25K is baseline as a failure; harvest your best ICs' practices and ship them as the shared setup, accepting that engineers dislike being told their personal setup is imperfect; **invest in one high-value end-to-end skill** rather than broad mandates, and expect a long-running one (code-done to PR-ready, including CI failure loops) to convert skeptics precisely because it proves the agent can be left alone; close the loop with CI, agentic reviews and a nightly code-gardener, because slop is inevitable and needs a detection-and-repair path; budget a standing percentage of IC time for setup work that produces no PRs; give experiments and prototypes an explicit opt-out from your production standards rather than letting them drag the bar down; and treat winning over skeptics as the real measure — you know they are bought in when they edit the shared setup themselves | `ReferencesElement → el-team-harness-engineering`, `el-progressive-disclosure-codebase`, `el-ship-it-skill`, `el-bad-setup-symptoms` |

## Dropped

- **The skiing metaphor close** ("if you're pizza-braking you're going to crash… you have to commit to the speed… be okay with falling, the point is to recover") — motivational.
- **The problems-we-hit list** (400–500 issues within a couple of weeks; agents creating issues because they were not wired correctly; merge hell) — largely folded into the KnowHow; the issue-flood number is caption-sourced and unverifiable.
- **The "enough gyan" aside** — the speaker glossing his own Hindi word for sermonizing; colour.
- **Step one create a CLAUDE.md, step two add skills, "is that it?"** — the rhetorical setup for `el-bad-setup-symptoms`.

## Review notes

1. **Where this file sits.** It is the batch's only team-practice talk and it lands almost entirely on `pat-harness-over-model` — notably on the **claim-1 (reliability/control) reading** that b15's FINDING 1 recommends scoping to. Nothing here claims the harness supplies capability; every claim is that a shared, well-organized harness is what makes agent output trustworthy and evenly distributed. Useful as a clean claim-1 exemplar at the pattern review, alongside the Anthropic file's split evidence.
2. **⚠ `sig-adoption-arc-fear-then-slop` held pattern-less — a strong `pat-ai-native-org` dysfunction-side point.** The arc (leverage → mandates → token-maxxing → slop and sev-2s → retraction → budgets) is the same shape as b17's Dailey velocity-sickness taxonomy, arriving independently from a frontier-lab engineering org rather than a tool vendor. The registry has recorded `pat-ai-native-org` as the widest-evidenced uncoined candidate since b8; this adds a second high-quality dysfunction data point. Rehome on coin.
3. **The 20–25K/40–50K context threshold is the most portable number in the batch.** It converts progressive disclosure from a principle into a check anyone can run against their own repo today. Worth surfacing in any derived report even though it is single-mention and unverified.
4. **Proposed cross-file edge, left to review.** The review-burden dynamic is a direct instance of `el-reviewdebt` **[b5, Gupta]**. Proposed at seeding: `el-team-harness-engineering` `UsesElement → el-reviewdebt`, or an `ins-` link. Not emitted because the speaker never frames it as measurable debt.
5. **⚠ Verify before seeding.** All quantities are caption-sourced and single-mention: ~10-person team, 10 PRs/day for fast adopters, 500k–1M token burns, 400–500 issues in a couple of weeks, the 20–25K/40–50K context figures, the ~100-line skill cap, and the "six-to-eight-week" lab release cadence. The nightly reviewer is captioned "gigantic reviews," normalized to *agentic reviews* — confirm before it enters a brief.
6. **Company note.** Third Amazon AGI Lab appearance and the first on internal practice. **Recommend widening `co-amazon`'s brief** to record that the lab runs team-level harness engineering with shared skills, since the existing brief is product-facing.
