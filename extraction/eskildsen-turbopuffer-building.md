# SPIKE extraction — "Building Turbopuffer" (Gergely Orosz interviewing Simon Eskildsen, CEO) — FOR REVIEW

Source transcript: `transcripts/eskildsen-turbopuffer-building.txt` (auto-captions — quotes are paraphrases, not verbatim; the guest is captioned "Simon Ericson" = **Simon Eskildsen** per the byline, the host "Gerge" = **Gergely Orosz**).
Video: https://youtu.be/jQDXzEVHMSE — AI Engineer, published 2026-08-03.
`stagingTimestamp` for the artifact and all signals: 2026-08-03 (publish date).
Entities marked **[registry]** are already in the registry — edges link to them, no new node.

**Format note: interview**, not a talk — Gergely Orosz (The Pragmatic Engineer) interviewing Simon Eskildsen (founder/CEO, Turbopuffer). Both named participants substantively contribute (Orosz supplies the Cursor-migration corroboration from his own reporting and frames the infra/state discussion), so both carry `ContributedByExpert` per the interview rule. Content is biographical for its first half; extraction concentrates on the industry-bearing second half.

---

## InformationArtifact (1 new)

| slug | name | artifactType | link |
|---|---|---|---|
| `ia-aie-eskildsen-building-turbopuffer` | Building Turbopuffer (Gergely Orosz, The Pragmatic Engineer, interviewing Simon Eskildsen, CEO — AI Engineer) | youtube | https://youtu.be/jQDXzEVHMSE |

Edges: `PublishedBySource → source-aie-yt` **[registry]**; `ContributedByExpert → exp-simon-eskildsen`, `exp-gergely-orosz`.

## Experts (2 new)

| slug | name | edges |
|---|---|---|
| `exp-simon-eskildsen` | Simon Eskildsen (founder & CEO, Turbopuffer; ⚠ captioned "Simon Ericson", resolved from the byline. Danish; IOI-track autodidact, no university; Shopify infrastructure 2013–2021 — sharding, multi-DC, storefront rewrite with co-founder Justine, creator of Toxiproxy; author of the napkin-math project; built Turbopuffer solo in summer 2023 as "a project, not a company") | `AffiliatedWithCompany → co-turbopuffer` |
| `exp-gergely-orosz` | Gergely Orosz (author of The Pragmatic Engineer — ⚠ captioned "Gerge"; interviewer; ex-Uber ("you had a long tenure at Uber" is confirmed on stage); his own Cursor deep-dive reporting supplies the Aurora-to-Turbopuffer migration account woven into this interview). No company node coined for The Pragmatic Engineer — independent-author precedent (Willison, Browne); the newsletter is noted here, not as a SourceEntity, since the artifact publishes via source-aie-yt | — (no AffiliatedWithCompany — independent) |

## Companies (1 new)

| slug | name | type | note |
|---|---|---|---|
| `co-turbopuffer` | Turbopuffer | developer | S3-native search/vector database ("so many AI companies are using you as an infrastructure layer"): object storage as the source of truth, cache hierarchy above it. Launched October 2023 from a single 8-core GCP instance; first customer Cursor (bill cut 95% vs prior vendor); customers include AI labs/startups (Reflection named). Bootstrapped-profitable culture: raised only for R&D (~$700K seed with an offer to return it) and employee liquidity; full-remote with "campfires"; runs on CPUs + NVMe + S3 by design ("AVX-512 is so sick" — to Jensen Huang's face) |

Reused **[registry]**, edge-only: `co-cursor` **[batch 3]** (first customer; the DRAM-vector unit-economics dinner-table hypothesis; Aman's early S3-KV-cache tweets; migrated everything "over the course of a week or two"; "never bet your business on a tiny startup where you're their biggest customer — except Turbopuffer"), `co-shopify` **[batch 11]** (Eskildsen's formation: sharding cutover a week before Black Friday, "you can't cache writes", Toxiproxy still in Shopify CI), `co-nvidia` **[batch 2]** (the Jensen/vapes/CPU meeting — color, but also the partner-event frame for the CPU-scarcity claims).

Kept in prose, not coined: **Readwise** (the 2022 recommendation-engine story: $30K/month vector bill vs $5K total infra — the cost wall that seeded Turbopuffer), **Reflection** (customer maxed out on longest-possible compute contracts — single mention), AWS/GCP/Azure as venues.

## Elements (4 new)

| slug | name | kind | domain | brief |
|---|---|---|---|---|
| `el-turbopuffer` | Turbopuffer (S3-native search engine) | product | infra | Search/vector database where **all writes commit directly to object storage** (invariant from day one: shut down every VM, no data lost) and hot data hotloads into NVMe/RAM caches. V1 was deliberately primitive: clustering over vectors, files named cluster-1…n plus a centroids file, search = download centroids + n closest clusters; an NGINX reverse-proxy as the entire cache layer. Launch price point: **~$1 per million vectors when the cheapest working alternative was ~$100** — the two-orders-of-magnitude claim that made Cursor's use case viable. "Why can't you work on a database like it's SaaS? If anyone uses it, we'll do it properly" |
| `el-object-storage-native-db` | Object-storage-native data architecture | concept | infra | The architectural thesis under Turbopuffer (and Cursor's independent wish for it): durability and source-of-truth live on S3-class storage (P99 ~200ms for a 256–512KB object; design to P99/P999 and minimize round trips), while "the actual code bases actively being used" sit in memory, everything else hotloading in and out of cache. Extends beyond vectors — Aman's early tweets about **S3 for KV caching** are cited as ahead-of-their-time ("barely anyone is still doing [it] even though the economics… it will happen"). The napkin numbers that justify it: DRAM ~$2/GB, S3 ~2¢/GB |
| `el-napkin-math` | Napkin math (first-principles performance accounting) | ops | infra | Eskildsen's public project and method: a GitHub table of ~50 system invariants (DRAM bandwidth, S3 round-trip cost/latency, NVMe/EBS throughput, per-GB prices on demand/spot/committed) regenerated by script, drilled via flashcards. Use: when a benchmark disagrees with the first-principles estimate — "you tell me the benchmark takes 10 [seconds]; **one of us is wrong**" — either the mental model has a gap or the benchmark measured the wrong thing (e.g. an accidental distributed query dragging P99). Origin: repeatedly watching teams make infrastructure decisions on poor benchmarks |
| `el-toxiproxy` | Toxiproxy (fault-injection proxy) | technology | infra | Shopify-era open-source layer-4/7 proxy sitting between an app and its databases: API calls make a dependency go down, slow, or misbehave, so driver/application failure handling is tested through the real protocol rather than mocks — implementing a service×dependency failure matrix in CI. Uncovered "tens of issues" in the MySQL driver and Rails that nobody in the ecosystem had tested; per Eskildsen still running in Shopify CI today. Lineage: began as a GDB hack closing the database file descriptor mid-test |

Registry element reuse (no new nodes, edges only): `el-agentic-environment` **[batch 11 late]** — the CPU-scarcity signal's mechanism ("the models are not that good at CAD or shipbuilding… you have to spin up even more RL environments to do that").

Element edges: all four new `IdentifiedInArtifact → ia-aie-eskildsen-building-turbopuffer`; `el-agentic-environment` also `IdentifiedInArtifact → ia-aie-eskildsen-building-turbopuffer` (additional artifact).
`el-turbopuffer` `DevelopedByCompany → co-turbopuffer`, `UsesElement → el-object-storage-native-db`; `el-object-storage-native-db` `EnablesPattern → pat-model-not-bottleneck` **[registry]** (the layer around the model deciding product viability); `el-napkin-math` `EnablesPattern → pat-verification-gap` **[registry]** is **not** asserted — see Review note 4.

## Signals (3 new)

All: `SpottedInArtifact → ia-aie-eskildsen-building-turbopuffer`, `SourcedFromSource → source-aie-yt` **[registry]**.

| slug | domain | name / brief | FormsPattern | extra edges |
|---|---|---|---|---|
| `sig-rl-and-agents-drive-cpu-scarcity` | infra | An infra vendor's ground truth against the GPU-only scarcity narrative: **CPUs are no longer easy to get.** "As RL is becoming a very large amount of the workloads" the labs are "sucking up a lot of CPUs" — teaching models to search, use git, "boot up bash… run real things and learn from that takes a lot of CPU" — and all the agents themselves run on CPUs; the feedback loop is explicit: applied gaps ("models are not that good at CAD or shipbuilding") → "spin up even more RL environments" → more CPU. NVMe SSDs and DRAM are tightening too; "it gets a lot worse before it gets a lot better"; **even the big companies fight each other for allocations** — Turbopuffer sells to companies it also competes against for CPUs; a customer (Reflection) is maxed out on the longest contracts available; region planning now follows power ("where is the power… is generally where they're going to ship the new CPUs") | — (**held pattern-less** — `pat-environments-economy` candidate evidence of a new kind: the first *physical-resource* footprint of the environments economy in the corpus — RL environments as a driver of general-purpose compute scarcity. See Review note 2) | `OnElement → el-agentic-environment` **[registry]**; `RelevantCompany → co-turbopuffer` |
| `sig-object-storage-collapsed-retrieval-economics` | infra | The unit-economics story of AI retrieval infra, with numbers at both ends: in 2022–23 vectors-in-DRAM made obvious features unshippable (Readwise's working recommendation engine: **$30K/month against $5K total other infra** — killed; Cursor's founders: "the unit economics of all the vectors in DRAM are not working"). Moving source-of-truth to S3 with cache hierarchy repriced the category — **~$1/million vectors vs ~$100 for the cheapest prior working option** — and Cursor, ~8 people, migrated everything in about two weeks for a **95% bill reduction** (promised on napkin math before the software deserved it). Context that forced retrieval in the first place: 2022 context windows of 4–8KB meant "you had to reach for search very quickly." The generalization is still early: S3-for-KV-caching "barely anyone is doing… I think it will happen" | `FormsPattern → pat-model-not-bottleneck` **[registry]** — what gated the AI product was never the model: it was the storage bill of the layer beside it | `OnElement → el-turbopuffer`, `el-object-storage-native-db`; `RelevantCompany → co-cursor` **[registry]**, `co-turbopuffer` |
| `sig-profitable-infra-counterculture` | infra | A counter-instance to AI-infra capital norms, stated as method: Turbopuffer launched as one person's summer project on a single 8-core instance, promised Cursor a 4K/month bill and then built toward it ("this number should just be lower than the other number… I don't think I'd spent enough time in San Francisco, because the money over here works a little bit differently"), raised **~$700K** with an explicit offer to shut down and return every dime absent PMF — "terrifying" to West-Coast ears — became **profitable later that year**, and formalized a six-reasons taxonomy for raising (R&D, growth, founder ego, employee liquidity, strategic partnership, M&A), having used only #1 and #4. Meanwhile its customers raise billions; the vendor underneath them runs on gross margin discipline | — (**pattern-less — no candidate.** Industry-texture counterpoint; the corpus has no capital-formation pattern and one talk doesn't seed one. Drop-option: fold into the artifact's prose entirely; kept as a signal because it is the interview's most repeated theme and a deliberate public stance by a load-bearing AI-infra vendor) | `RelevantCompany → co-turbopuffer`, `co-cursor` **[registry]** |

## Insights (2 new)

| slug | name | HighlightsPattern | ReliesOnElement |
|---|---|---|---|
| `ins-the-agentic-era-runs-on-general-purpose-compute` | The AI buildout's scarcity has quietly spread from accelerators to the unglamorous half of the datacenter: RL rollouts and production agents do "very general purpose things" — bash, git, browsers, file systems — so CPUs, NVMe and DRAM are now allocation-contested the way GPUs are, and power location decides where capacity lands. The GPU-shortage frame under-describes the constraint surface an agentic industry actually sits on; vendors that can run on "many different machine types" hold a structural advantage | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** (scoped: the binding constraints are in the layers around the model — here literally the compute logistics) | `ReliesOnElement → el-agentic-environment` **[registry]**, `el-object-storage-native-db` |
| `ins-object-storage-is-the-ai-storage-endgame` | S3-as-source-of-truth with hot caches is doing to AI-era data what commodity cloud did to compute: a ~100× price collapse turned retrieval from a killed feature into a default layer, an 8-person Cursor bet its business on it, and the same shape is queued up for KV caches and beyond. The durable insight is architectural, not product-specific: design storage to object-store durability + P99 round-trip math, and the workloads that "couldn't line up" economically start lining up | `HighlightsPattern → pat-model-not-bottleneck` **[registry]** | `ReliesOnElement → el-object-storage-native-db`, `el-turbopuffer`, `el-napkin-math` |

## KnowHow (3 new)

All `SourcedFromArtifact → ia-aie-eskildsen-building-turbopuffer`.

| slug | name | guidelines (condensed) | ReferencesElement |
|---|---|---|---|
| `how-napkin-math-an-infra-decision` | Replace benchmark trust with first-principles accounting | Maintain (or adopt) a table of system invariants — DRAM/NVMe/EBS bandwidth, S3 round-trip latency and cost, per-GB prices across purchase modes — and drill them until they're reflexive (flashcards). Before accepting any benchmark: compute what the operation *should* take (terms × documents × list sizes ÷ DRAM bandwidth; pages visited × ~1ms random SSD read). When benchmark and estimate disagree, treat it as a contradiction to resolve — "one of us is wrong" — and hunt the gap: wrong query plan, accidental distributed fan-out inflating P99, buggy discs, or a hole in your model. Design object-storage systems to P99/P999 (you fire many requests per logical operation, so you *will* hit the tail) and minimize round trips structurally. Batch effects break naive arithmetic (10K MySQL writes/sec on 1ms fsyncs — because fsyncs batch): verify mechanisms, don't stop at the first clean number | `ReferencesElement → el-napkin-math`, `el-object-storage-native-db` |
| `how-fault-inject-through-a-proxy` | Test failure handling through the protocol, not through mocks | Put a controllable proxy between the application and every stateful dependency; drive it by API to take dependencies down, slow them, or corrupt responses; encode the full "when component X is down, service Y behaves like Z" matrix as CI tests. Mocks hide the layer where failures actually land — the drivers; proxy-level injection found tens of upstream driver/framework bugs nobody had ever exercised. Default failure mode to design away: session store down ⇒ entire storefront down. Start crude if needed (close the DB file descriptor via debugger) but graduate to the proxy so it runs in CI forever | `ReferencesElement → el-toxiproxy` |
| `how-decide-why-you-raise` | Name which of the six reasons you're raising for | The six: (1) fund R&D, (2) fund growth, (3) founder ego, (4) reward/liquidity for employees, (5) strategic partnership, (6) M&A. Be honest about which one applies — ego raises are "very dangerous… you're diluting all of your employees" and pricing future hires' upside for a status game. Bootstrapping discipline as default (revenue > bills before salaries); offer investors the shutdown-and-return option if conviction is unproven; raise #4 deliberately so employees don't wait a decade for an IPO to see upside | `ReferencesElement → el-turbopuffer` (context anchor; no better target — drop the edge if an element-free knowhow is preferred) |

## Dropped

- **Biography** (PowerPoint Turing-completeness, FrontPage, Danish internet exhaustion, World of Warcraft English, IOI, the Nokia/NYT article that got him recruited) — formation color; compressed into the Expert brief.
- **Shopify war stories** beyond Toxiproxy (sharding cutover pre-Black-Friday, the mystery 128GB Redis box, Kardashian traffic spikes, "you can't cache writes") — Expert-brief prose; no elements.
- **The Jensen Huang vapes exchange** — color (retained one clause in the company brief for the CPU-positioning it illustrates).
- **Machine-type shopping** (GCP C4 / Z4D / ARM C4A preferences, SKU flexibility) — operational detail inside `sig-rl-and-agents-drive-cpu-scarcity`'s territory; prose only.
- **Remote culture mechanics** (campfires, turbo credits, the central-bank joke, twice-yearly offsites) — org color; noted in company brief, no knowhow (no AI bearing).
- **"Buen/Boyan" and "Locky"** — early engineer and first investor, first-name/nickname only; not coined (rule).
- **Justine** — co-founder, first-name-only in captions; not coined (rule), noted in Expert/company briefs. Add-option if the reconciler resolves the surname from public record.
- **PG-analyze / autovacuum consulting scene, Aurora vendor identification** — kept as prose inside the Cursor story ("we don't need to name names" is respected: the vendor names live in Orosz's published deep dive, not asserted here).

## Review notes

1. **Interview format handled per instruction:** both participants get `ContributedByExpert`. Orosz's contribution is real but secondary (corroboration + framing); if the reconciler wants contribution-weighting, the artifact name already marks him as interviewer.
2. **`pat-environments-economy` (uncoined) — a genuinely new evidence *type*.** Every prior candidate datapoint is about environments as products/assets (Harbor, Snorkel, Bugcrowd, Morgan Stanley…). `sig-rl-and-agents-drive-cpu-scarcity` is the first **macro-resource shadow**: a neutral third-party vendor (sells storage/search, not environments) reporting that RL-environment demand is reshaping *cloud CPU allocation*, with the applied-gap→more-environments feedback loop stated unprompted. If the pattern coins, this signal rehomes; if it doesn't, the signal still stands alone as infra evidence. Deliberately NOT forced onto the pattern per instructions.
3. **This is the corpus's least AI-typical file, on purpose** — Orosz closes by noting they "managed to talk very little about AI." Signal count (3) is low by design; the talk's graph value is concentrated in infra economics and two reusable ops knowhows. Precedent: `borucki-huggingface-2m-models.md` (batch 14) was flagged the same way.
4. **`el-napkin-math` and pat-verification-gap — considered, rejected.** The napkin-math method is verification culture, and `sig-benchmarks-vs-napkin-math` was drafted then demoted to a knowhow: the verification-gap pattern is about *AI-generation* outpacing verification, and stretching it to cover database-benchmark hygiene would dilute the pattern's meaning. The material survives fully in `how-napkin-math-an-infra-decision`. If a reviewer disagrees, the edge to add is `el-napkin-math` `EnablesPattern → pat-verification-gap`.
5. **Caption garbles normalized** (verify before public-facing use): "Simon Ericson" → **Simon Eskildsen**; "Gerge" → **Gergely (Orosz)**; "Toxyroxy" → **Toxiproxy**; "Turboper"/"Turop Puffer"/"turbuffer" → **Turbopuffer**; "Prim…"—n/a; "Chachi BT" → ChatGPT; "postcress"/"Postgress" → Postgres; "engine X"/"enginex" → NGINX; "T-Ox instance"/"Tox instance" → ⚠ unresolved GCP instance name (kept generic "single 8-core GCP instance"); "beach tree" → B-tree; "f-sync" → fsync; "Swallow told me" → ⚠ garbled attribution for the Cursor never-bet-your-business quote (a Cursor founder — likely Sualeh; not asserted); "Aman" → Aman (Sanger, Cursor co-founder — first name kept as spoken); "BFCM" → Black Friday/Cyber Monday; "Buen/Boyan…God" → unresolved name (not coined); "II competitions" → IOI; "napkin math" as spoken; "reddish server" → Redis server; "AVX 512" → AVX-512; "Base 10" n/a here.
6. **Numbers to preserve exactly** (stage-stated): S3 P99 ≈ 200ms for 256–512KB objects; ~$1/M vectors vs ~$100/M prior; 95% bill reduction for Cursor; $30K/mo vs $5K/mo Readwise arithmetic; DRAM ~$2/GB vs S3 ~2¢/GB; ~$700K first raise; 2022 context windows 4–8KB; launch October 2023; Shopify 2013–2021.
